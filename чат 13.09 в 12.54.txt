
GPT-6 Astra
get_page_content
Содержимое страницы
get_page_content
Содержимое страницы
Размышление




Предыдущая правка проблему не решила. Извините — я слишком уверенно описал её как готовое исправление, не проверив выполнение на вашей панели.

В присланной строке есть только итог очереди, а не причина ошибки. По ней нельзя определить, остановилась программа до отправки заявки, на публикации TXT или при подключении сертификата.

Я проверил ваш исходник: при сбое программа записывает конкретную ошибку сюда:

job.Message = SensitiveDataRedactor.Redact(message);
_database.SaveJob(job);
AddLog("ERROR", job.Domain, job.Message);

Эта ошибка уже должна быть в программе — повторно запускать выпуск для её получения не нужно.

Что нужно сейчас
Внизу программы, в «Журнале», найдите строку ERROR для casino743.cfd рядом с итогом от 13.09.2026 11:13:20. По исходнику новые записи появляются сверху, поэтому ошибка обычно находится ниже строки WARN.
Скопируйте её целиком: щёлкните строку → Ctrl+C. Этот же текст находится во вкладке «Задания» → casino743.cfd → «Сообщение».
Также нужно исключить запуск старого EXE: предыдущий скрипт создавал отдельную сборку, а старую программу не заменял. Не закрывая программу, выполните в PowerShell:

Get-Process -Name TextFileProcessor -ErrorAction Stop |
 Select-Object Id, Path |
 Format-List

Это только покажет путь запущенного EXE — ничего не изменит.

Пришлите текст ERROR и показанный путь. Создание сайтов, псевдонимы, БД и последовательную очередь менять не требуется. Без конкретной ошибки очередной большой «исправляющий» скрипт был бы снова догадкой; я не хочу повторять это за ваш счёт.

13.09.2026 11:13:15 INFO casino743.cfd Запуск выбранного задания 1/1.

13.09.2026 11:13:20 ERROR casino743.cfd Сайт casino743.cfd и оба псевдонима проверены. Этап запроса сертификата не завершён: Wildcard — отправка заявки Wildcard: ISPmanager: функция letsencrypt.generate; ошибка exists; поле crtname.
GPT-6 Astra
GPT-6 Astra
Навык активирован: web_search
Теперь причина понятна: ISPmanager отклоняет заявку из-за занятого имени сертификата — exists в поле crtname. Это не ошибка TXT-проверки: данный вызов letsencrypt.generate не завершился созданием новой заявки.

При этом занятое имя не означает, что сертификат уже выпущен. В панели может находиться готовый сертификат либо объект незавершённой заявки. По одной строке журнала различить их нельзя.

Что необходимо исправить в программе
Повторный запуск должен сначала проверять существующий объект, а не безусловно повторять создание:

Есть подходящий действующий сертификат — проверить покрытие домена и wildcard, затем его привязку к сайту.
Есть незавершённая заявка — продолжить её обработку, включая получение и проверку TXT.
Имя занято другим объектом — сообщить о конфликте, не удаляя и не заменяя его автоматически.
Просто игнорировать exists и считать выпуск успешным нельзя. Создавать при каждом повторе новое имя тоже не стоит.

Сейчас — одна проверка без изменений на сервере
Выполните по SSH этот блок целиком. Он читает список сертификатов пользователя www-root и выводит только записи, содержащие casino743.cfd, без ключей и паролей:

python3 - <<'PY'
import subprocess
import sys
import xml.etree.ElementTree as ET

try:
    p = subprocess.run(
        ["/usr/local/mgr5/sbin/mgrctl", "-m", "ispmgr",
         "sslcert", "su=www-root", "out=xml", "lang=en"],
        stdout=subprocess.PIPE,
        stderr=subprocess.PIPE,
        universal_newlines=True,
        timeout=90
    )
except (OSError, subprocess.TimeoutExpired):
    sys.exit("Не удалось получить список сертификатов.")

try:
    root = ET.fromstring(p.stdout)
except ET.ParseError:
    sys.exit("Панель не вернула корректный XML.")

error = root.find(".//error")
if error is not None:
    sys.exit("Ошибка панели: type={}; object={}".format(
        error.get("type", ""), error.get("object", "")
    ))
if p.returncode != 0 or root.tag != "doc":
    sys.exit("Неожиданный ответ панели.")

fields = (
    "key", "name", "info", "type", "valid_after",
    "active", "is_letsencrypt", "letsencrypt", "webdomains"
)
found = False
for elem in root.findall(".//elem"):
    values = {f: elem.findtext(f, default="") for f in fields}
    if not any("casino743.cfd" in v for v in values.values()):
        continue
    found = True
    print("---")
    for field in fields:
        print("{}: {}".format(field, values[field]))

if not found:
    print("Совпадений у www-root не найдено. Нужно уточнить crtname и владельца заявки.")
PY

Пришлите результат и фрагмент актуального кода, который вызывает letsencrypt.generate и задаёт crtname. Это позволит подготовить точечную правку именно обработки повторного запуска. Создание сайта, псевдонимов и порядок очереди для устранения этого конфликта менять не требуется.

> if not found:
>
> print("Совпадений у www-root не найдено. Нужно уточнить crtname и владельца заявки.")
>
> PY
Совпадений у www-root не найдено. Нужно уточнить crtname и владельца заявки.
[root@mail ~]#
GPT-6 Astra
GPT-6 Astra
get_serp
Поисковая выдача
Результаты (10)
Проверка не нашла совпадений, но это ещё не означает, что конфликтующего сертификата нет. Предыдущий скрипт искал только casino743.cfd в отдельных полях списка пользователя www-root. Программа могла отправить другое имя сертификата или работать от другого пользователя. Я преждевременно выбрал www-root для проверки.

Сейчас достоверно известно:

проверка сайта и псевдонимов прошла;
ISPmanager вернул exists для поля crtname;
какое именно значение crtname отправила программа, в журнале не указано.
По документации ISPmanager, crtname — это имя сертификата в панели, а не обязательно имя домена. Поэтому поиск только по домену мог пропустить конфликт.

Следующий шаг — посмотреть фактические параметры заявки
В исходниках программы найдите letsencrypt.generate и пришлите фрагмент, где формируются параметры этого вызова. Если crtname вычисляется отдельно, нужен и соответствующий участок.

Нужно увидеть значения или способ формирования:

crtname
name
username
domain_name
domain

Также важно, передаётся ли su и под каким пользователем программа подключается к панели. Пароль, токен и строку авторизации не присылайте.

Если исходников под рукой нет, пришлите доступную запись исходящего запроса с этими параметрами, удалив секреты.

Пока ничего удалять и повторно запускать выпуск не нужно. Следует сначала установить точное имя и владельца конфликтующего объекта — иначе очередная команда снова будет проверять предположение, а не причину ошибки.

ult jy& пришли команду повершелл
GPT-6 Astra
GPT-6 Astra
Вот команда для PowerShell на компьютере, где находятся исходники программы. Она откроет выбор папки и найдёт код с letsencrypt.generate и crtname. Файлы не изменяет, заявки не отправляет.

Вставьте весь блок целиком:

Add-Type -AssemblyName System.Windows.Forms

$dialog = New-Object System.Windows.Forms.FolderBrowserDialog
$dialog.Description = "Выберите папку с исходниками программы (файлы .cs или .sln)"

if ($dialog.ShowDialog() -eq [System.Windows.Forms.DialogResult]::OK) {
    $files = Get-ChildItem -LiteralPath $dialog.SelectedPath `
        -Recurse -File -Filter "*.cs" -ErrorAction SilentlyContinue |
        Where-Object { $_.FullName -notmatch '\\(bin|obj|\.git)\\' }

    $matches = @(
        $files | Select-String `
            -Pattern 'letsencrypt\.generate|crtname' `
            -Context 12,20
    )

    if ($matches.Count -gt 0) {
        $matches | ForEach-Object {
            Write-Host "`n========== $($_.Path):$($_.LineNumber) =========="
            $_.Context.PreContext
            ">>> " + $_.Line
            $_.Context.PostContext
        }
    } else {
        Write-Host "Совпадений нет. Проверьте, что выбрана папка исходников, а не только папка с EXE."
    }
}

$dialog.Dispose()

Пришлите выведенные фрагменты. Перед отправкой уберите пароли, токены и значения auth, если они окажутся в соседних строках.

PS C:\Users\User\Desktop\DomainProgram-Fix-20260910-231222\source\346Jvfch75v6f634vf765-60ecea8c7bc346c3ed310f3c416beb6549390cac> Add-Type -AssemblyName System.Windows.Forms
>>
>> $dialog = New-Object System.Windows.Forms.FolderBrowserDialog
>> $dialog.Description = "Выберите папку с исходниками программы (файлы .cs или .sln)"
>>
>> if ($dialog.ShowDialog() -eq [System.Windows.Forms.DialogResult]::OK) {
>> $files = Get-ChildItem -LiteralPath $dialog.SelectedPath `
>> -Recurse -File -Filter "*.cs" -ErrorAction SilentlyContinue |
>> Where-Object { $_.FullName -notmatch '\\(bin|obj|\.git)\\' }
>>
>> $matches = @(
>> $files | Select-String `
>> -Pattern 'letsencrypt\.generate|crtname' `
>> -Context 12,20
>> )
>>
>> if ($matches.Count -gt 0) {
>> $matches | ForEach-Object {
>> Write-Host "`n========== $($_.Path):$($_.LineNumber) =========="
>> $_.Context.PreContext
>> ">>> " + $_.Line
>> $_.Context.PostContext
>> }
>> } else {
>> Write-Host "Совпадений нет. Проверьте, что выбрана папка исходников, а не только папка с EXE."
>> }
>> }
>>
>> $dialog.Dispose()
>>

========== C:\Users\User\Desktop\DomainProgram-Fix-20260910-231222\DeploymentSchemaFix.original.cs:258 ==========
throw new InvalidOperationException(
"Для этого домена сохранён незавершённый запрос сертификата. " +
"Его исход неизвестен: проверьте ISPmanager перед повторным выпуском. " +
"Файл состояния: " + stateFile);
}

var certificateParameters = new Dictionary<string, string>
{
["out"] = "devel",
["username"] = expectedOwner,
["domain_name"] = domain,
["aliases"] = $"www.{domain} *.{domain}",
>>> ["crtname"] = domain + "_le1",
["email"] = "webmaster@" + domain,
["from_webdomain"] = "on",
["wildcard"] = "on",
["dns_check"] = "on",
["enable_cert"] = "on"
};

var certificateForm = await api.Call(
"letsencrypt.generate", certificateParameters, ct);

var certificateFields = FormValues(certificateForm);
foreach (var pair in certificateParameters)
certificateFields[pair.Key] = pair.Value;

certificateFields["out"] = "xml";
certificateFields["sok"] = "ok";

// Записываем намерение до сетевой мутации.
// При таймауте не отправляем повторный запрос автоматически.
using (var stream = new FileStream(

========== C:\Users\User\Desktop\DomainProgram-Fix-20260910-231222\DeploymentSchemaFix.original.cs:267 ==========
["username"] = expectedOwner,
["domain_name"] = domain,
["aliases"] = $"www.{domain} *.{domain}",
["crtname"] = domain + "_le1",
["email"] = "webmaster@" + domain,
["from_webdomain"] = "on",
["wildcard"] = "on",
["dns_check"] = "on",
["enable_cert"] = "on"
};

var certificateForm = await api.Call(
>>> "letsencrypt.generate", certificateParameters, ct);

var certificateFields = FormValues(certificateForm);
foreach (var pair in certificateParameters)
certificateFields[pair.Key] = pair.Value;

certificateFields["out"] = "xml";
certificateFields["sok"] = "ok";

// Записываем намерение до сетевой мутации.
// При таймауте не отправляем повторный запрос автоматически.
using (var stream = new FileStream(
stateFile, FileMode.CreateNew, FileAccess.Write, FileShare.None))
{
var bytes = Encoding.UTF8.GetBytes("outcome-unknown");
stream.Write(bytes, 0, bytes.Length);
stream.Flush(true);
}

await api.Call("letsencrypt.generate", certificateFields, ct);
await File.WriteAllTextAsync(stateFile, "submitted", ct);

========== C:\Users\User\Desktop\DomainProgram-Fix-20260910-231222\DeploymentSchemaFix.original.cs:286 ==========
certificateFields["sok"] = "ok";

// Записываем намерение до сетевой мутации.
// При таймауте не отправляем повторный запрос автоматически.
using (var stream = new FileStream(
stateFile, FileMode.CreateNew, FileAccess.Write, FileShare.None))
{
var bytes = Encoding.UTF8.GetBytes("outcome-unknown");
stream.Write(bytes, 0, bytes.Length);
stream.Flush(true);
}

>>> await api.Call("letsencrypt.generate", certificateFields, ct);
await File.WriteAllTextAsync(stateFile, "submitted", ct);

return new IspmanagerOperationResult
{
Success = true,
Message = $"Сайт {domain}, владелец и псевдонимы проверены. " +
"Запрос Wildcard отправлен в ISPmanager. " +
"Выпуск сертификата ещё не подтверждён: проверьте DNS-01/TXT и статус панели."
};
}
finally
{
SiteGate.Release();
}
}

internal static async Task<bool> EnsureDatabaseAsync(
IspmanagerSettings settings, string panelPassword,
DeploymentDbConfig db, CancellationToken ct)
{

========== C:\Users\User\Desktop\DomainProgram-Fix-20260910-231222\source\346Jvfch75v6f634vf765-60ecea8c7bc346c3ed310f3c416beb6549390cac\Services\DeploymentSchemaFix.cs:761 ==========
throw new InvalidOperationException(
"Есть запрос сертификата с неизвестным исходом. " +
"Сначала проверьте его в ISPmanager. " +
"Автоматический повтор не выполняется. Файл: " + stateFile);
}

var request = new Dictionary<string, string>
{
["out"] = "devel",
["username"] = owner,
["domain_name"] = domain,
["aliases"] = $"www.{domain} *.{domain}",
>>> ["crtname"] = domain + "_le1",
["email"] = "webmaster@" + domain,
["from_webdomain"] = "on",
["wildcard"] = "on",
["dns_check"] = "on",
["enable_cert"] = "on"
};

var form = await api.Call(
"letsencrypt.generate", request, ct);

RequireForm(form, "letsencrypt.generate");
RequireControls(form, "wildcard");

var fields = FormValues(form);

foreach (var pair in request)
fields[pair.Key] = pair.Value;

fields["out"] = "xml";
fields["sok"] = "ok";

========== C:\Users\User\Desktop\DomainProgram-Fix-20260910-231222\source\346Jvfch75v6f634vf765-60ecea8c7bc346c3ed310f3c416beb6549390cac\Services\DeploymentSchemaFix.cs:770 ==========
["username"] = owner,
["domain_name"] = domain,
["aliases"] = $"www.{domain} *.{domain}",
["crtname"] = domain + "_le1",
["email"] = "webmaster@" + domain,
["from_webdomain"] = "on",
["wildcard"] = "on",
["dns_check"] = "on",
["enable_cert"] = "on"
};

var form = await api.Call(
>>> "letsencrypt.generate", request, ct);

RequireForm(form, "letsencrypt.generate");
RequireControls(form, "wildcard");

var fields = FormValues(form);

foreach (var pair in request)
fields[pair.Key] = pair.Value;

fields["out"] = "xml";
fields["sok"] = "ok";

ct.ThrowIfCancellationRequested();

// CreateNew не позволяет двум процессам одновременно
// отправить запрос с одним локальным ключом состояния.
using (var stream = new FileStream(
stateFile,
FileMode.CreateNew,
FileAccess.Write,

========== C:\Users\User\Desktop\DomainProgram-Fix-20260910-231222\source\346Jvfch75v6f634vf765-60ecea8c7bc346c3ed310f3c416beb6549390cac\Services\DeploymentSchemaFix.cs:772 ==========
["aliases"] = $"www.{domain} *.{domain}",
["crtname"] = domain + "_le1",
["email"] = "webmaster@" + domain,
["from_webdomain"] = "on",
["wildcard"] = "on",
["dns_check"] = "on",
["enable_cert"] = "on"
};

var form = await api.Call(
"letsencrypt.generate", request, ct);

>>> RequireForm(form, "letsencrypt.generate");
RequireControls(form, "wildcard");

var fields = FormValues(form);

foreach (var pair in request)
fields[pair.Key] = pair.Value;

fields["out"] = "xml";
fields["sok"] = "ok";

ct.ThrowIfCancellationRequested();

// CreateNew не позволяет двум процессам одновременно
// отправить запрос с одним локальным ключом состояния.
using (var stream = new FileStream(
stateFile,
FileMode.CreateNew,
FileAccess.Write,
FileShare.None))
{

========== C:\Users\User\Desktop\DomainProgram-Fix-20260910-231222\source\346Jvfch75v6f634vf765-60ecea8c7bc346c3ed310f3c416beb6549390cac\Services\DeploymentSchemaFix.cs:800 ==========
stateFile,
FileMode.CreateNew,
FileAccess.Write,
FileShare.None))
{
var bytes = Encoding.UTF8.GetBytes("outcome-unknown");
stream.Write(bytes, 0, bytes.Length);
stream.Flush(true);
}

// При ошибке состояние остаётся outcome-unknown:
// не повторяем выпуск автоматически.
>>> await api.Call("letsencrypt.generate", fields, ct);
await File.WriteAllTextAsync(stateFile, "submitted", ct);

return new IspmanagerOperationResult
{
Success = true,
Message =
$"Сайт {domain}, владелец и оба псевдонима проверены. " +
"Запрос Wildcard отправлен в ISPmanager. " +
"Это не подтверждение завершённого выпуска сертификата. " +
"Для Wildcard требуется DNS-01; при внешнем DNS " +
"может понадобиться добавление актуальных TXT-записей. " +
"Проверьте выдачу и подключение сертификата в панели."
};
}

private static async Task<bool> DatabaseExists(
Api api,
string name,
string server,
string owner,

========== C:\Users\User\Desktop\DomainProgram-Fix-20260910-231222\Wildcard-Fix-20260912-192850-915\source\Services\DeploymentSchemaFix.Wildcard.cs:339 ==========
i.ToString(CultureInfo.InvariantCulture))
.FirstOrDefault(n => !usedNames.Contains(n))
?? throw new InvalidOperationException(
"Не найдено свободное имя сертификата.");

var request = new Dictionary<string, string>
{
["out"] = "devel",
["lang"] = "en",
["username"] = owner,
["domain_name"] = domain,
["aliases"] = "*." + domain,
>>> ["crtname"] = certificateName,
["email"] = "webmaster@" + domain,
["from_webdomain"] = "on",
["wildcard"] = "on",
["dns_check"] = "on",
// Подключаем сами только готовый сертификат.
["enable_cert"] = "off"
};

var form = await api.Call(
"letsencrypt.generate", request, ct);

RequireForm(form, "letsencrypt.generate");
RequireControls(form, "wildcard");

var fields = FormValues(form);
foreach (var pair in request)
fields[pair.Key] = pair.Value;

fields["out"] = "xml";
fields["sok"] = "ok";

========== C:\Users\User\Desktop\DomainProgram-Fix-20260910-231222\Wildcard-Fix-20260912-192850-915\source\Services\DeploymentSchemaFix.Wildcard.cs:349 ==========
["domain_name"] = domain,
["aliases"] = "*." + domain,
["crtname"] = certificateName,
["email"] = "webmaster@" + domain,
["from_webdomain"] = "on",
["wildcard"] = "on",
["dns_check"] = "on",
// Подключаем сами только готовый сертификат.
["enable_cert"] = "off"
};

var form = await api.Call(
>>> "letsencrypt.generate", request, ct);

RequireForm(form, "letsencrypt.generate");
RequireControls(form, "wildcard");

var fields = FormValues(form);
foreach (var pair in request)
fields[pair.Key] = pair.Value;

fields["out"] = "xml";
fields["sok"] = "ok";

ct.ThrowIfCancellationRequested();

await WcWriteStateAsync(
stateFile, "outcome-unknown", ct);

// Один запрос выпуска, без автоматического
// повторения POST при сетевой ошибке.
await api.Call("letsencrypt.generate", fields, ct);


========== C:\Users\User\Desktop\DomainProgram-Fix-20260910-231222\Wildcard-Fix-20260912-192850-915\source\Services\DeploymentSchemaFix.Wildcard.cs:351 ==========
["crtname"] = certificateName,
["email"] = "webmaster@" + domain,
["from_webdomain"] = "on",
["wildcard"] = "on",
["dns_check"] = "on",
// Подключаем сами только готовый сертификат.
["enable_cert"] = "off"
};

var form = await api.Call(
"letsencrypt.generate", request, ct);

>>> RequireForm(form, "letsencrypt.generate");
RequireControls(form, "wildcard");

var fields = FormValues(form);
foreach (var pair in request)
fields[pair.Key] = pair.Value;

fields["out"] = "xml";
fields["sok"] = "ok";

ct.ThrowIfCancellationRequested();

await WcWriteStateAsync(
stateFile, "outcome-unknown", ct);

// Один запрос выпуска, без автоматического
// повторения POST при сетевой ошибке.
await api.Call("letsencrypt.generate", fields, ct);

submittedHere = true;
submittedAt = DateTime.UtcNow;

========== C:\Users\User\Desktop\DomainProgram-Fix-20260910-231222\Wildcard-Fix-20260912-192850-915\source\Services\DeploymentSchemaFix.Wildcard.cs:368 ==========
fields[pair.Key] = pair.Value;

fields["out"] = "xml";
fields["sok"] = "ok";

ct.ThrowIfCancellationRequested();

await WcWriteStateAsync(
stateFile, "outcome-unknown", ct);

// Один запрос выпуска, без автоматического
// повторения POST при сетевой ошибке.
>>> await api.Call("letsencrypt.generate", fields, ct);

submittedHere = true;
submittedAt = DateTime.UtcNow;

await WcWriteStateAsync(
stateFile, "submitted", ct);
}
else if (DateTime.UtcNow - submittedAt >
TimeSpan.FromMinutes(3))
{
throw new InvalidOperationException(
"После отправки запроса Wildcard-заявка " +
"не появилась в списке сертификатов. " +
"Повторный запрос не отправлен. Проверьте панель.");
}

await Task.Delay(TimeSpan.FromSeconds(15), ct);
}
}
catch (OperationCanceledException)

========== C:\Users\User\Desktop\DomainProgram-Fix-20260910-231222\WildcardOnly-20260913-092114-435\source\Services\DeploymentSchemaFix.WildcardOnly.cs:214 ==========
"Не найдено свободное имя сертификата.");

phase = "получение формы выпуска";

var request = new Dictionary<string, string>
{
["out"] = "devel",
["username"] = owner,
["domain_name"] = domain,
// www покрывается Wildcard и отдельно в заявку не нужен.
// Псевдонимы самого сайта не изменяются.
["aliases"] = "*." + domain,
>>> ["crtname"] = certificateName,
["email"] = "webmaster@" + domain,
["from_webdomain"] = "on",
["wildcard"] = "on",
["dns_check"] = "on",
["enable_cert"] = "on"
};

var form = await api.Call(
"letsencrypt.generate", request, ct);

RequireForm(form, "letsencrypt.generate");
RequireControls(form, "wildcard");

var fields = FormValues(form);
foreach (var pair in request)
fields[pair.Key] = pair.Value;

fields["out"] = "xml";
fields["sok"] = "ok";


========== C:\Users\User\Desktop\DomainProgram-Fix-20260910-231222\WildcardOnly-20260913-092114-435\source\Services\DeploymentSchemaFix.WildcardOnly.cs:223 ==========
// www покрывается Wildcard и отдельно в заявку не нужен.
// Псевдонимы самого сайта не изменяются.
["aliases"] = "*." + domain,
["crtname"] = certificateName,
["email"] = "webmaster@" + domain,
["from_webdomain"] = "on",
["wildcard"] = "on",
["dns_check"] = "on",
["enable_cert"] = "on"
};

var form = await api.Call(
>>> "letsencrypt.generate", request, ct);

RequireForm(form, "letsencrypt.generate");
RequireControls(form, "wildcard");

var fields = FormValues(form);
foreach (var pair in request)
fields[pair.Key] = pair.Value;

fields["out"] = "xml";
fields["sok"] = "ok";

ct.ThrowIfCancellationRequested();

using (var stateStream = new FileStream(
attemptFile,
FileMode.CreateNew,
FileAccess.Write,
FileShare.None))
{
var bytes = Encoding.UTF8.GetBytes(

========== C:\Users\User\Desktop\DomainProgram-Fix-20260910-231222\WildcardOnly-20260913-092114-435\source\Services\DeploymentSchemaFix.WildcardOnly.cs:225 ==========
["aliases"] = "*." + domain,
["crtname"] = certificateName,
["email"] = "webmaster@" + domain,
["from_webdomain"] = "on",
["wildcard"] = "on",
["dns_check"] = "on",
["enable_cert"] = "on"
};

var form = await api.Call(
"letsencrypt.generate", request, ct);

>>> RequireForm(form, "letsencrypt.generate");
RequireControls(form, "wildcard");

var fields = FormValues(form);
foreach (var pair in request)
fields[pair.Key] = pair.Value;

fields["out"] = "xml";
fields["sok"] = "ok";

ct.ThrowIfCancellationRequested();

using (var stateStream = new FileStream(
attemptFile,
FileMode.CreateNew,
FileAccess.Write,
FileShare.None))
{
var bytes = Encoding.UTF8.GetBytes(
"outcome-unknown\n" + certificateName);
stateStream.Write(bytes);

========== C:\Users\User\Desktop\DomainProgram-Fix-20260910-231222\WildcardOnly-20260913-092114-435\source\Services\DeploymentSchemaFix.WildcardOnly.cs:250 ==========
attemptFile,
FileMode.CreateNew,
FileAccess.Write,
FileShare.None))
{
var bytes = Encoding.UTF8.GetBytes(
"outcome-unknown\n" + certificateName);
stateStream.Write(bytes);
stateStream.Flush(true);
}

phase = "отправка заявки Wildcard";
>>> await api.Call("letsencrypt.generate", fields, ct);
}

// Все последующие действия продолжают серверную заявку.
// Повторный letsencrypt.generate в этом цикле не отправляется.
var finishBy = DateTime.UtcNow.AddMinutes(18);
string lastTxtSet = "";
bool retriedForCurrentSet = false;
string lastState = "ожидание появления заявки";

while (DateTime.UtcNow < finishBy)
{
ct.ThrowIfCancellationRequested();

phase = "проверка состояния выпуска";
certificates = await Wc2Certificates(api, owner, ct);

ready = certificates
.Where(row => Wc2Ready(row, domain))
.OrderByDescending(row => Wc2Field(row, "valid_after"))
.FirstOrDefault();

========== C:\Users\User\Desktop\DomainProgram-Fix-20260910-231222\WildcardOnly-20260913-092114-435\source\Services\DeploymentSchemaFix.WildcardOnly.cs:254 ==========
{
var bytes = Encoding.UTF8.GetBytes(
"outcome-unknown\n" + certificateName);
stateStream.Write(bytes);
stateStream.Flush(true);
}

phase = "отправка заявки Wildcard";
await api.Call("letsencrypt.generate", fields, ct);
}

// Все последующие действия продолжают серверную заявку.
>>> // Повторный letsencrypt.generate в этом цикле не отправляется.
var finishBy = DateTime.UtcNow.AddMinutes(18);
string lastTxtSet = "";
bool retriedForCurrentSet = false;
string lastState = "ожидание появления заявки";

while (DateTime.UtcNow < finishBy)
{
ct.ThrowIfCancellationRequested();

phase = "проверка состояния выпуска";
certificates = await Wc2Certificates(api, owner, ct);

ready = certificates
.Where(row => Wc2Ready(row, domain))
.OrderByDescending(row => Wc2Field(row, "valid_after"))
.FirstOrDefault();

if (ready is not null)
{
phase = "назначение Wildcard и проверка HTTPS";
PS C:\Users\User\Desktop\DomainProgram-Fix-20260910-231222\source\346Jvfch75v6f634vf765-60ecea8c7bc346c3ed310f3c416beb6549390cac>
GPT-6 Astra
GPT-6 Astra
В выводе несколько версий программы. В старой имя фиксировано (domain + "_le1"), а в WildcardOnly оно вычисляется через certificateName. Поэтому менять _le1 в старом файле сейчас не нужно.

Текст ошибки «отправка заявки Wildcard» совпадает с этапом в DeploymentSchemaFix.WildcardOnly.cs: в этом коде форма уже получена, а ошибка возникает при отправке заполненной заявки. Нужно проверить, как выбирается имя и как читается список занятых имён.

Выполните этот блок в PowerShell. Он покажет путь работающей программы и нужные участки последней версии исходника — ничего не изменит:

& {
    $ErrorActionPreference = "Stop"

    Write-Host "`n=== ПУТЬ ЗАПУЩЕННОЙ ПРОГРАММЫ ==="
    $processes = @(Get-Process -Name TextFileProcessor -ErrorAction SilentlyContinue)

    if ($processes.Count) {
        $processes | Select-Object Id, Path | Format-List | Out-Host
    } else {
        Write-Host "Процесс TextFileProcessor не найден."
    }

    $base = Join-Path $env:USERPROFILE "Desktop\DomainProgram-Fix-20260910-231222"
    $file = Join-Path $base "WildcardOnly-20260913-092114-435\source\Services\DeploymentSchemaFix.WildcardOnly.cs"

    if (-not (Test-Path -LiteralPath $file -PathType Leaf)) {
        Write-Host "Исходник не найден по пути: $file"
        return
    }

    $lines = @(Get-Content -LiteralPath $file -Encoding UTF8)

    Write-Host "`n=== ВЫБОР ИМЕНИ И ОТПРАВКА ЗАЯВКИ: строки 130–250 ==="
    for ($i = 129; $i -lt [Math]::Min(250, $lines.Count); $i++) {
        "{0,4}: {1}" -f ($i + 1), $lines[$i]
    }

    Write-Host "`n=== МЕТОДЫ ЧТЕНИЯ СЕРТИФИКАТОВ И ИХ ПОЛЕЙ ==="
    $definitions = @(
        Select-String -LiteralPath $file -Encoding UTF8 `
            -Pattern '^\s*(?:private|internal|public)\s+.*\b(Wc2Certificates|Wc2Field)\s*\('
    )

    if ($definitions.Count) {
        foreach ($hit in $definitions) {
            $start = $hit.LineNumber - 1
            $end = [Math]::Min($start + 70, $lines.Count)
            Write-Host "`n--- Начиная со строки $($hit.LineNumber) ---"
            for ($i = $start; $i -lt $end; $i++) {
                "{0,4}: {1}" -f ($i + 1), $lines[$i]
            }
        }
    } else {
        Write-Host "Объяв

Продолжить
GPT-6 Astra
GPT-6 Astra
Предыдущий блок оборвался. Если PowerShell показывает >>, нажмите Ctrl+C, затем вставьте этот блок целиком вместо предыдущего.

Он только читает исходник и показывает путь запущенной программы — ничего не изменяет.

& {
    $ErrorActionPreference = "Stop"

    Write-Host "`n=== ПУТЬ ЗАПУЩЕННОЙ ПРОГРАММЫ ==="

    $processes = @(
        Get-Process -Name TextFileProcessor -ErrorAction SilentlyContinue
    )

    if ($processes.Count -gt 0) {
        $processes |
            Select-Object Id, Path |
            Format-List |
            Out-Host
    } else {
        Write-Host "Процесс TextFileProcessor не найден."
    }

    $base = Join-Path $env:USERPROFILE `
        "Desktop\DomainProgram-Fix-20260910-231222"

    $file = Join-Path $base `
        "WildcardOnly-20260913-092114-435\source\Services\DeploymentSchemaFix.WildcardOnly.cs"

    if (-not (Test-Path -LiteralPath $file -PathType Leaf)) {
        Write-Host "Исходник не найден: $file"
        return
    }

    $lines = @(Get-Content -LiteralPath $file -Encoding UTF8)

    Write-Host "`n=== ВЫБОР ИМЕНИ И ОТПРАВКА ЗАЯВКИ: строки 130–250 ==="

    for ($i = 129; $i -lt [Math]::Min(250, $lines.Count); $i++) {
        "{0,4}: {1}" -f ($i + 1), $lines[$i]
    }

    Write-Host "`n=== МЕТОДЫ ЧТЕНИЯ СЕРТИФИКАТОВ И ИХ ПОЛЕЙ ==="

    $definitions = @(
        Select-String -LiteralPath $file -Encoding UTF8 `
            -Pattern '^\s*(?:private|internal|public)\s+.*\b(Wc2Certificates|Wc2Field)\s*\('
    )

    if ($definitions.Count -gt 0) {
        foreach ($hit in $definitions) {
            $start = $hit.LineNumber - 1
            $end = [Math]::Min($start + 70, $lines.Count)

            Write-Host "`n--- Начиная со строки $($hit.LineNumber) ---"

            for ($i = $start; $i -lt $end; $i++) {
                "{0,4}: {1}" -f ($i + 1), $lines[$i]
            }
        }
    } else {
        Write-Host "Объявления не найдены. Показываю места использования:"

        Select-String -LiteralPath $file -Encoding UTF8 `
            -Pattern '\b(Wc2Certificates|Wc2Field)\s*\(' `
            -Context 3,12 |
            Out-String -Width 240 |
            Write-Host
    }

    Write-Host "`n=== ПРОВЕРКА ЗАВЕРШЕНА ==="
}

Пришлите вывод, предварительно убрав пароли и токены, если они встретятся в коде. Пока не меняйте имя сертификата и не удаляйте файл состояния заявки.

=== ПУТЬ ЗАПУЩЕННОЙ ПРОГРАММЫ ===

Id : 5768
Path : C:\Users\User\Desktop\DomainProgram-Fix-20260910-231222\WildcardOnly-20260913-092114-435\APP\TextFileProcessor.e
xe


=== ВЫБОР ИМЕНИ И ОТПРАВКА ЗАЯВКИ: строки 130–250 ===
130: phase = "подключение существующего Wildcard";
131: await Wc2AttachAndVerify(
132: api, ready, domain, owner, ct);
133:
134: return Wc2Success(domain);
135: }
136:
137: var pending = certificates
138: .Where(row => Wc2Covers(row, domain) && Wc2Pending(row))
139: .ToList();
140:
141: if (pending.Count > 1)
142: throw new InvalidOperationException(
143: "Найдено несколько незавершённых Wildcard-заявок. " +
144: "Автоматический выбор остановлен.");
145:
146: phase = "чтение сохранённых настроек Spaceship";
147: dns = Wc2Dns.Load();
148:
149: phase = "проверка DNS перед выпуском";
150: await dns.Preflight(domain, ct);
151:
152: if (pending.Count == 0)
153: {
154: // Новый маркер этой версии означает, что запрос уже мог
155: // быть принят. Сначала необходимо обнаружить его в панели.
156: // Не отправляем повторный POST после неизвестного исхода.
157: if (File.Exists(attemptFile))
158: throw new InvalidOperationException(
159: "Эта версия уже отправляла заявку, но сейчас " +
160: "подходящий сертификат или ожидающая заявка " +
161: "не обнаружены. Автоматический повтор остановлен. " +
162: "Проверьте результат заявки в ISPmanager. " +
163: "Маркер не удалялся.");
164:
165: var oldState = Path.Combine(directory, key + ".state");
166: if (File.Exists(oldState))
167: {
168: var state = (await File.ReadAllTextAsync(oldState, ct)).Trim();
169: if (state.Length > 0 && state != "submitted")
170: throw new InvalidOperationException(
171: "У прежней заявки неизвестный исход, а в панели " +
172: "не найдена ожидающая Wildcard-заявка. " +
173: "Новая заявка не отправлена.");
174:
175: // Старое submitted не считается выпущенным сертификатом.
176: // Отсутствие подходящего сертификата уже проверено в панели.
177: }
178:
179: var names = certificates
180: .SelectMany(row => new
181: {
182: Wc2CertKey(row), Wc2Field(row, "name")
183: })
184: .ToHashSet(StringComparer.Ordinal);
185:
186: string certificateName = "";
187: for (int number = 1; number <= 999; number++)
188: {
189: var candidate = domain + "_le" +
190: number.ToString(CultureInfo.InvariantCulture);
191:
192: if (!names.Contains(candidate) &&
193: !names.Contains(owner + "%#%" + candidate))
194: {
195: certificateName = candidate;
196: break;
197: }
198: }
199:
200: if (certificateName.Length == 0)
201: throw new InvalidOperationException(
202: "Не найдено свободное имя сертификата.");
203:
204: phase = "получение формы выпуска";
205:
206: var request = new Dictionary<string, string>
207: {
208: ["out"] = "devel",
209: ["username"] = owner,
210: ["domain_name"] = domain,
211: // www покрывается Wildcard и отдельно в заявку не нужен.
212: // Псевдонимы самого сайта не изменяются.
213: ["aliases"] = "*." + domain,
214: ["crtname"] = certificateName,
215: ["email"] = "webmaster@" + domain,
216: ["from_webdomain"] = "on",
217: ["wildcard"] = "on",
218: ["dns_check"] = "on",
219: ["enable_cert"] = "on"
220: };
221:
222: var form = await api.Call(
223: "letsencrypt.generate", request, ct);
224:
225: RequireForm(form, "letsencrypt.generate");
226: RequireControls(form, "wildcard");
227:
228: var fields = FormValues(form);
229: foreach (var pair in request)
230: fields[pair.Key] = pair.Value;
231:
232: fields["out"] = "xml";
233: fields["sok"] = "ok";
234:
235: ct.ThrowIfCancellationRequested();
236:
237: using (var stateStream = new FileStream(
238: attemptFile,
239: FileMode.CreateNew,
240: FileAccess.Write,
241: FileShare.None))
242: {
243: var bytes = Encoding.UTF8.GetBytes(
244: "outcome-unknown\n" + certificateName);
245: stateStream.Write(bytes);
246: stateStream.Flush(true);
247: }
248:
249: phase = "отправка заявки Wildcard";
250: await api.Call("letsencrypt.generate", fields, ct);

=== МЕТОДЫ ЧТЕНИЯ СЕРТИФИКАТОВ И ИХ ПОЛЕЙ ===

--- Начиная со строки 28 ---
28: private static string Wc2Field(XElement row, string name) =>
29: row.Element(name)?.Value.Trim() ?? "";
30:
31: private static string Wc2CertKey(XElement row)
32: {
33: var key = Wc2Field(row, "key");
34: return key.Length > 0 ? key : Wc2Field(row, "name");
35: }
36:
37: private static bool Wc2Covers(XElement row, string domain)
38: {
39: var names = SplitValues(Wc2Field(row, "info"))
40: .Select(x => x.TrimEnd('.'))
41: .ToHashSet(StringComparer.OrdinalIgnoreCase);
42:
43: return names.Contains(domain) &&
44: names.Contains("*." + domain);
45: }
46:
47: private static bool Wc2Pending(XElement row) =>
48: Wc2Field(row, "letsencrypt_verifying") == "on" ||
49: Wc2Field(row, "letsencrypt_txt") == "on";
50:
51: private static bool Wc2Ready(XElement row, string domain)
52: {
53: return Wc2Covers(row, domain) &&
54: Wc2Field(row, "type") == "ssl_existing" &&
55: !Wc2Pending(row) &&
56: Wc2Field(row, "letsencrypt_failed") != "on" &&
57: DateTime.TryParseExact(
58: Wc2Field(row, "valid_after"),
59: "yyyy-MM-dd",
60: CultureInfo.InvariantCulture,
61: DateTimeStyles.None,
62: out var end) &&
63: end.Date > DateTime.UtcNow.Date.AddDays(1);
64: }
65:
66: private static async Task<List<XElement>> Wc2Certificates(
67: Api api, string owner, CancellationToken ct)
68: {
69: var doc = await api.Call(
70: "sslcert",
71: new Dictionary<string, string>
72: {
73: ["out"] = "xml",
74: ["lang"] = "en",
75: ["su"] = owner
76: }, ct);
77:
78: return doc.Root!.Elements("elem").ToList();
79: }
80:
81: private static async Task<IspmanagerOperationResult> Wc2Run(
82: Api api,
83: IspmanagerSettings settings,
84: string domain,
85: string owner,
86: CancellationToken ct)
87: {
88: var phase = "проверка существующих сертификатов";
89: var directory = Path.Combine(
90: Environment.GetFolderPath(
91: Environment.SpecialFolder.LocalApplicationData),
92: "TextFileProcessor",
93: "deployment-certificate-requests");
94:
95: Directory.CreateDirectory(directory);
96:
97: var panel = new UriBuilder(settings.PanelUrl)

--- Начиная со строки 66 ---
66: private static async Task<List<XElement>> Wc2Certificates(
67: Api api, string owner, CancellationToken ct)
68: {
69: var doc = await api.Call(
70: "sslcert",
71: new Dictionary<string, string>
72: {
73: ["out"] = "xml",
74: ["lang"] = "en",
75: ["su"] = owner
76: }, ct);
77:
78: return doc.Root!.Elements("elem").ToList();
79: }
80:
81: private static async Task<IspmanagerOperationResult> Wc2Run(
82: Api api,
83: IspmanagerSettings settings,
84: string domain,
85: string owner,
86: CancellationToken ct)
87: {
88: var phase = "проверка существующих сертификатов";
89: var directory = Path.Combine(
90: Environment.GetFolderPath(
91: Environment.SpecialFolder.LocalApplicationData),
92: "TextFileProcessor",
93: "deployment-certificate-requests");
94:
95: Directory.CreateDirectory(directory);
96:
97: var panel = new UriBuilder(settings.PanelUrl)
98: {
99: Query = "",
100: Fragment = ""
101: }.Uri.AbsoluteUri.TrimEnd('/');
102:
103: var key = Convert.ToHexString(SHA256.HashData(
104: Encoding.UTF8.GetBytes(panel + "\n" + owner + "\n" + domain)));
105:
106: var attemptFile = Path.Combine(directory, key + ".wc2-attempt");
107:
108: // Межпроцессная блокировка. Сам файл может оставаться после выхода.
109: using var gate = new FileStream(
110: Path.Combine(directory, key + ".wc2-lock"),
111: FileMode.OpenOrCreate,
112: FileAccess.ReadWrite,
113: FileShare.None);
114:
115: Wc2Dns? dns = null;
116:
117: try
118: {
119: var certificates = await Wc2Certificates(api, owner, ct);
120:
121: var ready = certificates
122: .Where(row => Wc2Ready(row, domain))
123: .OrderByDescending(row =>
124: Wc2Field(row, "active") == "on")
125: .ThenByDescending(row => Wc2Field(row, "valid_after"))
126: .FirstOrDefault();
127:
128: if (ready is not null)
129: {
130: phase = "подключение существующего Wildcard";
131: await Wc2AttachAndVerify(
132: api, ready, domain, owner, ct);
133:
134: return Wc2Success(domain);
135: }

=== ПРОВЕРКА ЗАВЕРШЕНА ===
PS C:\Users\User\Desktop\DomainProgram-Fix-20260910-231222\source\346Jvfch75v6f634vf765-60ecea8c7bc346c3ed310f3c416beb6549390cac>
GPT-6 Astra
GPT-6 Astra
get_serp
Поисковая выдача
Результаты (10)
Подтвердилось: запущена версия WildcardOnly, а не старый EXE. В ней свободное имя выбирается по списку sslcert, но при отправке ISPmanager считает выбранное имя занятым.

Вижу важное различие:

список сертификатов запрашивается с su=owner;
заявка отправляется с username=owner, без явного su в показанном словаре.
Это повод проверить контекст пользователя, но ещё не доказательство причины: итоговые параметры зависят и от реализации Api.Call.

Кроме того, перед отправкой программа сохраняет выбранное имя в .wc2-attempt. Значит, теперь можно узнать его без нового выпуска и сравнить со списком панели.

Выполните в PowerShell этот блок целиком
Он прочитает локальный маркер и через SSH запросит списки сертификатов. Введите IP или SSH-адрес того сервера, где находится ISPmanager. Пароль вводится в SSH — сюда его присылать не нужно. Команды ничего не удаляют и не выпускают.

& {
    $ErrorActionPreference = "Stop"

    Write-Host "`n=== СОХРАНЁННОЕ ИМЯ СЕРТИФИКАТА ==="

    $stateDir = Join-Path $env:LOCALAPPDATA `
        "TextFileProcessor\deployment-certificate-requests"

    $found = $false

    if (Test-Path -LiteralPath $stateDir) {
        Get-ChildItem -LiteralPath $stateDir -Filter "*.wc2-attempt" -File |
            ForEach-Object {
                $content = @(Get-Content -LiteralPath $_.FullName -Encoding UTF8)

                if ($content.Count -ge 2 -and
                    $content[1].Trim() -like "casino743.cfd*") {
                    $found = $true
                    Write-Host "Файл: $($_.Name)"
                    Write-Host "Состояние: $($content[0])"
                    Write-Host "crtname: $($content[1])"
                    Write-Host "Изменён: $($_.LastWriteTime)"
                }
            }
    }

    if (-not $found) {
        Write-Host "Маркер с именем casino743.cfd* не найден у текущего пользователя Windows."
    }

    if (-not (Get-Command ssh -ErrorAction SilentlyContinue)) {
        Write-Host "SSH-клиент Windows не найден. Пришлите результат выше."
        return
    }

    $server = (Read-Host "`nВведите IP или SSH-имя сервера ISPmanager").Trim()

    if ($server -notmatch '^[a-zA-Z0-9][a-zA-Z0-9.:-]*$') {
        Write-Host "Некорректный адрес. Укажите только IP или имя, без https:// и пути."
        return
    }

    $python = @'
import subprocess
import xml.etree.ElementTree as ET

DOMAIN = "casino743.cfd"
SAFE = (
    "key", "name", "owner", "username", "user",
    "info", "type", "active", "valid_after",
    "letsencrypt_verifying", "letsencrypt_txt",
    "letsencrypt_failed", "webdomains"
)

for scope in ("admin", "www-root"):
    print("\n=== SSLCERT SCOPE: {} ===".format(scope), flush=True)
    args = [
        "/usr/local/mgr5/sbin/mgrctl",
        "-m", "ispmgr", "sslcert", "out=xml", "lang=en"
    ]
    if scope != "admin":
        args.append("su=" + scope)

    try:
        p = subprocess.run(
            args, stdout=subprocess.PIPE, stderr=subprocess.PIPE,
            universal_newlines=True, timeout=90
        )
        root = ET.fromstring(p.stdout)
    except (OSError, subprocess.TimeoutExpired, ET.ParseError) as exc:
        print("Cannot read XML: " + type(exc).__name__)
        continue

    errors = list(root.iter("error"))
    if errors:
        for err in errors:
            print("Panel error: type={}, object={}".format(
                err.get("type", ""), err.get("object", "")
            ))
        continue

    if p.returncode != 0:
        print("Command failed: exit={}".format(p.returncode))
        continue

    rows = root.findall(".//elem")
    print("XML root: {}; direct rows: {}; all rows: {}".format(
        root.tag, len(root.findall("elem")), len(rows)
    ))

    found = False
    for row in rows:
        values = {
            field: (row.findtext(field) or "").strip()
            for field in SAFE
        }
        if not any(DOMAIN in value.lower() for value in values.values()):
            continue
        found = True
        print("---")
        for field in SAFE:
            if values[field]:
                print("{}: {}".format(field, values[field]))

    if not found:
        print("No matching rows in selected fields.")
'@

    Write-Host "`n=== ПРОВЕРКА ПАНЕЛИ ПО SSH ==="
    $python | & ssh "root@$server" "python3 -"

    if ($LASTEXITCODE -ne 0) {
        Write-Host "SSH-проверка завершилась с ошибкой: $LASTEXITCODE"
    }
}

Пришлите вывод. Он покажет фактически выбранное crtname, видимость объекта в двух контекстах и структуру списка, который разбирает программа. Маркер пока оставляем: его удаление разрешит повторную отправку, не устранив конфликт.


=== СОХРАНЁННОЕ ИМЯ СЕРТИФИКАТА ===
Файл: 8B375F4F9A06D438096CBD28A852847DF47B6E1B158E1EC697FFFA1D13AD0B2D.wc2-attempt
Состояние: outcome-unknown
crtname: casino743.cfd_le1
Изменён: 09/13/2026 11:13:20

Введите IP или SSH-имя сервера ISPmanager: 185.115.33.18

=== ПРОВЕРКА ПАНЕЛИ ПО SSH ===
root@185.115.33.18's password:

=== SSLCERT SCOPE: admin ===
XML root: doc; direct rows: 194; all rows: 194
No matching rows in selected fields.

=== SSLCERT SCOPE: www-root ===
XML root: doc; direct rows: 194; all rows: 194
No matching rows in selected fields.
PS C:\Users\User\Desktop\DomainProgram-Fix-20260910-231222\source\346Jvfch75v6f634vf765-60ecea8c7bc346c3ed310f3c416beb6549390cac>
GPT-6 Astra
GPT-6 Astra
Навык активирован: page_content
get_page_content
Содержимое страницы
Размышление



Теперь известно точное имя: casino743.cfd_le1. Но в проверенных полях списка панели его нет. Это пока не подтверждает ни занятость имени, ни ошибку su. Одинаковые 194 строки также не доказывают, что контексты пользователей действительно различались.

Следует проверить структуру самих записей XML и владельца сайта: предыдущая проверка читала только заранее выбранные поля и могла пропустить данные.

Вставьте в PowerShell блок целиком. Он выполняет только чтение списка сайтов и сертификатов; закрытые ключи и пароли не выводит.

& {
    $python = @'
import subprocess
import xml.etree.ElementTree as ET
from collections import Counter

DOMAIN = "casino743.cfd"
SAFE = {
    "name", "crtname", "domain", "domain_name", "owner",
    "username", "user", "info", "type", "active",
    "valid_after", "letsencrypt_verifying",
    "letsencrypt_txt", "letsencrypt_failed"
}

def tagname(node):
    return node.tag.rsplit("}", 1)[-1]

def read_list(function):
    print("\n=== {} ===".format(function), flush=True)
    try:
        result = subprocess.run(
            ["/usr/local/mgr5/sbin/mgrctl", "-m", "ispmgr",
             function, "out=xml", "lang=en"],
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            universal_newlines=True,
            timeout=90
        )
        root = ET.fromstring(result.stdout)
    except (OSError, subprocess.TimeoutExpired, ET.ParseError) as error:
        print("Read failed: " + type(error).__name__)
        return

    errors = list(root.iter("error"))
    if errors:
        for error in errors:
            print("Panel error: type={}, object={}".format(
                error.get("type", ""), error.get("object", "")
            ))
        return

    if result.returncode != 0:
        print("Command exit code: {}".format(result.returncode))
        return

    rows = root.findall("elem")
    print("Rows: {}".format(len(rows)))

    # Only XML field names, not their contents.
    structure = Counter(
        tagname(child)
        for row in rows
        for child in row
    )
    print("Direct field names: " + ", ".join(
        "{} ({})".format(name, count)
        for name, count in sorted(structure.items())
    ))

    matched = 0
    for row in rows:
        # Search all text and attributes, not just expected fields.
        pieces = []
        for node in row.iter():
            pieces.append(node.text or "")
            pieces.extend(node.attrib.values())

        if not any(DOMAIN in value.lower() for value in pieces):
            continue

        matched += 1
        print("--- matching row {} ---".format(matched))

        for node in row.iter():
            field = tagname(node)
            if len(node) == 0 and field in SAFE:
                value = (node.text or "").strip()
                if value and "-----BEGIN" not in value:
                    print("{}: {}".format(field, value[:500]))

        locations = set()
        for node in row.iter():
            if DOMAIN in (node.text or "").lower():
                locations.add(tagname(node) + "/text")
            for key, value in node.attrib.items():
                if DOMAIN in value.lower():
                    locations.add(tagname(node) + "/@" + key)

        print("Match locations: " + ", ".join(sorted(locations)))

    print("Matching rows: {}".format(matched))

read_list("wwwdomain")
read_list("sslcert")
'@

    $python | & ssh root@185.115.33.18 "python3 -"

    if ($LASTEXITCODE -ne 0) {
        Write-Host "SSH-проверка завершилась с ошибкой: $LASTEXITCODE"
    }
}

Пришлите этот вывод и полный текст исходной ошибки программы с exists — пароли и токены удалите, если присутствуют. Это позволит сопоставить ответ панели с реальной структурой данных, а не продолжать подбирать имена.

Файл .wc2-attempt пока не удаляйте: outcome-unknown означает, что программа записала намерение отправить запрос, но само по себе не подтверждает, что панель его приняла.

=== wwwdomain ===
Panel error: type=missed, object=function

=== sslcert ===
Rows: 194
Direct field names: active (194), cert_type (1), info (194), is_letsencrypt (193), key (194), letsencrypt (188), letsencrypt_failed (1), letsencrypt_txt (4), letsencrypt_verifying (4), name (194), owner (194), type (194), valid_after (194), webdomains (191)
Matching rows: 0
PS C:\Users\User\Desktop\DomainProgram-Fix-20260910-231222\source\346Jvfch75v6f634vf765-60ecea8c7bc346c3ed310f3c416beb6549390cac>
GPT-6 Astra
GPT-6 Astra
В текущем ответе sslcert домен casino743.cfd не найден ни в тексте, ни в атрибутах записей. Поэтому занятость casino743.cfd_le1 этим списком не подтверждается.

Ошибка type=missed, object=function — из-за моей команды: для списка сайтов здесь нужно использовать webdomain, а не wwwdomain. Это не ошибка выпуска сертификата.

Есть ещё важная деталь: по документации ISPmanager у заявки два разных имени:

crtname — имя сертификата;
name — имя CSR-запроса.
Ваш код явно задаёт только crtname, а остальные значения берёт из FormValues(form). Конфликт может относиться к CSR, а не к сертификату, но без полного текста ошибки это только версия.

Следующая проверка — сайт и его владелец
Блок только читает данные. Новую заявку не отправляет.

& {
    $python = @'
import subprocess
import xml.etree.ElementTree as ET

domain = "casino743.cfd"

try:
    p = subprocess.run(
        ["/usr/local/mgr5/sbin/mgrctl", "-m", "ispmgr",
         "webdomain", "out=xml", "lang=en"],
        stdout=subprocess.PIPE,
        stderr=subprocess.PIPE,
        universal_newlines=True,
        timeout=90
    )
    root = ET.fromstring(p.stdout)

    errors = list(root.iter("error"))
    if errors:
        for error in errors:
            print("Panel error: type={}, object={}".format(
                error.get("type", ""), error.get("object", "")
            ))
    elif p.returncode != 0:
        print("Command failed: exit={}".format(p.returncode))
    else:
        rows = root.findall("elem")

пришли полный код
GPT-6 Astra
GPT-6 Astra
Вот полный блок проверки сайта и его владельца вместо оборвавшегося. Вставьте его целиком в PowerShell. Он только читает данные — не выпускает сертификаты, не меняет настройки и не удаляет маркеры.

& {
    $ErrorActionPreference = "Stop"

    if (-not (Get-Command ssh -ErrorAction SilentlyContinue)) {
        Write-Host "SSH-клиент Windows не найден."
        return
    }

    $python = @'
import subprocess
import xml.etree.ElementTree as ET

DOMAIN = "casino743.cfd"

# Only these field values may be printed.
SAFE_FIELDS = (
    "name",
    "owner",
    "username",
    "user",
    "domain",
    "domain_name",
    "aliases",
    "ssl",
    "ssl_port",
    "ssl_cert",
    "ssl_certificate",
    "site_ssl_cert",
    "ip",
    "ipaddr",
    "active",
)

def local_name(tag):
    return tag.rsplit("}", 1)[-1]

def main():
    print("\n=== WEBSITE AND OWNER CHECK ===", flush=True)
    print("Domain: " + DOMAIN, flush=True)

    args = [
        "/usr/local/mgr5/sbin/mgrctl",
        "-m", "ispmgr",
        "webdomain",
        "out=xml",
        "lang=en",
    ]

    try:
        result = subprocess.run(
            args,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            universal_newlines=True,
            timeout=90,
        )
    except subprocess.TimeoutExpired:
        print("Error: panel request timed out.")
        return 1
    except OSError as error:
        print("Cannot run mgrctl: " + type(error).__name__)
        return 1

    try:
        root = ET.fromstring(result.stdout)
    except ET.ParseError:
        print("Error: mgrctl returned invalid or empty XML.")
        print("Command exit code: {}".format(result.returncode))
        # Do not print raw output: it may contain sensitive data.
        return 1

    errors = [
        node for node in root.iter()
        if local_name(node.tag) == "error"
    ]

    if errors:
        for error in errors:
            print("Panel error: type={}, object={}".format(
                error.get("type", ""),
                error.get("object", ""),
            ))
        return 1

    if result.returncode != 0:
        print("Command failed: exit={}".format(result.returncode))
        return 1

    rows = [
        node for node in root
        if local_name(node.tag) == "elem"
    ]

    print("XML root: " + local_name(root.tag))
    print("Direct rows: {}".format(len(rows)))

    field_names = sorted({
        local_name(child.tag)
        for row in rows
        for child in row
    })
    print("Direct field names: " + ", ".join(field_names))

    matches = []

    for row in rows:
        # Search all text and attributes, but print only safe fields.
        matched = False

        for node in row.iter():
            values = [node.text or ""]
            values.extend(node.attrib.values())

            if any(DOMAIN in value.lower() for value in values):
                matched = True
                break

        if matched:
            matches.append(row)

    print("Matching rows: {}".format(len(matches)))

    for number, row in enumerate(matches, start=1):
        print("\n--- matching website {} ---".format(number))

        for field in SAFE_FIELDS:
            for node in row.iter():
                if local_name(node.tag) != field or len(node) != 0:
                    continue

                value = (node.text or "").strip()

                if value and "-----BEGIN" not in value:
                    # Escape control characters for safe console output.
                    safe_value = (
                        value[:500]
                        .encode("unicode_escape")
                        .decode("ascii")
                    )
                    print("{}: {}".format(field, safe_value))

        locations = set()

        for node in row.iter():
            if DOMAIN in (node.text or "").lower():
                locations.add(local_name(node.tag) + "/text")

            for key, value in node.attrib.items():
                if DOMAIN in value.lower():
                    locations.add(
                        local_name(node.tag) + "/@" + key
                    )

        print("Match locations: " + ", ".join(sorted(locations)))

    if not matches:
        print(
            "Domain not found in this webdomain response. "
            "This alone does not prove the site is absent "
            "from every user context or server."
        )

    print("\n=== CHECK COMPLETE ===")
    return 0

raise SystemExit(main())
'@

    Write-Host "`n=== ПОДКЛЮЧЕНИЕ К СЕРВЕРУ ISPmanager ==="
    Write-Host "Введите пароль SSH в терминале. Сюда пароль присылать не нужно."

    $python | & ssh root@185.115.33.18 "python3 -"
    $exitCode = $LASTEXITCODE

    if ($exitCode -ne 0) {
        Write-Host "`nПроверка завершилась с ошибкой. Код: $exitCode"
    } else {
        Write-Host "`nПроверка завершена. Изменения не выполнялись."
    }
}

Пришлите вывод и полный текст исходной ошибки выпуска с exists, если он сохранился. Повторно запускать выпуск ради получения ошибки не нужно.

=== WEBSITE AND OWNER CHECK ===
Domain: casino743.cfd
XML root: doc
Direct rows: 192
Direct field names: active, aiwordpress, analyzer, database, docroot, handler, id, ipaddr, name, owner, php, php_mode, php_version, public_docroot, secure, ssl_issued_success, ssl_not_used, ssl_status_le_processing_need_txt, webscript_status
Matching rows: 1

--- matching website 1 ---
name: casino743.cfd
owner: www-root
ipaddr: 185.115.33.18
active: on
Match locations: docroot/text, name/text

=== CHECK COMPLETE ===

Проверка завершена. Изменения не выполнялись.
PS C:\Users\User\Desktop\DomainProgram-Fix-20260910-231222\source\346Jvfch75v6f634vf765-60ecea8c7bc346c3ed310f3c416beb6549390cac>
GPT-6 Astra
GPT-6 Astra
Сайт найден на проверенном сервере, владелец — www-root. Значит, указанное нами имя владельца соответствует данным панели.

Сейчас подтверждено:

сайт casino743.cfd существует и активен;
программа записала выбранное имя casino743.cfd_le1 в маркер;
в полученном списке sslcert нет записей с этим доменом.
Причина конфликта пока не установлена. Моё прежнее утверждение, что ISPmanager считает занятым именно имя сертификата, было преждевременным: ошибка может относиться к имени CSR-запроса (name), а не сертификата (crtname).

Также поле ssl_status_le_processing_need_txt перечислено среди полей всего списка сайтов. Этот вывод не показывает, есть ли оно именно у casino743.cfd, поэтому ожидание TXT пока не подтверждено.

Что нужно сейчас
Пришлите полный текст исходной ошибки программы — начиная с «отправка заявки Wildcard» и включая exists, имя объекта и остальные подробности. Можно скриншот уже имеющегося сообщения или запись из журнала. Пароли и токены скройте.

Повторно запускать выпуск ради ошибки не нужно. До уточнения не удаляйте .wc2-attempt и не меняйте _le1 на _le2: это разрешит новую попытку, но не обязательно устранит причину.

В журнале только это
13.09.2026 11:13:15 INFO casino743.cfd Запуск выбранного задания 1/1.
13.09.2026 11:13:20 ERROR casino743.cfd Сайт casino743.cfd и оба псевдонима проверены. Этап запроса сертификата не завершён: Wildcard — отправка заявки Wildcard: ISPmanager: функция letsencrypt.generate; ошибка exists; поле crtname.

13.09.2026 11:13:20 WARN Очередь завершена. Выбрано: 1; успешно: 0; ошибок: 1; не начато: 0. Статус выпуска и установки Wildcard этим итогом не подтверждается.


GPT-6 Astra
GPT-6 Astra
Теперь подтверждено: ISPmanager вернул exists именно для поля crtname — имени сертификата. Версия о конфликте имени CSR этим журналом не подтверждается.

Но остаётся несоответствие: программа выбрала casino743.cfd_le1, а в позднее полученном списке панели этого имени нет. Это ещё не основание менять имя или удалять маркер. Нужно проверить, какие параметры фактически отправляет Api.Call и как программа разбирает ответ панели.

Следующий блок только читает исходники на вашем компьютере. К серверу не подключается, заявку не отправляет и ничего не изменяет.

& {
    $ErrorActionPreference = "Stop"

    $source = Join-Path $env:USERPROFILE `
        "Desktop\DomainProgram-Fix-20260910-231222\WildcardOnly-20260913-092114-435\source"

    if (-not (Test-Path -LiteralPath $source -PathType Container)) {
        Write-Host "Папка исходников не найдена: $source"
        return
    }

    $files = @(
        Get-ChildItem -LiteralPath $source -Recurse -File -Filter "*.cs" |
            Where-Object {
                $_.FullName -notmatch '[\\/](bin|obj)[\\/]'
            }
    )

    if ($files.Count -eq 0) {
        Write-Host "Файлы C# не найдены."
        return
    }

    # Ищем объявления, а не все места вызова.
    $patterns = @(
        '^\s*(?:(?:public|private|internal|protected|sealed|static|partial|abstract)\s+)*class\s+Api\b',
        '^\s*(?:(?:public|private|internal|protected|static|async|virtual|override|sealed|new)\s+)+[^\r\n=;{}]*\bCall\s*\(',
        '^\s*(?:(?:public|private|internal|protected|static|async)\s+)+[^\r\n=;{}]*\bFormValues\s*\('
    )

    Write-Host "`n=== API: КОНСТРУКТОР, ОТПРАВКА И РАЗБОР ФОРМЫ ==="

    $hits = @(
        Select-String -LiteralPath $files.FullName `
            -Encoding UTF8 -Pattern $patterns |
            Sort-Object Path, LineNumber
    )

    if ($hits.Count -eq 0) {
        Write-Host "Объявления по шаблону не найдены."
        Write-Host "Ниже — только расположение упоминаний:"

        Select-String -LiteralPath $files.FullName -Encoding UTF8 `
            -Pattern '\bclass\s+Api\b|\bFormValues\s*\(|\bCall\s*\(' |
            Select-Object Path, LineNumber |
            Format-Table -AutoSize |
            Out-String -Width 240 |
            Write-Host

        return
    }

    foreach ($group in ($hits | Group-Object Path)) {
        $lines = @(
            Get-Content -LiteralPath $group.Name -Encoding UTF8
        )

        Write-Host "`n=== ФАЙЛ: $($group.Name) ==="

        $lastPrinted = -1

        foreach ($hit in $group.Group) {
            $start = [Math]::Max(0, $hit.LineNumber - 4)

            $count = if ($hit.Line -match '\bclass\s+Api\b') {
                180
            } else {
                110
            }

            $end = [Math]::Min(
                $lines.Count - 1,
                $hit.LineNumber - 1 + $count
            )

            # Не печатаем повторно перекрывающиеся строки.
            $start = [Math]::Max($start, $lastPrinted + 1)

            if ($start -gt $end) {
                continue
            }

            Write-Host "`n--- Строки $($start + 1)–$($end + 1) ---"

            for ($i = $start; $i -le $end; $i++) {
                "{0,5}: {1}" -f ($i + 1), $lines[$i]
            }

            $lastPrinted = $end
        }
    }

    Write-Host "`n=== ЧТЕНИЕ ЗАВЕРШЕНО. ИЗМЕНЕНИЙ НЕТ ==="
}

Пришлите вывод, убрав реальные пароли, токены или строки авторизации, если они прописаны в исходниках. Названия переменных вроде Password скрывать не нужно.

По этим методам можно будет проверить, не меняется ли crtname при отправке, совпадает ли контекст чтения и выпуска и как обрабатывается ошибка exists. Пока сам журнал подтверждает отказ операции, но не объясняет, почему конфликтующий объект отсутствует в проверенном списке.

=== API: КОНСТРУКТОР, ОТПРАВКА И РАЗБОР ФОРМЫ ===

=== ФАЙЛ: C:\Users\User\Desktop\DomainProgram-Fix-20260910-231222\WildcardOnly-20260913-092114-435\source\Services\DeploymentSchemaFix.cs ===

--- Строки 22–205 ---
22:
23: private const string PhpMode = "php_mode_fcgi_nginxfpm";
24:
25: private sealed class Api : IDisposable
26: {
27: private readonly HttpClient client;
28: private readonly IspmanagerSettings settings;
29: private readonly string session;
30: private readonly string password;
31:
32: private Api(
33: HttpClient client,
34: IspmanagerSettings settings,
35: string session,
36: string password)
37: {
38: this.client = client;
39: this.settings = settings;
40: this.session = session;
41: this.password = password;
42: }
43:
44: internal static async Task<Api> Open(
45: IspmanagerSettings settings,
46: string password,
47: CancellationToken ct)
48: {
49: if (string.IsNullOrWhiteSpace(settings.Login) ||
50: string.IsNullOrWhiteSpace(settings.Owner) ||
51: string.IsNullOrEmpty(password))
52: {
53: throw new InvalidOperationException(
54: "Заполните логин, владельца и пароль ISPmanager.");
55: }
56:
57: var client = CreateClient(settings);
58:
59: try
60: {
61: var session = await AuthenticateAsync(
62: client, settings, password, ct);
63:
64: return new Api(client, settings, session, password);
65: }
66: catch
67: {
68: client.Dispose();
69: throw;
70: }
71: }
72:
73: internal async Task<XDocument> Call(
74: string function,
75: Dictionary<string, string>? parameters,
76: CancellationToken ct)
77: {
78: var result = await CallAsync(
79: client, settings, session, function, parameters, ct);
80:
81: if (!result.Success)
82: {
83: var error = result.Document?
84: .Descendants()
85: .FirstOrDefault(x => x.Name.LocalName == "error");
86:
87: var type = error?.Attribute("type")?.Value
88: ?? result.ErrorCode;
89:
90: var field = error?.Attribute("object")?.Value ?? "";
91:
92: // Не выводим полный XML, параметры запроса или пароли.
93: var message =
94: $"ISPmanager: функция {function}; " +
95: $"ошибка {type}; поле {field}.";
96:
97: var secrets = new List<string> { password, session };
98:
99: if (parameters is not null)
100: {
101: secrets.AddRange(parameters
102: .Where(p =>
103: p.Key.Contains(
104: "pass", StringComparison.OrdinalIgnoreCase) ||
105: p.Key.Contains(
106: "confirm", StringComparison.OrdinalIgnoreCase))
107: .Select(p => p.Value));
108: }
109:
110: foreach (var secret in secrets
111: .Where(s => !string.IsNullOrEmpty(s))
112: .OrderByDescending(s => s.Length))
113: {
114: message = message.Replace(
115: secret, "[СКРЫТО]", StringComparison.Ordinal);
116: }
117:
118: throw new InvalidOperationException(message);
119: }
120:
121: var document = result.Document;
122:
123: if (document?.Root?.Name.LocalName != "doc")
124: {
125: throw new InvalidOperationException(
126: $"ISPmanager: {function} не вернула XML-документ doc.");
127: }
128:
129: var responseFunction =
130: (string?)document.Root.Attribute("func") ?? "";
131:
132: var loginForm = document.Root.Elements("metadata").Any(e =>
133: {
134: var name = (string?)e.Attribute("name") ?? "";
135: return name.Equals(
136: "logon", StringComparison.OrdinalIgnoreCase) ||
137: name.Equals(
138: "auth", StringComparison.OrdinalIgnoreCase);
139: });
140:
141: if (responseFunction.Equals(
142: "logon", StringComparison.OrdinalIgnoreCase) ||
143: responseFunction.Equals(
144: "auth", StringComparison.OrdinalIgnoreCase) ||
145: loginForm)
146: {
147: throw new InvalidOperationException(
148: "ISPmanager вернул форму входа вместо результата API. " +
149: "Проверьте авторизацию приложения.");
150: }
151:
152: return document;
153: }
154:
155: public void Dispose() => client.Dispose();
156: }
157:
158: private sealed record SiteInfo(
159: string Id,
160: string Owner,
161: XDocument Form);
162:
163: private static string Value(XDocument doc, string name) =>
164: doc.Root?.Element(name)?.Value ?? "";
165:
166: private static HashSet<string> Options(
167: XDocument doc,
168: string name)
169: {
170: return (doc.Root?.Elements("slist")
171: .Where(x => (string?)x.Attribute("name") == name)
172: .SelectMany(x => x.Elements("val"))
173: .Select(x => (string?)x.Attribute("key") ?? "")
174: .Where(x => x.Length > 0)
175: ?? Enumerable.Empty<string>())
176: .ToHashSet(StringComparer.Ordinal);
177: }
178:
179: private static IEnumerable<XElement> ControlElements(XDocument doc)
180: {
181: return (doc.Root?.Elements("metadata")
182: .SelectMany(x => x.Descendants())
183: ?? Enumerable.Empty<XElement>())
184: .Where(x => x.Name.LocalName is
185: "input" or "select" or "textarea" or "slider");
186: }
187:
188: private static HashSet<string> Controls(XDocument doc)
189: {
190: return ControlElements(doc)
191: .Select(x => (string?)x.Attribute("name") ?? "")
192: .Where(x => x.Length > 0)
193: .ToHashSet(StringComparer.Ordinal);
194: }
195:
196: private static Dictionary<string, string> FormValues(
197: XDocument doc,
198: bool includeHidden = true)
199: {
200: var fields = new Dictionary<string, string>(
201: StringComparer.Ordinal);
202:
203: var reserved = new HashSet<string>(StringComparer.OrdinalIgnoreCase)
204: {
205: "auth", "authinfo", "func", "out", "lang",

--- Строки 206–306 ---
206: "sok", "elid", "plid"
207: };
208:
209: foreach (var control in ControlElements(doc))
210: {
211: var name = (string?)control.Attribute("name") ?? "";
212: var type = (string?)control.Attribute("type") ?? "";
213:
214: if (name.Length == 0 ||
215: reserved.Contains(name) ||
216: name.Contains("pass", StringComparison.OrdinalIgnoreCase) ||
217: name.Contains("confirm", StringComparison.OrdinalIgnoreCase) ||
218: type.Equals("password", StringComparison.OrdinalIgnoreCase))
219: {
220: continue;
221: }
222:
223: if (!includeHidden &&
224: (type.Equals("hidden", StringComparison.OrdinalIgnoreCase) ||
225: (string?)control.Attribute("readonly") == "yes"))
226: {
227: continue;
228: }
229:
230: var element = doc.Root?.Element(name);
231:
232: if (element is not null)
233: fields[name] = element.Value;
234: }
235:
236: return fields;
237: }
238:
239: private static void RequireForm(XDocument doc, string function)
240: {
241: if (doc.Root?.Elements("metadata").Any(e =>
242: (string?)e.Attribute("name") == function &&
243: (string?)e.Attribute("type") == "form") != true)
244: {
245: throw new InvalidOperationException(
246: $"Панель не вернула ожидаемую форму {function}. " +
247: "Сохранение не выполнялось.");
248: }
249: }
250:
251: private static void RequireControls(
252: XDocument doc,
253: params string names)
254: {
255: var controls = Controls(doc);
256: var missing = names.Where(x => !controls.Contains(x)).ToArray();
257:
258: if (missing.Length > 0)
259: {
260: throw new InvalidOperationException(
261: "Форма панели не содержит ожидаемые поля: " +
262: string.Join(", ", missing) +
263: ". Сохранение не выполнялось.");
264: }
265: }
266:
267: private static void RequireChoice(
268: XDocument doc,
269: string field,
270: string expected)
271: {
272: var options = Options(doc, field);
273:
274: var accepted = options.Count > 0
275: ? options.Contains(expected)
276: : string.Equals(
277: Value(doc, field), expected, StringComparison.Ordinal);
278:
279: if (!accepted)
280: {
281: throw new InvalidOperationException(
282: $"Панель не подтвердила значение поля {field}: {expected}.");
283: }
284: }
285:
286: private static bool SameDomain(string left, string right) =>
287: string.Equals(
288: left.Trim().TrimEnd('.'),
289: right.Trim().TrimEnd('.'),
290: StringComparison.OrdinalIgnoreCase);
291:
292: private static string SplitValues(string value) =>
293: value.Split(
294: new { ' ', '\r', '\n', '\t', ',' },
295: StringSplitOptions.RemoveEmptyEntries);
296:
297: private static HashSet<string> Aliases(XDocument form) =>
298: SplitValues(Value(form, "aliases"))
299: .Select(x => x.TrimEnd('.'))
300: .ToHashSet(StringComparer.OrdinalIgnoreCase);
301:
302: private static string PhpVersionKey(string input)
303: {
304: var value = (input ?? "").Trim();
305:
306: if (value == "native" ||

=== ЧТЕНИЕ ЗАВЕРШЕНО. ИЗМЕНЕНИЙ НЕТ ===
PS C:\Users\User\Desktop\DomainProgram-Fix-20260910-231222\source\346Jvfch75v6f634vf765-60ecea8c7bc346c3ed310f3c416beb6549390cac>
GPT-6 Astra
GPT-6 Astra
В этом фрагменте причина конфликта ещё не видна, но два момента уже понятны:

Api.Call передаёт словарь параметров в CallAsync без изменения в показанном коде.
Сообщение exists; поле crtname сформировано из ответа панели, а не придумано программой.
FormValues копирует значения формы, включая скрытые по умолчанию. Само по себе это не ошибка: важно, какие значения затем перезаписываются перед отправкой.

Теперь нужны два конкретных участка: реализация CallAsync и подготовка вызова letsencrypt.generate. Без них правка имени сертификата была бы догадкой.

Выполните полный блок ниже. Он только читает локальные исходники, к панели не обращается.

& {
    $ErrorActionPreference = "Stop"

    $source = Join-Path $env:USERPROFILE `
        "Desktop\DomainProgram-Fix-20260910-231222\WildcardOnly-20260913-092114-435\source"

    if (-not (Test-Path -LiteralPath $source -PathType Container)) {
        Write-Host "Папка исходников не найдена: $source"
        return
    }

    $files = @(
        Get-ChildItem -LiteralPath $source -Recurse -File -Filter "*.cs" |
            Where-Object {
                $_.FullName -notmatch '[\\/](bin|obj)[\\/]'
            }
    )

    if ($files.Count -eq 0) {
        Write-Host "Файлы C# не найдены."
        return
    }

    $hits = @(
        Select-String -LiteralPath $files.FullName -Encoding UTF8 `
            -Pattern '\bCallAsync\s*\(', 'letsencrypt\.generate' |
            Sort-Object Path, LineNumber
    )

    if ($hits.Count -eq 0) {
        Write-Host "CallAsync и letsencrypt.generate не найдены."
        return
    }

    foreach ($group in ($hits | Group-Object Path)) {
        $lines = @(
            Get-Content -LiteralPath $group.Name -Encoding UTF8
        )

        $ranges = @(
            foreach ($hit in $group.Group) {
                $isCallAsync = $hit.Line -match '\bCallAsync\s*\('

                $before = if ($isCallAsync) { 15 } else { 70 }
                $after  = if ($isCallAsync) { 150 } else { 45 }

                [PSCustomObject]@{
                    Start = [Math]::Max(
                        0, $hit.LineNumber - 1 - $before
                    )
                    End = [Math]::Min(
                        $lines.Count - 1,
                        $hit.LineNumber - 1 + $after
                    )
                }
            }
        )

        Write-Host "`n=== ФАЙЛ: $($group.Name) ==="

        $lastPrinted = -1

        foreach ($range in ($ranges | Sort-Object Start, End)) {
            $start = [Math]::Max($range.Start, $lastPrinted + 1)
            $end = $range.End

            if ($start -gt $end) {
                continue
            }

            Write-Host "`n--- Строки $($start + 1)–$($end + 1) ---"

            for ($i = $start; $i -le $end; $i++) {
                "{0,5}: {1}" -f ($i + 1), $lines[$i]
            }

            $lastPrinted = $end
        }
    }

    Write-Host "`n=== ЧТЕНИЕ ЗАВЕРШЕНО. ИЗМЕНЕНИЙ НЕТ ==="
}

Пришлите вывод. Если в исходниках есть реальные значения паролей или токенов, замените их на [СКРЫТО]; названия переменных оставьте.

Маркер outcome-unknown пока сохраняем. Хотя журнал содержит явную ошибку панели, он не гарантирует отсутствия частично созданного объекта, поэтому автоматически разрешать повторную отправку сейчас не стоит.

=== ФАЙЛ: C:\Users\User\Desktop\DomainProgram-Fix-20260910-231222\WildcardOnly-20260913-092114-435\source\Services\DeploymentSchemaFix.cs ===

--- Строки 63–228 ---
63:
64: return new Api(client, settings, session, password);
65: }
66: catch
67: {
68: client.Dispose();
69: throw;
70: }
71: }
72:
73: internal async Task<XDocument> Call(
74: string function,
75: Dictionary<string, string>? parameters,
76: CancellationToken ct)
77: {
78: var result = await CallAsync(
79: client, settings, session, function, parameters, ct);
80:
81: if (!result.Success)
82: {
83: var error = result.Document?
84: .Descendants()
85: .FirstOrDefault(x => x.Name.LocalName == "error");
86:
87: var type = error?.Attribute("type")?.Value
88: ?? result.ErrorCode;
89:
90: var field = error?.Attribute("object")?.Value ?? "";
91:
92: // Не выводим полный XML, параметры запроса или пароли.
93: var message =
94: $"ISPmanager: функция {function}; " +
95: $"ошибка {type}; поле {field}.";
96:
97: var secrets = new List<string> { password, session };
98:
99: if (parameters is not null)
100: {
101: secrets.AddRange(parameters
102: .Where(p =>
103: p.Key.Contains(
104: "pass", StringComparison.OrdinalIgnoreCase) ||
105: p.Key.Contains(
106: "confirm", StringComparison.OrdinalIgnoreCase))
107: .Select(p => p.Value));
108: }
109:
110: foreach (var secret in secrets
111: .Where(s => !string.IsNullOrEmpty(s))
112: .OrderByDescending(s => s.Length))
113: {
114: message = message.Replace(
115: secret, "[СКРЫТО]", StringComparison.Ordinal);
116: }
117:
118: throw new InvalidOperationException(message);
119: }
120:
121: var document = result.Document;
122:
123: if (document?.Root?.Name.LocalName != "doc")
124: {
125: throw new InvalidOperationException(
126: $"ISPmanager: {function} не вернула XML-документ doc.");
127: }
128:
129: var responseFunction =
130: (string?)document.Root.Attribute("func") ?? "";
131:
132: var loginForm = document.Root.Elements("metadata").Any(e =>
133: {
134: var name = (string?)e.Attribute("name") ?? "";
135: return name.Equals(
136: "logon", StringComparison.OrdinalIgnoreCase) ||
137: name.Equals(
138: "auth", StringComparison.OrdinalIgnoreCase);
139: });
140:
141: if (responseFunction.Equals(
142: "logon", StringComparison.OrdinalIgnoreCase) ||
143: responseFunction.Equals(
144: "auth", StringComparison.OrdinalIgnoreCase) ||
145: loginForm)
146: {
147: throw new InvalidOperationException(
148: "ISPmanager вернул форму входа вместо результата API. " +
149: "Проверьте авторизацию приложения.");
150: }
151:
152: return document;
153: }
154:
155: public void Dispose() => client.Dispose();
156: }
157:
158: private sealed record SiteInfo(
159: string Id,
160: string Owner,
161: XDocument Form);
162:
163: private static string Value(XDocument doc, string name) =>
164: doc.Root?.Element(name)?.Value ?? "";
165:
166: private static HashSet<string> Options(
167: XDocument doc,
168: string name)
169: {
170: return (doc.Root?.Elements("slist")
171: .Where(x => (string?)x.Attribute("name") == name)
172: .SelectMany(x => x.Elements("val"))
173: .Select(x => (string?)x.Attribute("key") ?? "")
174: .Where(x => x.Length > 0)
175: ?? Enumerable.Empty<string>())
176: .ToHashSet(StringComparer.Ordinal);
177: }
178:
179: private static IEnumerable<XElement> ControlElements(XDocument doc)
180: {
181: return (doc.Root?.Elements("metadata")
182: .SelectMany(x => x.Descendants())
183: ?? Enumerable.Empty<XElement>())
184: .Where(x => x.Name.LocalName is
185: "input" or "select" or "textarea" or "slider");
186: }
187:
188: private static HashSet<string> Controls(XDocument doc)
189: {
190: return ControlElements(doc)
191: .Select(x => (string?)x.Attribute("name") ?? "")
192: .Where(x => x.Length > 0)
193: .ToHashSet(StringComparer.Ordinal);
194: }
195:
196: private static Dictionary<string, string> FormValues(
197: XDocument doc,
198: bool includeHidden = true)
199: {
200: var fields = new Dictionary<string, string>(
201: StringComparer.Ordinal);
202:
203: var reserved = new HashSet<string>(StringComparer.OrdinalIgnoreCase)
204: {
205: "auth", "authinfo", "func", "out", "lang",
206: "sok", "elid", "plid"
207: };
208:
209: foreach (var control in ControlElements(doc))
210: {
211: var name = (string?)control.Attribute("name") ?? "";
212: var type = (string?)control.Attribute("type") ?? "";
213:
214: if (name.Length == 0 ||
215: reserved.Contains(name) ||
216: name.Contains("pass", StringComparison.OrdinalIgnoreCase) ||
217: name.Contains("confirm", StringComparison.OrdinalIgnoreCase) ||
218: type.Equals("password", StringComparison.OrdinalIgnoreCase))
219: {
220: continue;
221: }
222:
223: if (!includeHidden &&
224: (type.Equals("hidden", StringComparison.OrdinalIgnoreCase) ||
225: (string?)control.Attribute("readonly") == "yes"))
226: {
227: continue;
228: }

=== ФАЙЛ: C:\Users\User\Desktop\DomainProgram-Fix-20260910-231222\WildcardOnly-20260913-092114-435\source\Services\DeploymentSchemaFix.WildcardOnly.cs ===

--- Строки 153–268 ---
153: {
154: // Новый маркер этой версии означает, что запрос уже мог
155: // быть принят. Сначала необходимо обнаружить его в панели.
156: // Не отправляем повторный POST после неизвестного исхода.
157: if (File.Exists(attemptFile))
158: throw new InvalidOperationException(
159: "Эта версия уже отправляла заявку, но сейчас " +
160: "подходящий сертификат или ожидающая заявка " +
161: "не обнаружены. Автоматический повтор остановлен. " +
162: "Проверьте результат заявки в ISPmanager. " +
163: "Маркер не удалялся.");
164:
165: var oldState = Path.Combine(directory, key + ".state");
166: if (File.Exists(oldState))
167: {
168: var state = (await File.ReadAllTextAsync(oldState, ct)).Trim();
169: if (state.Length > 0 && state != "submitted")
170: throw new InvalidOperationException(
171: "У прежней заявки неизвестный исход, а в панели " +
172: "не найдена ожидающая Wildcard-заявка. " +
173: "Новая заявка не отправлена.");
174:
175: // Старое submitted не считается выпущенным сертификатом.
176: // Отсутствие подходящего сертификата уже проверено в панели.
177: }
178:
179: var names = certificates
180: .SelectMany(row => new
181: {
182: Wc2CertKey(row), Wc2Field(row, "name")
183: })
184: .ToHashSet(StringComparer.Ordinal);
185:
186: string certificateName = "";
187: for (int number = 1; number <= 999; number++)
188: {
189: var candidate = domain + "_le" +
190: number.ToString(CultureInfo.InvariantCulture);
191:
192: if (!names.Contains(candidate) &&
193: !names.Contains(owner + "%#%" + candidate))
194: {
195: certificateName = candidate;
196: break;
197: }
198: }
199:
200: if (certificateName.Length == 0)
201: throw new InvalidOperationException(
202: "Не найдено свободное имя сертификата.");
203:
204: phase = "получение формы выпуска";
205:
206: var request = new Dictionary<string, string>
207: {
208: ["out"] = "devel",
209: ["username"] = owner,
210: ["domain_name"] = domain,
211: // www покрывается Wildcard и отдельно в заявку не нужен.
212: // Псевдонимы самого сайта не изменяются.
213: ["aliases"] = "*." + domain,
214: ["crtname"] = certificateName,
215: ["email"] = "webmaster@" + domain,
216: ["from_webdomain"] = "on",
217: ["wildcard"] = "on",
218: ["dns_check"] = "on",
219: ["enable_cert"] = "on"
220: };
221:
222: var form = await api.Call(
223: "letsencrypt.generate", request, ct);
224:
225: RequireForm(form, "letsencrypt.generate");
226: RequireControls(form, "wildcard");
227:
228: var fields = FormValues(form);
229: foreach (var pair in request)
230: fields[pair.Key] = pair.Value;
231:
232: fields["out"] = "xml";
233: fields["sok"] = "ok";
234:
235: ct.ThrowIfCancellationRequested();
236:
237: using (var stateStream = new FileStream(
238: attemptFile,
239: FileMode.CreateNew,
240: FileAccess.Write,
241: FileShare.None))
242: {
243: var bytes = Encoding.UTF8.GetBytes(
244: "outcome-unknown\n" + certificateName);
245: stateStream.Write(bytes);
246: stateStream.Flush(true);
247: }
248:
249: phase = "отправка заявки Wildcard";
250: await api.Call("letsencrypt.generate", fields, ct);
251: }
252:
253: // Все последующие действия продолжают серверную заявку.
254: // Повторный letsencrypt.generate в этом цикле не отправляется.
255: var finishBy = DateTime.UtcNow.AddMinutes(18);
256: string lastTxtSet = "";
257: bool retriedForCurrentSet = false;
258: string lastState = "ожидание появления заявки";
259:
260: while (DateTime.UtcNow < finishBy)
261: {
262: ct.ThrowIfCancellationRequested();
263:
264: phase = "проверка состояния выпуска";
265: certificates = await Wc2Certificates(api, owner, ct);
266:
267: ready = certificates
268: .Where(row => Wc2Ready(row, domain))

--- Строки 269–270 ---
269: .OrderByDescending(row => Wc2Field(row, "valid_after"))
270: .FirstOrDefault();

--- Строки 271–295 ---
271:
272: if (ready is not null)
273: {
274: phase = "назначение Wildcard и проверка HTTPS";
275: await Wc2AttachAndVerify(
276: api, ready, domain, owner, ct);
277:
278: return Wc2Success(domain);
279: }
280:
281: pending = certificates
282: .Where(row => Wc2Covers(row, domain) && Wc2Pending(row))
283: .ToList();
284:
285: if (pending.Count > 1)
286: throw new InvalidOperationException(
287: "Обнаружено несколько ожидающих Wildcard-заявок.");
288:
289: if (pending.Count == 0)
290: {
291: if (certificates.Any(row =>
292: Wc2Covers(row, domain) &&
293: Wc2Field(row, "letsencrypt_failed") == "on"))
294: throw new InvalidOperationException(
295: "ISPmanager сообщил letsencrypt_failed. " +

--- Строки 296–299 ---
296: "Нужен текст ошибки этой заявки в панели. " +
297: "Повторный выпуск автоматически не запускался.");
298:
299: lastState = "заявка пока не появилась в списке";

=== ФАЙЛ: C:\Users\User\Desktop\DomainProgram-Fix-20260910-231222\WildcardOnly-20260913-092114-435\source\Services\IspmanagerAutomationService.cs ===

--- Строки 16–181 ---
16: public async Task<IspmanagerOperationResult> TestConnectionAsync(
17: IspmanagerSettings settings,
18: string password,
19: CancellationToken cancellationToken)
20: {
21: ValidateSettings(settings, password);
22:
23: using var client = CreateClient(settings);
24:
25: var sessionId = await AuthenticateAsync(
26: client,
27: settings,
28: password,
29: cancellationToken);
30:
31: var listResult = await CallAsync(
32: client,
33: settings,
34: sessionId,
35: "webdomain",
36: null,
37: cancellationToken);
38:
39: EnsureSuccess(
40: listResult,
41: "Не удалось получить список WWW-доменов");
42:
43: return new IspmanagerOperationResult
44: {
45: Success = true,
46: Message =
47: "Подключение к ISPmanager API выполнено успешно. " +
48: "Список WWW-доменов получен."
49: };
50: }
51:
52: public Task<IspmanagerOperationResult> CreateWebDomainAsync(
53: IspmanagerSettings settings,
54: string password,
55: string domain,
56: CancellationToken cancellationToken)
57: {
58: return DeploymentSchemaFix.EnsureSiteAsync(
59: settings, password, domain, cancellationToken);
60: }
61: internal static HttpClient CreateClient(IspmanagerSettings settings)
62: {
63: if (!Uri.TryCreate(settings.PanelUrl, UriKind.Absolute, out var uri) ||
64: uri.Scheme != Uri.UriSchemeHttps ||
65: !string.IsNullOrEmpty(uri.UserInfo))
66: {
67: throw new InvalidOperationException(
68: "Для ISPmanager нужен HTTPS URL без реквизитов внутри адреса.");
69: }
70:
71: var handler = new HttpClientHandler
72: {
73: AllowAutoRedirect = false
74: };
75:
76: // Проверка TLS обязательна. IgnoreCertificateErrors не применяется.
77: var client = new HttpClient(handler)
78: {
79: Timeout = RequestTimeout
80: };
81: client.DefaultRequestHeaders.UserAgent.ParseAdd(
82: "TextFileProcessor-DeploymentFix/1.0");
83: return client;
84: }
85: internal static async Task<string> AuthenticateAsync(
86: HttpClient client,
87: IspmanagerSettings settings,
88: string password,
89: CancellationToken cancellationToken)
90: {
91: var form = new Dictionary<string, string>
92: {
93: ["out"] = "xml",
94: ["func"] = "auth",
95: ["username"] = settings.Login.Trim(),
96: ["password"] = password
97: };
98:
99: var result = await SendFormAsync(
100: client,
101: NormalizePanelUrl(settings.PanelUrl),
102: form,
103: cancellationToken);
104:
105: EnsureSuccess(
106: result,
107: "ISPmanager отклонил авторизацию");
108:
109: if (result.Document is null)
110: {
111: throw new InvalidOperationException(
112: "ISPmanager не вернул XML при авторизации.");
113: }
114:
115: var authElement = result.Document
116: .Descendants()
117: .FirstOrDefault(element =>
118: string.Equals(
119: element.Name.LocalName,
120: "auth",
121: StringComparison.OrdinalIgnoreCase));
122:
123: if (authElement is null)
124: {
125: throw new InvalidOperationException(
126: "ISPmanager не вернул номер API-сессии.");
127: }
128:
129: var sessionId = authElement.Value.Trim();
130:
131: if (string.IsNullOrWhiteSpace(sessionId))
132: {
133: sessionId =
134: authElement.Attribute("id")?.Value?.Trim() ??
135: string.Empty;
136: }
137:
138: if (string.IsNullOrWhiteSpace(sessionId))
139: {
140: throw new InvalidOperationException(
141: "ISPmanager вернул пустой номер API-сессии.");
142: }
143:
144: return sessionId;
145: }
146:
147: internal static async Task<ApiResult> CallAsync(
148: HttpClient client,
149: IspmanagerSettings settings,
150: string sessionId,
151: string function,
152: IReadOnlyDictionary<string, string>? parameters,
153: CancellationToken cancellationToken)
154: {
155: var form = new Dictionary<string, string>
156: {
157: ["out"] = "xml",
158: ["lang"] = "ru",
159: ["auth"] = sessionId,
160: ["func"] = function
161: };
162:
163: if (parameters is not null)
164: {
165: foreach (var parameter in parameters)
166: {
167: form[parameter.Key] = parameter.Value;
168: }
169: }
170:
171: return await SendFormAsync(
172: client,
173: NormalizePanelUrl(settings.PanelUrl),
174: form,
175: cancellationToken);
176: }
177:
178: private static async Task<ApiResult> SendFormAsync(
179: HttpClient client,
180: string url,
181: IReadOnlyDictionary<string, string> form,

--- Строки 182–297 ---
182: CancellationToken cancellationToken)
183: {
184: using var content =
185: new FormUrlEncodedContent(form);
186:
187: HttpResponseMessage response;
188:
189: try
190: {
191: response = await client.PostAsync(
192: url,
193: content,
194: cancellationToken);
195: }
196: catch (OperationCanceledException)
197: when (!cancellationToken.IsCancellationRequested)
198: {
199: return ApiResult.Failure(
200: "timeout",
201: "Истекло время ожидания ответа ISPmanager.");
202: }
203: catch (HttpRequestException exception)
204: {
205: return ApiResult.Failure(
206: "connection",
207: exception.Message);
208: }
209:
210: using (response)
211: {
212: var responseText =
213: await response.Content.ReadAsStringAsync(
214: cancellationToken);
215:
216: if (!response.IsSuccessStatusCode)
217: {
218: return ApiResult.Failure(
219: ((int)response.StatusCode).ToString(
220: CultureInfo.InvariantCulture),
221: $"HTTP {(int)response.StatusCode}: " +
222: response.ReasonPhrase);
223: }
224:
225: if (string.IsNullOrWhiteSpace(responseText))
226: {
227: return ApiResult.Failure(
228: "empty_response",
229: "ISPmanager вернул пустой ответ.");
230: }
231:
232: XDocument document;
233:
234: try
235: {
236: document = XDocument.Parse(responseText);
237: }
238: catch
239: {
240: return ApiResult.Failure(
241: "invalid_xml",
242: "ISPmanager вернул ответ неизвестного формата.");
243: }
244:
245: var errorElement = document
246: .Descendants()
247: .FirstOrDefault(element =>
248: string.Equals(
249: element.Name.LocalName,
250: "error",
251: StringComparison.OrdinalIgnoreCase));
252:
253: if (errorElement is null)
254: {
255: return ApiResult.SuccessResult(document);
256: }
257:
258: var errorCode =
259: errorElement.Attribute("code")?.Value ??
260: FindElementValue(errorElement, "code") ??
261: "ispmanager_error";
262:
263: var errorMessage =
264: errorElement.Attribute("msg")?.Value ??
265: FindElementValue(errorElement, "msg") ??
266: FindElementValue(errorElement, "message") ??
267: errorElement.Value;
268:
269: if (string.IsNullOrWhiteSpace(errorMessage))
270: {
271: errorMessage =
272: "ISPmanager вернул ошибку без описания.";
273: }
274:
275: return ApiResult.Failure(
276: errorCode.Trim(),
277: errorMessage.Trim(),
278: document);
279: }
280: }
281:
282: private static async Task<bool> DomainExistsAsync(
283: HttpClient client,
284: IspmanagerSettings settings,
285: string sessionId,
286: string domain,
287: CancellationToken cancellationToken)
288: {
289: var result = await CallAsync(
290: client,
291: settings,
292: sessionId,
293: "webdomain",
294: null,
295: cancellationToken);
296:
297: EnsureSuccess(

--- Строки 298–439 ---
298: result,
299: "Не удалось проверить список WWW-доменов");
300:
301: if (result.Document is null)
302: {
303: return false;
304: }
305:
306: var expectedDomain =
307: NormalizeDomain(domain);
308:
309: return result.Document
310: .Descendants()
311: .Where(element =>
312: string.Equals(
313: element.Name.LocalName,
314: "name",
315: StringComparison.OrdinalIgnoreCase))
316: .Select(element =>
317: NormalizeDomainSafe(element.Value))
318: .Any(value =>
319: string.Equals(
320: value,
321: expectedDomain,
322: StringComparison.OrdinalIgnoreCase));
323: }
324:
325: private static string? FindElementValue(
326: XElement parent,
327: string localName)
328: {
329: return parent
330: .Descendants()
331: .FirstOrDefault(element =>
332: string.Equals(
333: element.Name.LocalName,
334: localName,
335: StringComparison.OrdinalIgnoreCase))
336: ?.Value;
337: }
338:
339: internal static void EnsureSuccess(
340: ApiResult result,
341: string operation)
342: {
343: if (result.Success)
344: {
345: return;
346: }
347:
348: throw new InvalidOperationException(
349: $"{operation}. " +
350: $"Ошибка {result.ErrorCode}: " +
351: result.ErrorMessage);
352: }
353:
354: private static void ValidateSettings(
355: IspmanagerSettings settings,
356: string password)
357: {
358: if (!Uri.TryCreate(
359: settings.PanelUrl,
360: UriKind.Absolute,
361: out var panelUri) ||
362: (
363: panelUri.Scheme != Uri.UriSchemeHttps &&
364: panelUri.Scheme != Uri.UriSchemeHttp
365: ))
366: {
367: throw new InvalidOperationException(
368: "Укажите корректный URL панели ISPmanager.");
369: }
370:
371: if (string.IsNullOrWhiteSpace(settings.Login))
372: {
373: throw new InvalidOperationException(
374: "Укажите логин ISPmanager.");
375: }
376:
377: if (string.IsNullOrEmpty(password))
378: {
379: throw new InvalidOperationException(
380: "Введите или сохраните пароль ISPmanager.");
381: }
382:
383: if (string.IsNullOrWhiteSpace(settings.Owner))
384: {
385: throw new InvalidOperationException(
386: "Укажите владельца WWW-домена.");
387: }
388:
389: if (string.IsNullOrWhiteSpace(settings.PhpVersion))
390: {
391: throw new InvalidOperationException(
392: "Укажите версию PHP.");
393: }
394: }
395:
396: private static string NormalizePanelUrl(
397: string panelUrl)
398: {
399: var value = panelUrl.Trim();
400:
401: var fragmentIndex = value.IndexOf('#');
402:
403: if (fragmentIndex >= 0)
404: {
405: value = value[..fragmentIndex];
406: }
407:
408: value = value.TrimEnd('/');
409:
410: if (!value.EndsWith(
411: "/ispmgr",
412: StringComparison.OrdinalIgnoreCase))
413: {
414: value += "/ispmgr";
415: }
416:
417: return value;
418: }
419:
420: internal static string NormalizeDomain(
421: string input)
422: {
423: var value = input.Trim();
424:
425: if (string.IsNullOrWhiteSpace(value))
426: {
427: throw new InvalidOperationException(
428: "Не указан домен для создания.");
429: }
430:
431: if (!value.Contains(
432: "://",
433: StringComparison.Ordinal))
434: {
435: value = "https://" + value;
436: }
437:
438: if (!Uri.TryCreate(
439: value,

=== ЧТЕНИЕ ЗАВЕРШЕНО. ИЗМЕНЕНИЙ НЕТ ===
PS C:\Users\User\Desktop\DomainProgram-Fix-20260910-231222\source\346Jvfch75v6f634vf765-60ecea8c7bc346c3ed310f3c416beb6549390cac>
GPT-6 Astra
GPT-6 Astra
Навык активирован: web_search
get_serp
Поисковая выдача
Результаты (9)
get_serp
Поисковая выдача
Результаты (9)
get_serp
Поисковая выдача
Результаты (1)
get_serp
Поисковая выдача
Результаты (1)
Размышление





Теперь путь отправки проверен: в показанном коде crtname не подменяется, а автоматического повтора HTTP-запроса нет. После чтения формы программа снова устанавливает выбранное имя сертификата и отправляет его с sok=ok.

Но обнаружено расхождение с документированным API, которое нужно проверить перед правкой:

в документации ISPmanager список имён для сертификата передаётся через domain;
программа вместо этого явно задаёт aliases;
значения domain и name — имени CSR — могут незаметно наследоваться из FormValues(form).
Это не доказывает причину exists/crtname, но означает, что сейчас состав заявки зависит от значений, возвращённых формой. Предыдущих данных недостаточно, чтобы считать эту заявку корректной.

Проверим саму форму, а не очередной фрагмент исходников
Блок ниже запрашивает форму letsencrypt.generate без sok=ok — без команды сохранения/выпуска. Показывает только разрешённые поля и названия элементов формы; закрытые ключи, пароли и полный XML не выводит. Маркеры не изменяет.

& {
    $python = @'
import json
import subprocess
import xml.etree.ElementTree as ET

DOMAIN = "casino743.cfd"
OWNER = "www-root"
CERTNAME = DOMAIN + "_le1"

def tag(node):
    return node.tag.rsplit("}", 1)[-1]

def show(value):
    return json.dumps(value, ensure_ascii=True)

def main():
    print("=== LETSENCRYPT FORM: WITHOUT sok ===", flush=True)

    # Reproduce the application's form request.
    # No sok=ok: do not submit the form.
    args = [
        "/usr/local/mgr5/sbin/mgrctl",
        "-m", "ispmgr",
        "letsencrypt.generate",
        "out=devel",
        "lang=ru",
        "username=" + OWNER,
        "domain_name=" + DOMAIN,
        "aliases=*." + DOMAIN,
        "crtname=" + CERTNAME,
        "email=webmaster@" + DOMAIN,
        "from_webdomain=on",
        "wildcard=on",
        "dns_check=on",
        "enable_cert=on",
    ]

    try:
        result = subprocess.run(
            args,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            universal_newlines=True,
            timeout=90,
        )
    except subprocess.TimeoutExpired:
        print("Error: request timed out. No automatic retry.")
        return 1
    except OSError as error:
        print("Execution error: " + type(error).__name__)
        return 1

    try:
        root = ET.fromstring(result.stdout)
    except ET.ParseError:
        print("Error: invalid or empty XML.")
        print("Exit code: {}".format(result.returncode))
        print("Raw response omitted.")
        return 1

    errors = [node for node in root.iter() if tag(node) == "error"]
    if errors:
        for error in errors:
            print("Panel error: type={}, object={}".format(
                show(error.get("type", "")),
                show(error.get("object", "")),
            ))
        return 1

    if result.returncode != 0:
        print("Command failed: {}".format(result.returncode))
        return 1

    print("XML root: " + show(tag(root)))

    metadata = [node for node in root if tag(node) == "metadata"]
    for node in metadata:
        print("Metadata: name={}, type={}".format(
            show(node.get("name", "")),
            show(node.get("type", "")),
        ))

    controls = {}
    for meta in metadata:
        for node in meta.iter():
            if tag(node) not in ("input", "select", "textarea", "slider"):
                continue
            name = node.get("name", "")
            if name:
                controls.setdefault(name, []).append(node)

    print("\nControl names:")
    print(show(sorted(controls)))

    allowed = (
        "username", "domain_name", "domain", "aliases",
        "crtname", "name", "email", "keylen",
        "from_webdomain", "wildcard", "dns_check",
        "skip_check_a_record", "enable_cert",
    )

    print("\nSelected form values:")
    for name in allowed:
        values = [
            node.text or ""
            for node in root
            if tag(node) == name and len(node) == 0
        ]

        print("{}: control={}, value={}".format(
            name,
            "yes" if name in controls else "no",
            show(values) if values else "<absent>",
        ))

        for node in controls.get(name, []):
            attrs = {
                key: node.attrib[key]
                for key in ("type", "readonly", "required")
                if key in node.attrib
            }
            if attrs:
                print("  attributes: " + show(attrs))

    print("\n=== FORM READ COMPLETE; NO SUBMISSION SENT ===")
    return 0

raise SystemExit(main())
'@

    $python | & ssh root@185.115.33.18 "python3 -"
    $exitCode = $LASTEXITCODE

    if ($exitCode -ne 0) {
        Write-Host "`nЧтение формы завершилось с ошибкой: $exitCode"
    }
}

Пришлите вывод и уточните адрес панели и логин, указанные в программе — без пароля, токена и параметров авторизации в URL. SSH-проверка выполняется от root; пока не подтверждено, что приложение обращается к той же панели в том же пользовательском контексте.

Менять _le1 на _le2 и удалять маркер пока не нужно: сначала отделим конфликт имени от ошибки состава заявки и различий доступа.

=== LETSENCRYPT FORM: WITHOUT sok ===
XML root: "doc"
Metadata: name="letsencrypt.generate", type="form"

Control names:
["crtname", "dns_check", "domain", "domain_name", "domain_type", "email", "enable_cert", "enable_cert_email", "from_emaildomain", "from_webdomain", "hide_dns", "keylen", "name", "skip_check_a_record", "username", "web_email", "wildcard"]

Selected form values:
username: control=yes, value=["www-root"]
attributes: {"readonly": "yes"}
domain_name: control=yes, value=["casino743.cfd"]
attributes: {"readonly": "yes"}
domain: control=yes, value=["*.casino743.cfd casino743.cfd"]
attributes: {"type": "text", "required": "yes"}
aliases: control=no, value=<absent>
crtname: control=yes, value=["casino743.cfd_le1"]
attributes: {"type": "text", "readonly": "yes", "required": "yes"}
name: control=yes, value=["casino743.cfd_le1"]
attributes: {"type": "text", "readonly": "yes", "required": "yes"}
email: control=yes, value=["webmaster@casino743.cfd"]
attributes: {"type": "text", "required": "yes"}
keylen: control=yes, value=["2048"]
attributes: {"type": "text"}
from_webdomain: control=yes, value=["on"]
attributes: {"type": "hidden"}
wildcard: control=yes, value=["on"]
attributes: {"type": "checkbox"}
dns_check: control=yes, value=["on"]
attributes: {"type": "checkbox"}
skip_check_a_record: control=yes, value=["on"]
attributes: {"type": "hidden"}
enable_cert: control=yes, value=["on"]
attributes: {"type": "checkbox"}

=== FORM READ COMPLETE; NO SUBMISSION SENT ===
PS C:\Users\User\Desktop\DomainProgram-Fix-20260910-231222\source\346Jvfch75v6f634vf765-60ecea8c7bc346c3ed310f3c416beb6549390cac>
GPT-6 Astra
GPT-6 Astra
get_serp
Поисковая выдача
Результаты (3)
Форма вернула правильный состав доменов: *.casino743.cfd casino743.cfd. Поэтому уточняю предыдущий вывод: отсутствие явного параметра domain в request в данном случае не приводит к его отсутствию в заявке — FormValues(form) копирует его из формы.

Теперь подтверждено:

Поле	Значение перед сохранением по показанному коду
domain	*.casino743.cfd casino743.cfd
crtname	casino743.cfd_le1
name	casino743.cfd_le1
wildcard	on
dns_check	on
aliases здесь лишний: такого элемента управления нет. Но доказательств, что именно он вызывает exists/crtname, нет. Совпадение name и crtname тоже не основание для правки: эти значения вернула сама панель.

Следующая проверка — существующие сертификаты
Нужно выяснить, присутствует ли casino743.cfd_le1 в списке панели и под каким владельцем. Это позволит проверить, почему программа сочла имя свободным.

Блок ниже только читает список sslcert. Форму выпуска не вызывает, сертификаты не изменяет, маркеры не трогает. Адрес SSH вводится отдельно: укажите сервер именно той панели, к которой подключено приложение.

& {
    $sshTarget = Read-Host "SSH-адрес сервера панели (root@адрес)"

    if ($sshTarget -notmatch '^root@[a-zA-Z0-9][a-zA-Z0-9.:-]*$') {
        Write-Host "Ожидается адрес вида root@185.115.33.18"
        return
    }

    $python = @'
import json
import subprocess
import xml.etree.ElementTree as ET

DOMAIN = "casino743.cfd"

def local(node):
    return node.tag.rsplit("}", 1)[-1]

def main():
    try:
        result = subprocess.run(
            [
                "/usr/local/mgr5/sbin/mgrctl",
                "-m", "ispmgr", "sslcert", "out=xml"
            ],
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            universal_newlines=True,
            timeout=90
        )
    except (OSError, subprocess.TimeoutExpired) as error:
        print("Read failed: " + type(error).__name__)
        return 1

    try:
        root = ET.fromstring(result.stdout)
    except ET.ParseError:
        print("Invalid XML. Raw output omitted.")
        return 1

    errors = [n for n in root.iter() if local(n) == "error"]
    if errors:
        for error in errors:
            print(json.dumps({
                "type": error.get("type", ""),
                "object": error.get("object", "")
            }, ensure_ascii=True))
        return 1

    if result.returncode != 0 or local(root) != "doc":
        print("Unexpected command result. Raw output omitted.")
        return 1

    rows = [n for n in root if local(n) == "elem"]
    print("Rows returned: " + str(len(rows)))

    # Only field names; no arbitrary field values.
    print("Row field names:")
    print(json.dumps(
        sorted({local(child) for row in rows for child in row}),
        ensure_ascii=True
    ))

    allowed = {
        "id", "elid", "name", "crtname", "owner", "username",
        "domain", "domain_name", "common_name",
        "letsencrypt", "letsencrypt_failed",
        "valid_after", "valid_before"
    }

    matches = 0
    for row in rows:
        data = {}
        for child in row:
            key = local(child)
            if key in allowed and len(child) == 0:
                data.setdefault(key, []).append(child.text or "")

        values = [v for group in data.values() for v in group]
        if not any(DOMAIN.casefold() in v.casefold() for v in values):
            continue

        matches += 1
        print(json.dumps(data, ensure_ascii=True))

    print("Matching rows: " + str(matches))
    print("=== LIST READ COMPLETE; NO CHANGES REQUESTED ===")
    return 0

raise SystemExit(main())
'@

    $python | & ssh $sshTarget "python3 -"

    if ($LASTEXITCODE -ne 0) {
        Write-Host "Не удалось прочитать список сертификатов."
    }
}

Пришлите вывод и логин ISPmanager, используемый приложением, без пароля. Проверка через SSH от root и запрос приложения от другого пользователя могут видеть разные списки.

Пока имя не меняем и маркер не удаляем. Даже обнаруженный объект с таким именем сначала нужно проверить: это может быть сертификат или незавершённая заявка, которую следует продолжить, а не дублировать.

SSH-адрес сервера панели (root@адрес): root@185.115.33.18
root@185.115.33.18's password:
Rows returned: 194
Row field names:
["active", "cert_type", "info", "is_letsencrypt", "key", "letsencrypt", "letsencrypt_failed", "letsencrypt_txt", "letsencrypt_verifying", "name", "owner", "type", "valid_after", "webdomains"]
Matching rows: 0
=== LIST READ COMPLETE; NO CHANGES REQUESTED ===
PS C:\Users\User\Desktop\DomainProgram-Fix-20260910-231222\source\346Jvfch75v6f634vf765-60ecea8c7bc346c3ed310f3c416beb6549390cac>
GPT-6 Astra
GPT-6 Astra
Этот результат пока не подтверждает, что сертификата нет. В предыдущем фильтре я упустил поле key, которое присутствует в вашем списке. Также поиск не учитывал info и webdomains. Поэтому Matching rows: 0 означает только отсутствие домена в проверенных полях.

Теперь нужно сопоставить фактическую структуру списка с тем, как программа получает ключ и имя сертификата. Блок ниже делает обе проверки:

Читает список сертификатов повторно, ищет домен во всех текстовых полях, но выводит только ограниченные сведения.
Читает локальные реализации Wc2Certificates, Wc2CertKey и Wc2Field.
Ничего не изменяет и выпуск не запускает.

& {
    $source = Join-Path $env:USERPROFILE `
        "Desktop\DomainProgram-Fix-20260910-231222\WildcardOnly-20260913-092114-435\source"

    $sshTarget = Read-Host "SSH-адрес сервера панели (root@адрес)"
    if ($sshTarget -notmatch '^root@[a-zA-Z0-9][a-zA-Z0-9.:-]*$') {
        Write-Host "Некорректный SSH-адрес."
        return
    }

    $python = @'
import json
import subprocess
import xml.etree.ElementTree as ET

DOMAIN = "casino743.cfd"
EXPECTED = DOMAIN + "_le1"
OWNER = "www-root"

def local(node):
    return node.tag.rsplit("}", 1)[-1]

def output(data):
    print(json.dumps(data, ensure_ascii=True))

def main():
    try:
        result = subprocess.run(
            [
                "/usr/local/mgr5/sbin/mgrctl",
                "-m", "ispmgr", "sslcert", "out=xml"
            ],
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            universal_newlines=True,
            timeout=90
        )
    except (OSError, subprocess.TimeoutExpired) as error:
        print("Read failed: " + type(error).__name__)
        return 1

    try:
        root = ET.fromstring(result.stdout)
    except ET.ParseError:
        print("Invalid XML; raw response omitted.")
        return 1

    errors = [n for n in root.iter() if local(n) == "error"]
    if errors:
        for error in errors:
            output({
                "error_type": error.get("type", ""),
                "error_object": error.get("object", "")
            })
        return 1

    if result.returncode != 0 or local(root) != "doc":
        print("Unexpected command result; raw response omitted.")
        return 1

    rows = [n for n in root if local(n) == "elem"]
    print("Rows returned: " + str(len(rows)))

    safe_fields = {
        "name", "owner", "cert_type", "type", "active",
        "is_letsencrypt", "letsencrypt", "letsencrypt_failed",
        "letsencrypt_verifying", "valid_after"
    }

    matches = 0
    owner_rows = 0

    for row in rows:
        if any(
            local(n) == "owner" and (n.text or "").strip() == OWNER
            for n in row
        ):
            owner_rows += 1

        # Search all leaf text fields, but do not print their raw contents.
        matched_fields = sorted({
            local(n)
            for n in row.iter()
            if len(n) == 0
            and DOMAIN.casefold() in (n.text or "").casefold()
        })

        if not matched_fields:
            continue

        matches += 1
        summary = {"matched_fields": matched_fields}

        for child in row:
            field = local(child)
            if field not in safe_fields or len(child) != 0:
                continue
            value = child.text or ""
            if len(value) > 256 or "-----BEGIN" in value:
                value = "[OMITTED]"
            summary.setdefault(field, []).append(value)

        # Only comparisons for key: never print its raw contents.
        keys = [
            n.text or ""
            for n in row
            if local(n) == "key" and len(n) == 0
        ]
        summary["key_equals_expected"] = EXPECTED in keys
        summary["key_equals_owner_expected"] = (
            OWNER + "%#%" + EXPECTED
        ) in keys

        output(summary)

    print("Rows with owner www-root: " + str(owner_rows))
    print("Matching rows across all leaf fields: " + str(matches))
    print("=== SERVER READ COMPLETE; NO CHANGES REQUESTED ===")
    return 0

raise SystemExit(main())
'@

    $python | & ssh $sshTarget "python3 -"
    if ($LASTEXITCODE -ne 0) {
        Write-Host "Серверная проверка завершилась с ошибкой."
    }

    Write-Host "`n=== ЛОКАЛЬНЫЕ ФУНКЦИИ ЧТЕНИЯ СЕРТИФИКАТОВ ==="

    if (-not (Test-Path -LiteralPath $source -PathType Container)) {
        Write-Host "Папка не найдена: $source"
        return
    }

    $files = @(
        Get-ChildItem -LiteralPath $source -Recurse -File -Filter "*.cs" |
            Where-Object { $_.FullName -notmatch '[\\/](bin|obj)[\\/]' }
    )

    $patterns = @(
        '^\s*(?:(?:private|internal|public|static|async)\s+)+(?:[\w.<>,?%%MATH_BLOCK_0%%]+\s+)+Wc2CertKey\s*\(',
        '^\s*(?:(?:private|internal|public|static|async)\s+)+(?:[\w.<>,?\[\]]+\s+)+Wc2Field\s*\('
    )

    $hits = @(
        if ($files.Count -gt 0) {
            Select-String -LiteralPath $files.FullName `
                -Encoding UTF8 -Pattern $patterns |
                Sort-Object Path, LineNumber
        }
    )

    if ($hits.Count -eq 0) {
        Write-Host "Объявления функций не найдены этим шаблоном."
    }

    foreach ($group in ($hits | Group-Object Path)) {
        $lines = @(Get-Content -LiteralPath $group.Name -Encoding UTF8)
        Write-Host "`n=== $($group.Name) ==="
        $last = -1

        foreach ($hit in $group.Group) {
            $start = [Math]::Max($last + 1, $hit.LineNumber - 3)
            $end = [Math]::Min($lines.Count - 1, $hit.LineNumber + 65)

            for ($i = $start; $i -le $end; $i++) {
                "{0,5}: {1}" -f ($i + 1), $lines[$i]
            }

            $last = [Math]::Max($last, $end)
        }
    }

    Write-Host "`n=== ЧТЕНИЕ ЗАВЕРШЕНО. ИЗМЕНЕНИЙ НЕТ ==="
}

Пришлите вывод и логин в настройках приложения — только имя пользователя, без пароля. www-root в форме — владелец сертификата, не обязательно пользователь, под которым приложение авторизуется.

Если окажется, что программа не учитывает идентификатор из key, это объяснит возможную ошибку проверки занятости имени. Пока это проверяемая гипотеза, а не установленная причина.

SSH-адрес сервера панели (root@адрес): root@185.115.33.18
root@185.115.33.18's password:
Rows returned: 194
Rows with owner www-root: 194
Matching rows across all leaf fields: 0
=== SERVER READ COMPLETE; NO CHANGES REQUESTED ===

=== ЛОКАЛЬНЫЕ ФУНКЦИИ ЧТЕНИЯ СЕРТИФИКАТОВ ===

=== C:\Users\User\Desktop\DomainProgram-Fix-20260910-231222\WildcardOnly-20260913-092114-435\source\Services\DeploymentSchemaFix.WildcardOnly.cs ===
26: private sealed record Wc2Txt(string Name, string Content);
27:
28: private static string Wc2Field(XElement row, string name) =>
29: row.Element(name)?.Value.Trim() ?? "";
30:
31: private static string Wc2CertKey(XElement row)
32: {
33: var key = Wc2Field(row, "key");
34: return key.Length > 0 ? key : Wc2Field(row, "name");
35: }
36:
37: private static bool Wc2Covers(XElement row, string domain)
38: {
39: var names = SplitValues(Wc2Field(row, "info"))
40: .Select(x => x.TrimEnd('.'))
41: .ToHashSet(StringComparer.OrdinalIgnoreCase);
42:
43: return names.Contains(domain) &&
44: names.Contains("*." + domain);
45: }
46:
47: private static bool Wc2Pending(XElement row) =>
48: Wc2Field(row, "letsencrypt_verifying") == "on" ||
49: Wc2Field(row, "letsencrypt_txt") == "on";
50:
51: private static bool Wc2Ready(XElement row, string domain)
52: {
53: return Wc2Covers(row, domain) &&
54: Wc2Field(row, "type") == "ssl_existing" &&
55: !Wc2Pending(row) &&
56: Wc2Field(row, "letsencrypt_failed") != "on" &&
57: DateTime.TryParseExact(
58: Wc2Field(row, "valid_after"),
59: "yyyy-MM-dd",
60: CultureInfo.InvariantCulture,
61: DateTimeStyles.None,
62: out var end) &&
63: end.Date > DateTime.UtcNow.Date.AddDays(1);
64: }
65:
66: private static async Task<List<XElement>> Wc2Certificates(
67: Api api, string owner, CancellationToken ct)
68: {
69: var doc = await api.Call(
70: "sslcert",
71: new Dictionary<string, string>
72: {
73: ["out"] = "xml",
74: ["lang"] = "en",
75: ["su"] = owner
76: }, ct);
77:
78: return doc.Root!.Elements("elem").ToList();
79: }
80:
81: private static async Task<IspmanagerOperationResult> Wc2Run(
82: Api api,
83: IspmanagerSettings settings,
84: string domain,
85: string owner,
86: CancellationToken ct)
87: {
88: var phase = "проверка существующих сертификатов";
89: var directory = Path.Combine(
90: Environment.GetFolderPath(
91: Environment.SpecialFolder.LocalApplicationData),
92: "TextFileProcessor",
93: "deployment-certificate-requests");
94:
95: Directory.CreateDirectory(directory);
96:
97: var panel = new UriBuilder(settings.PanelUrl)

=== ЧТЕНИЕ ЗАВЕРШЕНО. ИЗМЕНЕНИЙ НЕТ ===
PS C:\Users\User\Desktop\DomainProgram-Fix-20260910-231222\source\346Jvfch75v6f634vf765-60ecea8c7bc346c3ed310f3c416beb6549390cac>
GPT-6 Astra
GPT-6 Astra
Проверка не подтвердила конфликт с существующим сертификатом в прочитанном списке. Среди 194 записей нет домена casino743.cfd, а Wc2CertKey() уже правильно учитывает поле key. Моя предыдущая гипотеза о пропущенном key в программе не подтвердилась.

При этом обнаружились два важных момента.

1. Чтение списка и выпуск выполняются в разных контекстах
Список программа получает с:

["su"] = owner

А форму выпуска запрашивает с:

["username"] = owner

Это не равнозначные параметры: su используется для выполнения запроса от имени пользователя, а username задаёт владельца в форме. Само по себе такое различие ещё не доказывает ошибку. Нужно посмотреть, что добавляет обёртка Api.Call, прежде чем менять контекст запросов.

2. В первоначальном фрагменте есть ошибка компиляции
Вы присылали:

.SelectMany(row => new
{
    Wc2CertKey(row), Wc2Field(row, "name")
})

Если в файле действительно написано именно так, этот код не компилируется. Здесь нужен массив:

.SelectMany(row => new[]
{
    Wc2CertKey(row),
    Wc2Field(row, "name")
})

Это отдельная ошибка, не объяснение ответа ISPmanager. Но она означает, что необходимо проверить соответствие исходников запущенной сборке.

Следующий шаг — проверить обёртку и точный текст исходника
Выполните этот блок. Он только читает локальные файлы, к серверу не обращается:

& {
    $source = Join-Path $env:USERPROFILE `
        "Desktop\DomainProgram-Fix-20260910-231222\WildcardOnly-20260913-092114-435\source"

    if (-not (Test-Path -LiteralPath $source -PathType Container)) {
        Write-Host "Папка не найдена: $source"
        return
    }

    $files = @(
        Get-ChildItem -LiteralPath $source -Recurse -File -Filter "*.cs" |
            Where-Object { $_.FullName -notmatch '[\\/](bin|obj)[\\/]' }
    )

    if ($files.Count -eq 0) {
        Write-Host "Файлы C# не найдены."
        return
    }

    $hits = @(
        Select-String -LiteralPath $files.FullName -Encoding UTF8 -Pattern @(
            '\bclass\s+Api\b',
            '\bFormValues\s*\(',
            '\bvar\s+names\s*=\s*certificates\b'
        ) | Sort-Object Path, LineNumber
    )

    foreach ($group in ($hits | Group-Object Path)) {
        $lines = @(Get-Content -LiteralPath $group.Name -Encoding UTF8)
        Write-Host "`n=== ФАЙЛ: $($group.Name) ==="

        $last = -1
        foreach ($hit in $group.Group) {
            $start = [Math]::Max($last + 1, $hit.LineNumber - 4)
            $end = [Math]::Min($lines.Count - 1, $hit.LineNumber + 100)

            if ($start -gt $end) { continue }

            for ($i = $start; $i -le $end; $i++) {
                "{0,5}: {1}" -f ($i + 1), $lines[$i]
            }

            $last = $end
        }
    }

    Write-Host "`n=== ЧТЕНИЕ ЗАВЕРШЕНО. ИЗМЕНЕНИЙ НЕТ ==="
}

Вместе с выводом пришлите полное сообщение последней ошибки приложения и логин из его настроек. Повторно запускать выпуск ради сообщения не нужно — подойдёт уже имеющийся журнал. Пароль и идентификатор сессии не присылайте.

Пока не меняем имя сертификата, не удаляем маркер и не отправляем новую заявку: имеющихся данных ещё недостаточно для безопасной правки логики выпуска.

=== ФАЙЛ: C:\Users\User\Desktop\DomainProgram-Fix-20260910-231222\WildcardOnly-20260913-092114-435\source\Services\DeploymentSchemaFix.cs ===
22:
23: private const string PhpMode = "php_mode_fcgi_nginxfpm";
24:
25: private sealed class Api : IDisposable
26: {
27: private readonly HttpClient client;
28: private readonly IspmanagerSettings settings;
29: private readonly string session;
30: private readonly string password;
31:
32: private Api(
33: HttpClient client,
34: IspmanagerSettings settings,
35: string session,
36: string password)
37: {
38: this.client = client;
39: this.settings = settings;
40: this.session = session;
41: this.password = password;
42: }
43:
44: internal static async Task<Api> Open(
45: IspmanagerSettings settings,
46: string password,
47: CancellationToken ct)
48: {
49: if (string.IsNullOrWhiteSpace(settings.Login) ||
50: string.IsNullOrWhiteSpace(settings.Owner) ||
51: string.IsNullOrEmpty(password))
52: {
53: throw new InvalidOperationException(
54: "Заполните логин, владельца и пароль ISPmanager.");
55: }
56:
57: var client = CreateClient(settings);
58:
59: try
60: {
61: var session = await AuthenticateAsync(
62: client, settings, password, ct);
63:
64: return new Api(client, settings, session, password);
65: }
66: catch
67: {
68: client.Dispose();
69: throw;
70: }
71: }
72:
73: internal async Task<XDocument> Call(
74: string function,
75: Dictionary<string, string>? parameters,
76: CancellationToken ct)
77: {
78: var result = await CallAsync(
79: client, settings, session, function, parameters, ct);
80:
81: if (!result.Success)
82: {
83: var error = result.Document?
84: .Descendants()
85: .FirstOrDefault(x => x.Name.LocalName == "error");
86:
87: var type = error?.Attribute("type")?.Value
88: ?? result.ErrorCode;
89:
90: var field = error?.Attribute("object")?.Value ?? "";
91:
92: // Не выводим полный XML, параметры запроса или пароли.
93: var message =
94: $"ISPmanager: функция {function}; " +
95: $"ошибка {type}; поле {field}.";
96:
97: var secrets = new List<string> { password, session };
98:
99: if (parameters is not null)
100: {
101: secrets.AddRange(parameters
102: .Where(p =>
103: p.Key.Contains(
104: "pass", StringComparison.OrdinalIgnoreCase) ||
105: p.Key.Contains(
106: "confirm", StringComparison.OrdinalIgnoreCase))
107: .Select(p => p.Value));
108: }
109:
110: foreach (var secret in secrets
111: .Where(s => !string.IsNullOrEmpty(s))
112: .OrderByDescending(s => s.Length))
113: {
114: message = message.Replace(
115: secret, "[СКРЫТО]", StringComparison.Ordinal);
116: }
117:
118: throw new InvalidOperationException(message);
119: }
120:
121: var document = result.Document;
122:
123: if (document?.Root?.Name.LocalName != "doc")
124: {
125: throw new InvalidOperationException(
126: $"ISPmanager: {function} не вернула XML-документ doc.");
193: .ToHashSet(StringComparer.Ordinal);
194: }
195:
196: private static Dictionary<string, string> FormValues(
197: XDocument doc,
198: bool includeHidden = true)
199: {
200: var fields = new Dictionary<string, string>(
201: StringComparer.Ordinal);
202:
203: var reserved = new HashSet<string>(StringComparer.OrdinalIgnoreCase)
204: {
205: "auth", "authinfo", "func", "out", "lang",
206: "sok", "elid", "plid"
207: };
208:
209: foreach (var control in ControlElements(doc))
210: {
211: var name = (string?)control.Attribute("name") ?? "";
212: var type = (string?)control.Attribute("type") ?? "";
213:
214: if (name.Length == 0 ||
215: reserved.Contains(name) ||
216: name.Contains("pass", StringComparison.OrdinalIgnoreCase) ||
217: name.Contains("confirm", StringComparison.OrdinalIgnoreCase) ||
218: type.Equals("password", StringComparison.OrdinalIgnoreCase))
219: {
220: continue;
221: }
222:
223: if (!includeHidden &&
224: (type.Equals("hidden", StringComparison.OrdinalIgnoreCase) ||
225: (string?)control.Attribute("readonly") == "yes"))
226: {
227: continue;
228: }
229:
230: var element = doc.Root?.Element(name);
231:
232: if (element is not null)
233: fields[name] = element.Value;
234: }
235:
236: return fields;
237: }
238:
239: private static void RequireForm(XDocument doc, string function)
240: {
241: if (doc.Root?.Elements("metadata").Any(e =>
242: (string?)e.Attribute("name") == function &&
243: (string?)e.Attribute("type") == "form") != true)
244: {
245: throw new InvalidOperationException(
246: $"Панель не вернула ожидаемую форму {function}. " +
247: "Сохранение не выполнялось.");
248: }
249: }
250:
251: private static void RequireControls(
252: XDocument doc,
253: params string names)
254: {
255: var controls = Controls(doc);
256: var missing = names.Where(x => !controls.Contains(x)).ToArray();
257:
258: if (missing.Length > 0)
259: {
260: throw new InvalidOperationException(
261: "Форма панели не содержит ожидаемые поля: " +
262: string.Join(", ", missing) +
263: ". Сохранение не выполнялось.");
264: }
265: }
266:
267: private static void RequireChoice(
268: XDocument doc,
269: string field,
270: string expected)
271: {
272: var options = Options(doc, field);
273:
274: var accepted = options.Count > 0
275: ? options.Contains(expected)
276: : string.Equals(
277: Value(doc, field), expected, StringComparison.Ordinal);
278:
279: if (!accepted)
280: {
281: throw new InvalidOperationException(
282: $"Панель не подтвердила значение поля {field}: {expected}.");
283: }
284: }
285:
286: private static bool SameDomain(string left, string right) =>
287: string.Equals(
288: left.Trim().TrimEnd('.'),
289: right.Trim().TrimEnd('.'),
290: StringComparison.OrdinalIgnoreCase);
291:
292: private static string SplitValues(string value) =>
293: value.Split(
294: new { ' ', '\r', '\n', '\t', ',' },
295: StringSplitOptions.RemoveEmptyEntries);
296:
297: private static HashSet<string> Aliases(XDocument form) =>
512: {
513: // Сохраняем возвращённые значения полей формы.
514: // Служебные hidden-поля и пароли не копируем.
515: var fields = FormValues(form, includeHidden: false);
516:
517: fields["out"] = "xml";
518: fields["sok"] = "ok";
519: fields["tconvert"] = "punycode";
520: fields["aliases"] = string.Join(" ", aliases);
521:
522: if (existing is not null)
523: {
524: fields["elid"] = existing.Id;
525: fields["name"] = Value(form, "name");
526:
527: // Владельца и каталог существующего сайта не меняем.
528: fields.Remove("owner");
529: fields.Remove("home");
530: fields.Remove("script_selector");
531: fields.Remove("emailcreate");
532:
533: // Эти hidden-поля относятся к текущей конфигурации,
534: // а не к авторизации или ограничениям пользователя.
535: foreach (var name in new { "currname", "ssl_port" })
536: {
537: if (form.Root?.Element(name) is not null)
538: fields[name] = Value(form, name);
539: }
540: }
541: else
542: {
543: RequireControls(
544: form,
545: "home", "ipaddrs", "ipsrc", "email",
546: "php", "php_mode", "php_fpm_version",
547: "script_selector", "secure");
548:
549: RequireChoice(form, "php_mode", PhpMode);
550: RequireChoice(
551: form, "php_fpm_version", requestedPhp!);
552: RequireChoice(form, "ipsrc", "manual");
553: RequireChoice(form, "script_selector", "*");
554:
555: fields["name"] = domain;
556: fields["owner"] = owner;
557: fields["home"] = "www/" + domain;
558: fields["public_docroot"] = "";
559: fields["ipsrc"] = "manual";
560: fields["ipaddrs"] = SelectIpAddresses(form);
561: fields["email"] = "webmaster@" + domain;
562: fields["emailcreate"] = "off";
563: fields["script_selector"] = "*";
564:
565: fields["php"] = "on";
566: fields["php_mode"] = PhpMode;
567: fields["php_fpm_version"] = requestedPhp!;
568: fields["dirindex"] = "index.php index.html";
569:
570: // Сертификат запрашивается отдельно после создания.
571: // Не запускаем второй выпуск через форму сайта.
572: fields["secure"] = "off";
573: fields["redirect_http"] = "off";
574: fields["hsts"] = "off";
575: fields.Remove("ssl_cert");
576:
577: if (Controls(form).Contains("nginx_proxy"))
578: {
579: RequireChoice(
580: form, "nginx_proxy", "proxy_disable");
581: fields["nginx_proxy"] = "proxy_disable";
582: }
583:
584: // Не назначаем новый сайт приоритетным или техническим.
585: if (Controls(form).Contains("foreground"))
586: fields["foreground"] = "off";
587:
588: if (Controls(form).Contains("techdomain"))
589: fields["techdomain"] = "off";
590:
591: if (Controls(form).Contains("nodejs"))
592: fields["nodejs"] = "off";
593:
594: if (Controls(form).Contains("python"))
595: fields["python"] = "off";
596: }
597:
598: // Единственный запрос сохранения.
599: // При таймауте автоматического повторного POST нет.
600: // PreserveExistingSslCertificate
601: if (existing is not null)
602: {
603: var currentSecure = Value(form, "secure").Trim();
604:
605: if (currentSecure == "on")
606: {
607: var currentCertificate =
608: Value(form, "ssl_cert").Trim();
609:
610: if (string.IsNullOrWhiteSpace(currentCertificate))
611: {
612: throw new InvalidOperationException(
613: "Перед сохранением webdomain.edit: " +
614: "у сайта включён HTTPS, но форма панели " +
615: "не вернула идентификатор ssl_cert. " +
616: "Сохранение сайта остановлено. " +

=== ФАЙЛ: C:\Users\User\Desktop\DomainProgram-Fix-20260910-231222\WildcardOnly-20260913-092114-435\source\Services\DeploymentSchemaFix.WildcardOnly.cs ===
176: // Отсутствие подходящего сертификата уже проверено в панели.
177: }
178:
179: var names = certificates
180: .SelectMany(row => new
181: {
182: Wc2CertKey(row), Wc2Field(row, "name")
183: })
184: .ToHashSet(StringComparer.Ordinal);
185:
186: string certificateName = "";
187: for (int number = 1; number <= 999; number++)
188: {
189: var candidate = domain + "_le" +
190: number.ToString(CultureInfo.InvariantCulture);
191:
192: if (!names.Contains(candidate) &&
193: !names.Contains(owner + "%#%" + candidate))
194: {
195: certificateName = candidate;
196: break;
197: }
198: }
199:
200: if (certificateName.Length == 0)
201: throw new InvalidOperationException(
202: "Не найдено свободное имя сертификата.");
203:
204: phase = "получение формы выпуска";
205:
206: var request = new Dictionary<string, string>
207: {
208: ["out"] = "devel",
209: ["username"] = owner,
210: ["domain_name"] = domain,
211: // www покрывается Wildcard и отдельно в заявку не нужен.
212: // Псевдонимы самого сайта не изменяются.
213: ["aliases"] = "*." + domain,
214: ["crtname"] = certificateName,
215: ["email"] = "webmaster@" + domain,
216: ["from_webdomain"] = "on",
217: ["wildcard"] = "on",
218: ["dns_check"] = "on",
219: ["enable_cert"] = "on"
220: };
221:
222: var form = await api.Call(
223: "letsencrypt.generate", request, ct);
224:
225: RequireForm(form, "letsencrypt.generate");
226: RequireControls(form, "wildcard");
227:
228: var fields = FormValues(form);
229: foreach (var pair in request)
230: fields[pair.Key] = pair.Value;
231:
232: fields["out"] = "xml";
233: fields["sok"] = "ok";
234:
235: ct.ThrowIfCancellationRequested();
236:
237: using (var stateStream = new FileStream(
238: attemptFile,
239: FileMode.CreateNew,
240: FileAccess.Write,
241: FileShare.None))
242: {
243: var bytes = Encoding.UTF8.GetBytes(
244: "outcome-unknown\n" + certificateName);
245: stateStream.Write(bytes);
246: stateStream.Flush(true);
247: }
248:
249: phase = "отправка заявки Wildcard";
250: await api.Call("letsencrypt.generate", fields, ct);
251: }
252:
253: // Все последующие действия продолжают серверную заявку.
254: // Повторный letsencrypt.generate в этом цикле не отправляется.
255: var finishBy = DateTime.UtcNow.AddMinutes(18);
256: string lastTxtSet = "";
257: bool retriedForCurrentSet = false;
258: string lastState = "ожидание появления заявки";
259:
260: while (DateTime.UtcNow < finishBy)
261: {
262: ct.ThrowIfCancellationRequested();
263:
264: phase = "проверка состояния выпуска";
265: certificates = await Wc2Certificates(api, owner, ct);
266:
267: ready = certificates
268: .Where(row => Wc2Ready(row, domain))
269: .OrderByDescending(row => Wc2Field(row, "valid_after"))
270: .FirstOrDefault();
271:
272: if (ready is not null)
273: {
274: phase = "назначение Wildcard и проверка HTTPS";
275: await Wc2AttachAndVerify(
276: api, ready, domain, owner, ct);
277:
278: return Wc2Success(domain);
279: }
280:
281: pending = certificates
282: .Where(row => Wc2Covers(row, domain) && Wc2Pending(row))
283: .ToList();
284:
285: if (pending.Count > 1)
286: throw new InvalidOperationException(
287: "Обнаружено несколько ожидающих Wildcard-заявок.");
288:
289: if (pending.Count == 0)
290: {
291: if (certificates.Any(row =>
292: Wc2Covers(row, domain) &&
293: Wc2Field(row, "letsencrypt_failed") == "on"))
294: throw new InvalidOperationException(
295: "ISPmanager сообщил letsencrypt_failed. " +
296: "Нужен текст ошибки этой заявки в панели. " +
297: "Повторный выпуск автоматически не запускался.");
298:
299: lastState = "заявка пока не появилась в списке";
300: await Task.Delay(TimeSpan.FromSeconds(15), ct);
301: continue;
302: }
303:
304: var current = pending[0];
305:
306: if (Wc2Field(current, "letsencrypt_failed") == "on")
307: throw new InvalidOperationException(
308: "ISPmanager сообщил ошибку текущей заявки.");
309:
310: if (Wc2Field(current, "letsencrypt_txt") == "on")
311: {
312: phase = "получение актуальных ACME TXT";
313:
314: var form = await api.Call(
315: "webdomain.letsencrypt.txt",
316: new Dictionary<string, string>
317: {
318: ["out"] = "devel",
319: ["lang"] = "en",
320: ["elid"] = domain
321: }, ct);
322:
323: var returnedId = Value(form, "elid").Trim();
324: var certKey = Wc2CertKey(current);
325: var certName = Wc2Field(current, "name");
326:
327: var allowedIds = new HashSet<string>(
328: StringComparer.Ordinal)
329: {
567: if (Value(form, "secure") != "on" || current != selected)
568: {
569: // Остальные настройки берём из исходной формы сайта.
570: var fields = FormValues(form, includeHidden: false);
571: fields.Remove("owner");
572: fields.Remove("home");
573: fields.Remove("script_selector");
574: fields.Remove("emailcreate");
575:
576: foreach (var name in new { "currname", "ssl_port" })
577: {
578: if (form.Root?.Element(name) is not null)
579: fields[name] = Value(form, name);
580: else if (schema.Root?.Element(name) is not null)
581: fields[name] = Value(schema, name);
582: }
583:
584: fields["out"] = "xml";
585: fields["sok"] = "ok";
586: fields["elid"] = site.Id;
587: fields["name"] = Value(form, "name");
588: fields["tconvert"] = "punycode";
589: fields["secure"] = "on";
590: fields["ssl_cert"] = selected;
591:
592: // Один запрос сохранения, без повтора при сетевой ошибке.
593: await api.Call("webdomain.edit", fields, ct);
594: }
595:
596: var saved = await FindSite(api, domain, ct) ??
597: throw new InvalidOperationException(
598: "После назначения сертификата сайт не найден.");
599:
600: VerifySite(saved, domain, owner);
601:
602: if (Value(saved.Form, "secure") != "on" ||
603: Value(saved.Form, "ssl_cert").Trim() != selected)
604: throw new InvalidOperationException(
605: "Панель не подтвердила назначение выбранного Wildcard.");
606:
607: // Контроль: параметры сайта, не относящиеся к SSL, не изменились.
608: foreach (var name in new
609: {
610: "home", "php", "php_mode", "php_fpm_version",
611: "dirindex", "nginx_proxy"
612: })
613: {
614: if (form.Root?.Element(name) is not null &&
615: Value(form, name) != Value(saved.Form, name))
616: throw new InvalidOperationException(
617: "После назначения SSL изменилось поле " + name +
618: ". Дальнейшие действия с этим сайтом остановлены.");
619: }
620:
621: await Wc2VerifyTls(saved.Form, domain, ct);
622: }
623:
624: private static async Task Wc2VerifyTls(
625: XDocument site, string domain, CancellationToken ct)
626: {
627: var addresses = SplitValues(Value(site, "ipaddrs"))
628: .Where(s => IPAddress.TryParse(s, out _))
629: .Select(IPAddress.Parse)
630: .OrderBy(a => a.AddressFamily == AddressFamily.InterNetwork ? 0 : 1)
631: .ToList();
632:
633: if (addresses.Count == 0)
634: throw new InvalidOperationException(
635: "Wildcard назначен в панели, но IP сайта не получен. " +
636: "TLS-проверка не выполнена.");
637:
638: int port = 443;
639: var portText = Value(site, "ssl_port").Trim();
640:
641: if (portText.Length > 0 &&
642: (!int.TryParse(portText, out port) || port < 1 || port > 65535))
643: throw new InvalidOperationException(
644: "Wildcard назначен, но панель вернула некорректный SSL-порт.");
645:
646: // Проверяем первый IPv4, либо первый IPv6, если IPv4 нет.
647: var address = addresses[0];
648:
649: for (int attempt = 0; attempt < 10; attempt++)
650: {
651: ct.ThrowIfCancellationRequested();
652:
653: using var probe =
654: CancellationTokenSource.CreateLinkedTokenSource(ct);
655: probe.CancelAfter(TimeSpan.FromSeconds(15));
656:
657: try
658: {
659: using var tcp = new TcpClient(address.AddressFamily);
660: await tcp.ConnectAsync(address, port, probe.Token);
661:
662: // Стандартная проверка доверия и имени сертификата.
663: // Callback, отключающего проверку TLS, нет.
664: using var tls = new SslStream(tcp.GetStream(), false);
665: await tls.AuthenticateAsClientAsync(
666: new SslClientAuthenticationOptions
667: {
668: TargetHost = domain,
669: EnabledSslProtocols = SslProtocols.None
670: }, probe.Token);
671:

=== ЧТЕНИЕ ЗАВЕРШЕНО. ИЗМЕНЕНИЙ НЕТ ===
PS C:\Users\User\Desktop\DomainProgram-Fix-20260910-231222\source\346Jvfch75v6f634vf765-60ecea8c7bc346c3ed310f3c416beb6549390cac>
GPT-6 Astra
GPT-6 Astra
Навык активирован: page_content
В выводе видна не одна, а несколько ошибок синтаксиса C#. Если файл действительно содержит этот текст, он не может успешно компилироваться. Поэтому сначала нужно проверить сборку: ответ exists/crtname мог прийти от ранее собранной версии приложения, а не от этих исходников.

Есть и другая возможность: символы [] потерялись при передаче текста. Автоматически переписывать файлы до проверки компилятором не стоит.

Что именно выглядит неправильно
В DeploymentSchemaFix.cs функция SplitValues должна возвращать массив строк, а разделители должны быть массивом символов:

private static string[] SplitValues(string value) =>
    value.Split(
        new[] { ' ', '\r', '\n', '\t', ',' },
        StringSplitOptions.RemoveEmptyEntries);

В DeploymentSchemaFix.WildcardOnly.cs список имён должен формироваться так:

var names = certificates
    .SelectMany(row => new[]
    {
        Wc2CertKey(row),
        Wc2Field(row, "name")
    })
    .ToHashSet(StringComparer.Ordinal);

В обоих файлах показанные циклы по строковым значениям тоже требуют массива:

foreach (var name in new[] { "currname", "ssl_port" })

И аналогично для проверки настроек сайта:

foreach (var name in new[]
{
    "home", "php", "php_mode", "php_fpm_version",
    "dirindex", "nginx_proxy"
})

Это исправления компиляции, не установленное исправление ошибки ISPmanager.

Проверим исходники компилятором, без запуска приложения
Следующий блок находит проект и выполняет сборку без восстановления пакетов. Исходники не меняет, приложение не запускает, запрос выпуска не отправляет. Сборка может создать или обновить локальные файлы в bin и obj.

& {
    $source = Join-Path $env:USERPROFILE `
        "Desktop\DomainProgram-Fix-20260910-231222\WildcardOnly-20260913-092114-435\source"

    if (-not (Test-Path -LiteralPath $source -PathType Container)) {
        Write-Host "Папка не найдена: $source"
        return
    }

    if (-not (Get-Command dotnet -ErrorAction SilentlyContinue)) {
        Write-Host "Команда dotnet не найдена. Для проверки нужен .NET SDK."
        return
    }

    $projects = @(
        Get-ChildItem -LiteralPath $source -Recurse -File -Filter "*.csproj" |
            Where-Object { $_.FullName -notmatch '[\\/](bin|obj)[\\/]' }
    )

    if ($projects.Count -ne 1) {
        Write-Host "Найдено проектов: $($projects.Count). Автоматический выбор не выполнен."
        $projects | ForEach-Object { $_.FullName }
        return
    }

    Write-Host "=== ПРОВЕРКА СБОРКИ ==="
    Write-Host "Проект: $($projects[0].FullName)"

    & dotnet build $projects[0].FullName `
        --configuration Release `
        --no-restore `
        --verbosity minimal `
        --nologo

    $code = $LASTEXITCODE
    Write-Host "`nКод завершения сборки: $code"
    Write-Host "=== ПРИЛОЖЕНИЕ НЕ ЗАПУСКАЛОСЬ ==="
}

Пришлите результат. Если появится NETSDK1004 об отсутствии project.assets.json, это означает необходимость восстановления зависимостей, а не подтверждение синтаксических ошибок.

По серверной части вывод пока прежний: домен не найден в полученном списке сертификатов, Wc2CertKey() учитывает key, а причина exists/crtname не установлена. Показанная обёртка передаёт запрос дальше в CallAsync; её недостаточно, чтобы окончательно судить о параметрах HTTP-запроса. Маркер и имя сертификата сейчас менять незачем.

=== ПРОВЕРКА СБОРКИ ===
Проект: C:\Users\User\Desktop\DomainProgram-Fix-20260910-231222\WildcardOnly-20260913-092114-435\source\TextFileProcessor.csproj
TextFileProcessor -> C:\Users\User\Desktop\DomainProgram-Fix-20260910-231222\WildcardOnly-20260913-092114-435\source\
bin\Release\net8.0-windows\win-x64\TextFileProcessor.dll

Сборка успешно завершена.
Предупреждений: 0
Ошибок: 0

Прошло времени 00:00:00.62

Код завершения сборки: 0
=== ПРИЛОЖЕНИЕ НЕ ЗАПУСКАЛОСЬ ===
PS C:\Users\User\Desktop\DomainProgram-Fix-20260910-231222\source\346Jvfch75v6f634vf765-60ecea8c7bc346c3ed310f3c416beb6549390cac>
GPT-6 Astra
GPT-6 Astra
Сборка прошла успешно: 0 ошибок и 0 предупреждений. Мой предыдущий вывод о некомпилируемых исходниках был слишком категоричным — по отображению кода в переписке этого утверждать нельзя. Предложенные замены new[] и string[] сейчас не применяем.

Обычная сборка может быть инкрементальной, но углубляться в это без необходимости не будем: ошибка exists/crtname пока не связана с компиляцией.

Что уже установлено
В прочитанном через SSH списке из 194 сертификатов домен casino743.cfd не найден.
Wc2CertKey() уже учитывает поле key.
Прочитанная через SSH форма содержит оба имени: *.casino743.cfd casino743.cfd.
Api.Call() передаёт параметры в CallAsync(), реализацию которого мы ещё не видели.
Следующий шаг — проверить фактическое формирование HTTP-запроса приложения, а не снова читать тот же серверный список. Нужно выяснить, как CallAsync() передаёт авторизацию, su и поля формы и предусмотрены ли в нём повторы запросов.

Пришлите:

Метод CallAsync() целиком. В редакторе можно перейти к его определению из строки:
var result = await CallAsync(
    client, settings, session, function, parameters, ct);

Если он вызывает отдельный метод отправки HTTP-запроса, нужен и тот метод.
Логин, владельца и адрес панели из настроек приложения. Без пароля, токена и параметров авторизации в URL. Это позволит сопоставить приложение с проверенной через SSH панелью.
Последнее уже имеющееся сообщение ошибки целиком, включая этап операции. Повторно запускать выпуск для этого не нужно.
Если в коде есть встроенные секреты, замените их на [СКРЫТО] перед отправкой.

Сертификаты, имя _le1 и файл-маркер пока оставляем без изменений. Причина отказа ещё не установлена; успешную сборку не следует принимать за исправление серверной ошибки.

PS C:\Users\User\Desktop\DomainProgram-Fix-20260910-231222\source\346Jvfch75v6f634vf765-60ecea8c7bc346c3ed310f3c416beb6549390cac> var result = await CallAsync(
>> client, settings, session, function, parameters, ct);
>>
ParserError:
Line |
1 | var result = await CallAsync(
| ~~~
| The 'var' keyword is not supported in this version of the language.
PS C:\Users\User\Desktop\DomainProgram-Fix-20260910-231222\source\346Jvfch75v6f634vf765-60ecea8c7bc346c3ed310f3c416beb6549390cac>
ljkuj tot ,eltv ljltksdfnm&
пришли готовый правильный код изменения исправления программы именно ее 1 этап добавления домена в панель управления сайта и создание\выпуска Wild сертификата
GPT-6 Astra
GPT-6 Astra
Вы вставили строку C# в PowerShell. Это моя ошибка подачи: я должен был дать готовую команду, а не фрагмент для поиска. Эта ошибка не изменила программу или сервер.

Понимаю: вам нужно исправление первого этапа — добавление домена в ISPmanager и выпуск Wildcard, а не очередная серия проверок.

Но по отдельным фрагментам я не могу честно выдать готовую замену: не видны реализация HTTP-запросов, весь процесс создания сайта и работа с маркером заявки. Подстановка выдуманных методов или простая смена _le1 на _le2 может привести к повторному выпуску и не устранит причину ошибки.

Один шаг — передайте исходники, а не новые диагностические выводы
Вставьте в PowerShell весь блок ниже. Он соберёт архив только из файлов C# и проектов из проверенной папки. Исходники не изменяет, приложение не запускает, к панели не подключается. Настройки JSON, сертификаты, журналы и содержимое bin/obj в архив не попадут.

& {
    $ErrorActionPreference = "Stop"

    $source = Join-Path $env:USERPROFILE `
        "Desktop\DomainProgram-Fix-20260910-231222\WildcardOnly-20260913-092114-435\source"

    if (-not (Test-Path -LiteralPath $source -PathType Container)) {
        throw "Не найдена папка исходников: $source"
    }

    $files = @(
        Get-ChildItem -LiteralPath $source -Recurse -File |
            Where-Object {
                $_.Extension -in ".cs", ".csproj" -and
                $_.FullName -notmatch '[\\/](bin|obj|\.git|\.vs)[\\/]'
            }
    )

    if ($files.Count -eq 0) {
        throw "Файлы исходников не найдены."
    }

    $id = [Guid]::NewGuid().ToString("N")
    $stage = Join-Path $env:TEMP "DomainProgram-source-$id"
    $desktop = [Environment]::GetFolderPath("Desktop")
    $archive = Join-Path $desktop "DomainProgram-source-$id.zip"

    New-Item -ItemType Directory -Path $stage | Out-Null

    foreach ($file in $files) {
        $relative = $file.FullName.Substring($source.Length).
            TrimStart([char[]]"\/")

        $destination = Join-Path $stage $relative
        $parent = Split-Path -Parent $destination

        New-Item -ItemType Directory -Path $parent -Force | Out-Null
        Copy-Item -LiteralPath $file.FullName -Destination $destination
    }

    Compress-Archive -Path (Join-Path $stage "*") `
        -DestinationPath $archive

    Write-Host "`nАрхив создан:"
    Write-Host $archive
    Write-Host "`nФайлов исходников: $($files.Count)"
    Write-Host "Программа и сервер не изменены."
}

Прикрепите созданный ZIP к сообщению. Если пароли или токены прописаны непосредственно в .cs или .csproj, удалите их из копии для передачи — эта команда автоматически секреты не распознаёт.

По этим файлам можно подготовить конкретную замену для вашей программы и команду её установки с резервной копией, сохранив остальные этапы. В исправлении нужно проверить создание сайта без изменения существующих настроек, заявку на *.домен вместе с основным доменом, DNS-подтверждение, защиту от повторной отправки и назначение готового сертификата.

Успешная сборка уже проверена. Повторять её сейчас, удалять маркер или заново запускать выпуск не требуется.

пришли код открытие папки и архова, где он
GPT-6 Astra
GPT-6 Astra
Вставьте весь блок в PowerShell. Он найдёт последний созданный архив, покажет его в папке и откроет:

& {
    $desktop = [Environment]::GetFolderPath("Desktop")

    $archive = Get-ChildItem -LiteralPath $desktop `
        -Filter "DomainProgram-source-*.zip" -File |
        Sort-Object LastWriteTime -Descending |
        Select-Object -First 1

    if ($null -eq $archive) {
        Write-Host "Архив не найден. Сначала выполните предыдущий блок создания архива."
        Start-Process explorer.exe -ArgumentList "`"$desktop`""
        return
    }

    Write-Host "Архив находится здесь:"
    Write-Host $archive.FullName

    Start-Process explorer.exe `
        -ArgumentList "/select,`"$($archive.FullName)`""

    Invoke-Item -LiteralPath $archive.FullName
}

Затем перетащите ZIP-файл из папки в этот чат или прикрепите через кнопку вложения.

вот все сдесь: https://github.com/raliya/346Jvfch75v6f634vf765/tree/main/DomainProgram-source-ce452fbfb36e44ebb18e1b983e37dadf
жду решение код один правильный!
GPT-6 Astra
GPT-6 Astra
get_page_content
Содержимое страницы
get_page_content
Содержимое страницы
get_page_content
Содержимое страницы
get_page_content
Содержимое страницы
get_page_content
Содержимое страницы
Навык активирован: web_search
get_serp
Поисковая выдача
Результаты (9)
get_page_content
Содержимое страницы
get_page_content
Содержимое страницы
get_page_content
Содержимое страницы
Размышление


Прочитал исходники по вашей ссылке, включая CallAsync() и весь модуль Wildcard. Ошибок с new[] в файлах нет — моя прежняя версия об ошибках синтаксиса была неверной.

В запросе выпуска есть конкретная проблема: программа не задаёт имя CSR отдельно, а копирует его из формы вместе с остальными полями. При этом aliases используется вместо явного списка доменов сертификата. По документации ISPmanager:

name — имя CSR;
crtname — имя сертификата;
domain — имена, защищаемые сертификатом.
Ниже один PowerShell-блок для исправления этого участка вашей программы. Он:

создаёт отдельную копию проекта, сохраняя оригинал;
явно задаёт domain и разные имена CSR/сертификата;
убирает копирование произвольных полей формы в заявку;
сохраняет существующую логику создания сайта, DNS TXT, ожидания выпуска и проверки HTTPS;
собирает новую программу и открывает папку с ней.
Важно: я не запускал исправление на вашей панели, поэтому не буду обещать, что причина exists/crtname окончательно устранена. Старые маркеры неизвестного результата не удаляются: если такой маркер уже блокирует домен, программа сохранит блокировку, а не отправит потенциальный дубликат заявки.

Закройте программу и вставьте весь блок в PowerShell
& {
    $ErrorActionPreference = "Stop"

    # Полный локальный проект, который у вас успешно собирался.
    # В GitHub-архиве нет XAML и manifest, поэтому используем его,
    # а не пытаемся собирать неполную выгрузку.
    $source = Join-Path $env:USERPROFILE `
        "Desktop\DomainProgram-Fix-20260910-231222\WildcardOnly-20260913-092114-435\source"

    $desktop = [Environment]::GetFolderPath("Desktop")
    $stamp = Get-Date -Format "yyyyMMdd-HHmmss"
    $suffix = [Guid]::NewGuid().ToString("N").Substring(0, 6)

    $work = Join-Path $desktop "Stage1-WildcardFix-$stamp-$suffix"
    $copy = Join-Path $work "source"
    $output = Join-Path $work "program"

    try {
        if (-not (Get-Command dotnet -ErrorAction SilentlyContinue)) {
            throw "Не найден .NET SDK: команда dotnet недоступна."
        }

        if (-not (Get-Command robocopy.exe -ErrorAction SilentlyContinue)) {
            throw "Не найдена системная утилита robocopy.exe."
        }

        if (Get-Process -Name TextFileProcessor -ErrorAction SilentlyContinue) {
            throw "Сначала закройте все окна TextFileProcessor и повторите этот блок."
        }

        $required = @(
            "TextFileProcessor.csproj",
            "App.xaml",
            "MainWindow.xaml",
            "app.manifest",
            "Services\DeploymentSchemaFix.cs",
            "Services\DeploymentSchemaFix.WildcardOnly.cs",
            "Services\IspmanagerAutomationService.cs"
        )

        foreach ($relative in $required) {
            $path = Join-Path $source $relative

            if (-not (Test-Path -LiteralPath $path -PathType Leaf)) {
                throw "Не найден необходимый файл: $path"
            }
        }

        Write-Host "`n=== СОЗДАНИЕ ОТДЕЛЬНОЙ КОПИИ ==="
        Write-Host "Оригинал: $source"
        Write-Host "Копия:    $copy"

        New-Item -ItemType Directory -Path $copy -Force | Out-Null

        & robocopy.exe $source $copy /E /XJ /R:1 /W:1 `
            /XD bin obj .git .vs `
            /XF *.user *.suo `
            /NFL /NDL /NJH /NJS /NP | Out-Null

        $copyCode = $LASTEXITCODE

        if ($copyCode -ge 8) {
            throw "Ошибка копирования проекта. Код robocopy: $copyCode"
        }

        $target = Join-Path $copy `
            "Services\DeploymentSchemaFix.WildcardOnly.cs"

        $text = [IO.File]::ReadAllText($target)

        if ($text.Contains("STAGE1_WILDCARD_FORM_FIX_V1")) {
            throw "Этот файл уже содержит данное исправление."
        }

        # Меняем только подготовку формы и параметров выпуска.
        # Создание сайта, DNS, ожидание и проверка TLS остаются прежними.
        $pattern = '(?ms)^[ \t]*phase = "получение формы выпуска";.*?^[ \t]*ct\.ThrowIfCancellationRequested\(\);'
        $rx = [regex]::new($pattern)
        $matches = $rx.Matches($text)

        if ($matches.Count -ne 1) {
            throw (
                "Структура исходника отличается от изученной версии. " +
                "Ожидался один блок подготовки заявки, найдено: " +
                $matches.Count +
                ". Изменения не выполнены."
            )
        }

        $oldBlock = $matches[0].Value

        if (
            -not $oldBlock.Contains('var fields = FormValues(form);') -or
            -not $oldBlock.Contains('["aliases"] = "*." + domain') -or
            -not $oldBlock.Contains('"letsencrypt.generate", request, ct')
        ) {
            throw "Блок заявки отличается от версии в GitHub. Автоматическая замена остановлена."
        }

        $replacement = @'
                // STAGE1_WILDCARD_FORM_FIX_V1
                phase = "получение формы выпуска";

                // crtname — сертификат, name — отдельный CSR.
                // Не используем автоматически подставленное имя CSR.
                var csrName = certificateName + "_csr";

                if (names.Contains(csrName) ||
                    names.Contains(owner + "%#%" + csrName))
                {
                    throw new InvalidOperationException(
                        "Имя CSR уже присутствует в полученном списке: " +
                        csrName + ". Новая заявка не отправлена.");
                }

                // Документированные параметры letsencrypt.generate.
                // aliases относится к сайту и здесь не используется.
                var request = new Dictionary<string, string>(
                    StringComparer.Ordinal)
                {
                    ["out"] = "devel",
                    ["lang"] = "en",
                    ["username"] = owner,
                    ["domain_name"] = domain,
                    ["domain"] = domain + " *." + domain,
                    ["crtname"] = certificateName,
                    ["name"] = csrName,
                    ["email"] = "webmaster@" + domain,
                    ["keylen"] = "2048",
                    ["wildcard"] = "on",
                    ["dns_check"] = "on",

                    // DNS-01 подтверждает TXT, а не A-запись сайта.
                    // Саму DNS-01 проверку не отключаем.
                    ["skip_check_a_record"] = "on",
                    ["enable_cert"] = "on"
                };

                // Получаем форму без sok=ok.
                var form = await api.Call(
                    "letsencrypt.generate", request, ct);

                RequireForm(form, "letsencrypt.generate");
                RequireControls(
                    form, "domain", "crtname", "wildcard", "dns_check");

                // name может быть скрытым параметром формы.
                if (form.Root?.Element("name") is null &&
                    !Controls(form).Contains("name"))
                {
                    throw new InvalidOperationException(
                        "Форма выпуска не подтвердила параметр name для CSR. " +
                        "Заявка не отправлена.");
                }

                var returnedOwner = Value(form, "username").Trim();
                if (returnedOwner.Length > 0 &&
                    !string.Equals(
                        returnedOwner, owner, StringComparison.Ordinal))
                {
                    throw new InvalidOperationException(
                        "Форма выпуска вернула другого владельца. " +
                        "Заявка не отправлена.");
                }

                // Не копируем FormValues(form): возвращённые служебные
                // значения не должны подменять параметры новой заявки.
                var fields = new Dictionary<string, string>(
                    request, StringComparer.Ordinal)
                {
                    ["out"] = "xml",
                    ["sok"] = "ok"
                };

                var requestedDomains = SplitValues(fields["domain"])
                    .ToHashSet(StringComparer.OrdinalIgnoreCase);

                if (requestedDomains.Count != 2 ||
                    !requestedDomains.Contains(domain) ||
                    !requestedDomains.Contains("*." + domain) ||
                    fields["name"] == fields["crtname"])
                {
                    throw new InvalidOperationException(
                        "Некорректные параметры Wildcard-заявки. " +
                        "Отправка остановлена.");
                }

                ct.ThrowIfCancellationRequested();
'@

        $match = $matches[0]

        $patched = (
            $text.Substring(0, $match.Index) +
            $replacement +
            $text.Substring($match.Index + $match.Length)
        )

        # Резервная копия вне дерева компилируемых исходников.
        $backup = Join-Path $work "before-patch"
        New-Item -ItemType Directory -Path $backup | Out-Null

        Copy-Item -LiteralPath $target -Destination (
            Join-Path $backup "DeploymentSchemaFix.WildcardOnly.cs"
        )

        $utf8 = [Text.UTF8Encoding]::new($false)
        [IO.File]::WriteAllText($target, $patched, $utf8)

        $project = Join-Path $copy "TextFileProcessor.csproj"
        $log = Join-Path $work "build.log"

        Write-Host "`n=== ВОССТАНОВЛЕНИЕ ЗАВИСИМОСТЕЙ ==="

        & dotnet restore $project -r win-x64 --nologo 2>&1 |
            Tee-Object -FilePath $log

        if ($LASTEXITCODE -ne 0) {
            throw "Не удалось восстановить зависимости. Журнал: $log"
        }

        Write-Host "`n=== СБОРКА И ПУБЛИКАЦИЯ ==="

        # В копию не переносились bin/obj:
        # компиляция выполняется из исправленных исходников.
        & dotnet publish $project `
            --configuration Release `
            --runtime win-x64 `
            --self-contained true `
            --no-restore `
            --output $output `
            --nologo `
            "-p:Version=1.0.1" `
            "-p:InformationalVersion=Stage1-WildcardFormFix-v1" `
            "-p:UseSharedCompilation=false" `
            2>&1 | Tee-Object -FilePath $log -Append

        if ($LASTEXITCODE -ne 0) {
            throw "Сборка не завершилась успешно. Журнал: $log"
        }

        $exe = Join-Path $output "TextFileProcessor.exe"

        if (-not (Test-Path -LiteralPath $exe -PathType Leaf)) {
            throw "Сборка завершилась, но EXE не найден: $exe"
        }

        $hash = (Get-FileHash -LiteralPath $exe -Algorithm SHA256).Hash

        @(
            "Patch: Stage1-WildcardFormFix-v1"
            "Created: $(Get-Date -Format o)"
            "Executable: $exe"
            "SHA256: $hash"
            "Original source: $source"
            "Changed file: Services\DeploymentSchemaFix.WildcardOnly.cs"
            "Existing request markers: preserved"
        ) | Set-Content -LiteralPath (
            Join-Path $work "BUILD-INFO.txt"
        ) -Encoding UTF8

        Write-Host "`n=== НОВАЯ ПРОГРАММА СОБРАНА ===" -ForegroundColor Green
        Write-Host $exe
        Write-Host "`nОригинальный проект не изменён."
        Write-Host "Серверные операции этим скриптом не запускались."
        Write-Host "Настройки и маркеры заявок не удалялись."
        Write-Host "Запускайте EXE из открывшейся папки, не старый ярлык."

        Start-Process explorer.exe `
            -ArgumentList "/select,`"$exe`""
    }
    catch {
        Write-Host "`nОСТАНОВЛЕНО: $($_.Exception.Message)" -ForegroundColor Red
        Write-Host "Оригинальный проект не изменён."
        Write-Host "Не запускайте промежуточную сборку при ошибке."
    }
}

После успешной сборки откройте выделенный TextFileProcessor.exe, выберите один домен и запустите только создание WWW-домена — не «все процессы». Именно этот обработчик в ваших исходниках выполняет создание сайта и затем выпуск Wildcard.

Критерий завершения — сообщение программы о назначенном Wildcard и успешной TLS-проверке, а не просто успешная сборка или отправленная заявка.

13.09.2026 12:07:31 INFO Очередь завершена. Успешно: 3; ошибок: 0; отменено/пропущено: 0.
13.09.2026 12:07:43 INFO casino743.cfd Запуск выбранного задания 1/1.
13.09.2026 12:07:48 ERROR casino743.cfd Сайт casino743.cfd и оба псевдонима проверены. Этап запроса сертификата не завершён: Wildcard — проверка DNS перед выпуском: Эта версия уже отправляла заявку, но сейчас подходящий сертификат или ожидающая заявка не обнаружены. Автоматический повтор остановлен. Проверьте результат заявки в ISPmanager. Маркер не удалялся.

13.09.2026 12:07:48 WARN Очередь завершена. Выбрано: 1; успешно: 0; ошибок: 1; не начато: 0. Статус выпуска и установки Wildcard этим итогом не подтверждается.
13.09.2026 12:09:08 INFO casino743.cfd Запуск выбранного задания 1/3.
13.09.2026 12:09:12 ERROR casino743.cfd Сайт casino743.cfd и оба псевдонима проверены. Этап запроса сертификата не завершён: Wildcard — проверка DNS перед выпуском: Эта версия уже отправляла заявку, но сейчас подходящий сертификат или ожидающая заявка не обнаружены. Автоматический повтор остановлен. Проверьте результат заявки в ISPmanager. Маркер не удалялся.
13.09.2026 12:09:12 INFO casino753.cfd Запуск выбранного задания 2/3.

13.09.2026 12:09:16 ERROR casino753.cfd Сайт casino753.cfd и оба псевдонима проверены. Этап запроса сертификата не завершён: Wildcard — получение формы выпуска: Форма выпуска не подтвердила параметр name для CSR. Заявка не отправлена.
13.09.2026 12:09:16 INFO casino723.cfd Запуск выбранного задания 3/3.
13.09.2026 12:09:21 ERROR casino723.cfd Сайт casino723.cfd и оба псевдонима проверены. Этап запроса сертификата не завершён: Wildcard — получение формы выпуска: Форма выпуска не подтвердила параметр name для CSR. Заявка не отправлена.
13.09.2026 12:09:21 WARN Очередь завершена. Выбрано: 3; успешно: 0; ошибок: 3; не начато: 0. Статус выпуска и установки Wildcard этим итогом не подтверждается.

Ничего не выпустилось!!



GPT-6 Astra
GPT-6 Astra
Навык активирован: page_content
get_page_content
Содержимое страницы
Размышление


Да, по этому журналу выпуск не подтверждён ни для одного из трёх доменов. В предыдущем исправлении я добавил неверную проверку — извините.

Здесь две разные причины:

casino753.cfd и casino723.cfd — мой код остановил отправку, потому что параметр name отсутствовал в описании формы. Отсутствие поля в форме не доказывает, что API не принимает этот параметр. Эту проверку нужно убрать, сохранив явную передачу имени CSR.
casino743.cfd — сохранился маркер более ранней попытки. Он записывается перед отправкой, поэтому сам по себе не доказывает, что панель приняла заявку. Удалять его вслепую и снова отправлять заявку неправильно: сначала нужен результат прежней попытки в ISPmanager.
Ниже один блок, исправляющий подтверждённую ошибку для двух незаблокированных доменов. Он найдёт копию проекта из предыдущего блока, создаст новую отдельную копию, уберёт только ошибочную проверку и соберёт EXE. Оригинал, сервер и маркеры не изменяет.

Закройте программу и выполните в PowerShell
& {
    $ErrorActionPreference = "Stop"

    try {
        if (Get-Process -Name TextFileProcessor -ErrorAction SilentlyContinue) {
            throw "Закройте TextFileProcessor перед сборкой."
        }

        if (-not (Get-Command dotnet -ErrorAction SilentlyContinue)) {
            throw "Не найден .NET SDK — команда dotnet недоступна."
        }

        if (-not (Get-Command robocopy.exe -ErrorAction SilentlyContinue)) {
            throw "Не найдена robocopy.exe."
        }

        $desktop = [Environment]::GetFolderPath("Desktop")
        $relative = "Services\DeploymentSchemaFix.WildcardOnly.cs"

        # Ищем проект, созданный предыдущим исправлением.
        $candidates = @(
            Get-ChildItem -LiteralPath $desktop -Directory `
                -Filter "Stage1-WildcardFix-*" |
            Sort-Object LastWriteTime -Descending
        )

        $source = $null

        foreach ($folder in $candidates) {
            $candidate = Join-Path $folder.FullName "source"
            $file = Join-Path $candidate $relative

            if (Test-Path -LiteralPath $file -PathType Leaf) {
                $content = [IO.File]::ReadAllText($file)

                if ($content.Contains("STAGE1_WILDCARD_FORM_FIX_V1")) {
                    $source = $candidate
                    break
                }
            }
        }

        if (-not $source) {
            throw (
                "На рабочем столе не найден source внутри " +
                "Stage1-WildcardFix-* с предыдущим исправлением."
            )
        }

        foreach ($name in @(
            "TextFileProcessor.csproj",
            "App.xaml",
            "MainWindow.xaml",
            "app.manifest"
        )) {
            if (-not (Test-Path -LiteralPath (Join-Path $source $name))) {
                throw "В проекте отсутствует $name. Сборка остановлена."
            }
        }

        $original = [IO.File]::ReadAllText(
            (Join-Path $source $relative)
        ).Replace("`r`n", "`n")

        # Удаляем строго известную ошибочную проверку.
        $badCheck = @'
                // name может быть скрытым параметром формы.
                if (form.Root?.Element("name") is null &&
                    !Controls(form).Contains("name"))
                {
                    throw new InvalidOperationException(
                        "Форма выпуска не подтвердила параметр name для CSR. " +
                        "Заявка не отправлена.");
                }
'@
        $badCheck = $badCheck.Replace("`r`n", "`n")

        $count = [regex]::Matches(
            $original,
            [regex]::Escape($badCheck)
        ).Count

        if ($count -ne 1) {
            throw (
                "Ожидалась одна известная ошибочная проверка; найдено: " +
                $count + ". Исходники не изменены."
            )
        }

        $replacement = @'
                // STAGE1_WILDCARD_FORM_FIX_V2
                // name передаётся явно как документированный параметр API.
                // Наличие отдельного поля name в UI-форме не требуется.
'@

        $patched = $original.Replace($badCheck, $replacement)

        if (
            -not $patched.Contains('["name"] = csrName') -or
            -not $patched.Contains('["domain"] = domain + " *." + domain')
        ) {
            throw "Не найдены обязательные параметры CSR и доменов. Остановлено."
        }

        $stamp = Get-Date -Format "yyyyMMdd-HHmmss"
        $suffix = [Guid]::NewGuid().ToString("N").Substring(0, 6)
        $work = Join-Path $desktop "Wildcard-FormFixV2-$stamp-$suffix"
        $copy = Join-Path $work "source"
        $output = Join-Path $work "program"
        $log = Join-Path $work "build.log"

        Write-Host "`nИсходный проект: $source"
        Write-Host "Новая копия:     $copy"

        New-Item -ItemType Directory -Path $copy -Force | Out-Null

        & robocopy.exe $source $copy /E /XJ /R:1 /W:1 `
            /XD bin obj .git .vs `
            /XF *.user *.suo `
            /NFL /NDL /NJH /NJS /NP | Out-Null

        if ($LASTEXITCODE -ge 8) {
            throw "Ошибка копирования проекта."
        }

        # Резервный исходник — вне дерева сборки.
        Copy-Item -LiteralPath (Join-Path $source $relative) `
            -Destination (Join-Path $work "WildcardOnly.before.cs.txt")

        [IO.File]::WriteAllText(
            (Join-Path $copy $relative),
            $patched,
            [Text.UTF8Encoding]::new($false)
        )

        $project = Join-Path $copy "TextFileProcessor.csproj"

        Write-Host "`n=== СБОРКА НОВОЙ ПРОГРАММЫ ==="

        & dotnet publish $project `
            --configuration Release `
            --runtime win-x64 `
            --self-contained true `
            --output $output `
            --nologo `
            "-p:Version=1.0.2" `
            "-p:InformationalVersion=Wildcard-FormFixV2" `
            "-p:UseSharedCompilation=false" `
            2>&1 | Tee-Object -FilePath $log

        if ($LASTEXITCODE -ne 0) {
            throw "Сборка завершилась ошибкой. Журнал: $log"
        }

        $exe = Join-Path $output "TextFileProcessor.exe"

        if (-not (Test-Path -LiteralPath $exe -PathType Leaf)) {
            throw "EXE не найден после сборки: $exe"
        }

        Write-Host "`nГОТОВО: ошибочная проверка name удалена." `
            -ForegroundColor Green
        Write-Host $exe
        Write-Host "`nСборка — не подтверждение выпуска сертификата."
        Write-Host "Маркеры прежних попыток сохранены."
        Write-Host "Серверные операции этим блоком не выполнялись."

        Start-Process explorer.exe `
            -ArgumentList "/select,`"$exe`""
    }
    catch {
        Write-Host "`nОСТАНОВЛЕНО: $($_.Exception.Message)" `
            -ForegroundColor Red
        Write-Host "Исходный проект не изменён."
    }
}

После сборки: запустите выделенный EXE и выполните создание WWW-домена только для casino753.cfd. Этот блок устраняет конкретную остановку из журнала; весь выпуск на вашей панели я проверить не могу.

Для casino743.cfd нужен отдельный факт: откройте в ISPmanager раздел SSL-сертификаты, найдите записи этого домена и пришлите их имена, статусы и текст ошибки выпуска, если он есть. Если записей нет — сообщите это. Пароли, API-ключи и закрытый ключ сертификата не присылайте. Без этих данных нельзя обоснованно решить, продолжать старую заявку или разрешать новую.

13.09.2026 12:36:11 ERROR casino723.cfd Сайт casino723.cfd и оба псевдонима проверены. Этап запроса сертификата не завершён: Wildcard — отправка заявки Wildcard: ISPmanager: функция letsencrypt.generate; ошибка exists; поле crtname.
НЕ РАБОТАЕТ!
НЕ ВЫПУСКАЕТ!!!
СРОЧНО ПРИШЛИ НОРМАЛЬНУЮ СБОРКУ!
СКОЛЬКО МОЖНО!!!!!!!!!!!!!
GPT-6 Astra
GPT-6 Astra
get_page_content
Содержимое страницы
get_page_content
Содержимое страницы
get_page_content
Содержимое страницы
get_page_content
Содержимое страницы
Вы правы: предыдущие исправления проблему не решили. Извините — я предложил изменения, не подтвердив причину конфликта на вашей панели.

Сейчас ISPmanager возвращает exists именно для crtname — имени сертификата, а не для CSR. Менять name дальше бессмысленно.

Я повторно прочитал исходники. В них есть важное несоответствие:

список сертификатов запрашивается с su=владелец;
заявка отправляется без su, только с username=владелец;
«свободное» имя выбирается по полученному списку.
Это возможная причина расхождения, но журнал её ещё не доказывает. Также после отказа остаётся маркер outcome-unknown, блокирующий следующую попытку.

Готового проверенного EXE у меня нет: здесь я не могу собрать Windows-приложение и испытать его на вашей панели. Не буду выдавать очередную замену строк за рабочую сборку. Сейчас нужны ответы самой панели — их можно получить одним блоком, без нового выпуска и без удаления сертификатов.

Выполните этот блок в SSH-терминале сервера с ISPmanager, не в PowerShell
Он запросит владельца сайта — то же значение, которое указано в настройках программы. Затем покажет списки сертификатов в двух контекстах и поля формы выпуска. Пароли, приватные ключи и полный XML не выводит. Запросов с sok=ok нет.

python3 - <<'PY'
import getpass
import json
import os
import subprocess
import xml.etree.ElementTree as ET

mgr = "/usr/local/mgr5/sbin/mgrctl"
domain = "casino723.cfd"

if not os.path.isfile(mgr):
    raise SystemExit("mgrctl не найден. Блок нужен на сервере с ISPmanager.")

owner = input("Владелец сайта из настроек программы: ").strip()
if not owner or any(c.isspace() for c in owner):
    raise SystemExit("Нужно непустое имя владельца без пробелов.")

def emit(label, value):
    print(label + ": " + json.dumps(value, ensure_ascii=False))

def read(label, function, params):
    print("\n=== " + label + " ===")
    args = [mgr, "-m", "ispmgr", function, "out=devel"]
    args += [k + "=" + v for k, v in params.items()]
    try:
        result = subprocess.run(
            args, capture_output=True, text=True, timeout=60
        )
    except (OSError, subprocess.TimeoutExpired):
        print("Не удалось выполнить чтение. Сырой ответ не выводится.")
        return None

    try:
        root = ET.fromstring(result.stdout)
    except ET.ParseError:
        emit("Ошибка", {
            "exit_code": result.returncode,
            "description": "Ответ не XML; сырой ответ скрыт"
        })
        return None

    error = root.find(".//error")
    if error is not None:
        emit("Ошибка API", {
            k: error.get(k, "") for k in ("type", "object", "code")
        })
        return None

    if result.returncode != 0:
        emit("Код завершения", result.returncode)
        return None
    return root

def certificates(label, params):
    root = read(label, "sslcert", params)
    if root is None:
        return

    rows = root.findall("elem")
    emit("Количество возвращённых записей", len(rows))
    matched = 0

    fields = (
        "name", "owner", "username", "info", "type",
        "active", "valid_after",
        "letsencrypt_verifying", "letsencrypt_txt",
        "letsencrypt_failed"
    )

    for row in rows:
        name = row.findtext("name", "")
        info = row.findtext("info", "")
        if domain.lower() not in (name + " " + info).lower():
            continue
        matched += 1
        emit("Сертификат", {
            field: row.findtext(field, "")
            for field in fields
            if row.find(field) is not None
        })

    emit("Записей с указанным доменом", matched)

certificates("Список в текущем контексте", {})
certificates("Список с su=владелец", {"su": owner})

for label, extra in (
    ("Форма без su — как в текущей заявке", {}),
    ("Форма с su=владелец", {"su": owner}),
):
    params = {
        "username": owner,
        "domain_name": domain,
        "wildcard": "on",
        "dns_check": "on",
    }
    params.update(extra)
    root = read(label, "letsencrypt.generate", params)
    if root is None:
        continue

    allowed = (
        "username", "domain_name", "domain", "crtname",
        "name", "wildcard", "dns_check", "from_webdomain"
    )
    emit("Значения формы", {
        field: root.findtext(field, "")
        for field in allowed
        if root.find(field) is not None
    })

    controls = set()
    for metadata in root.findall("metadata"):
        for element in metadata.iter():
            if element.tag in ("input", "select", "textarea"):
                name = element.get("name", "")
                if name in allowed:
                    controls.add(name)
    emit("Найденные поля", sorted(controls))

print("\nДиагностика завершена. Выпуск и удаление не запрашивались.")
PY

Пришлите вывод этого блока. Он позволит проверить, какое имя уже занято и совпадает ли контекст владельца. Пока не удаляйте маркеры и не запускайте очередь повторно: это не исправляет exists/crtname и затрудняет определение состояния прежней заявки.

> PY
Владелец сайта из настроек программы: Traceback (most recent call last):
File "<stdin>", line 26, in <module>
EOFError: EOF when reading a line
[root@mail ~]#
GPT-6 Astra
GPT-6 Astra
Это моя ошибка в диагностическом блоке. При запуске python3 - <<'PY' стандартный ввод занят текстом скрипта, поэтому input() получает конец ввода и вызывает EOFError. До запросов к ISPmanager выполнение не дошло.

В предыдущем блоке замените строку:

owner = input("Владелец сайта из настроек программы: ").strip()

на эти четыре строки без отступа перед print и with:

print("Владелец сайта из настроек программы: ", end="", flush=True)
with open("/dev/tty", "r") as terminal:
    owner = terminal.readline().strip()

Остальной код оставьте без изменений и повторно выполните весь блок от python3 - <<'PY' до завершающей строки PY в SSH-терминале. Теперь имя владельца будет считываться непосредственно из терминала.

Это исправляет запуск диагностики, не сам выпуск сертификата. Пришлите её вывод — по нему можно будет проверить конфликт crtname, не удаляя сертификаты и не отправляя новые заявки.

> PY
Владелец сайта из настроек программы: root

=== Список в текущем контексте ===
Traceback (most recent call last):
File "<stdin>", line 172, in <module>
File "<stdin>", line 114, in certificates
File "<stdin>", line 58, in read
File "/usr/lib64/python3.6/subprocess.py", line 423, in run
with Popen(*popenargs, **kwargs) as process:
TypeError: __init__() got an unexpected keyword argument 'capture_output'
[root@mail ~]#
GPT-6 Astra
GPT-6 Astra
Размышление


На сервере Python 3.6, а я использовал параметры для Python 3.7+. Это моя ошибка совместимости. На этом запуске запрос к ISPmanager ещё не выполнился.

В диагностическом блоке замените:

        result = subprocess.run(
            args, capture_output=True, text=True, timeout=60
        )

на совместимый вариант с теми же отступами:

        result = subprocess.run(
            args,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            universal_newlines=True,
            timeout=60
        )

Исправление чтения через /dev/tty оставьте. Затем запустите весь диагностический блок повторно.

Важно про введённое root: нужно значение владельца WWW-домена из настроек программы, а не SSH-логин. Если там действительно указан root, оставьте его; если другой пользователь — введите именно его. Это существенно для проверки конфликта имени сертификата.

Блок по-прежнему только читает данные — ничего не выпускает и не удаляет.

> PY
Владелец сайта из настроек программы: root

=== Список в текущем контексте ===
Количество возвращённых записей: 194
Записей с указанным доменом: 0

=== Список с su=владелец ===
Количество возвращённых записей: 194
Записей с указанным доменом: 0

=== Форма без su — как в текущей заявке ===
Значения формы: {"username": "www-root", "domain_name": "casino723.cfd", "domain": "casino723.cfd", "crt
name": "casino723.cfd_le2", "wildcard": "on", "dns_check": "on", "from_webdomain": ""}
Найденные поля: ["crtname", "dns_check", "domain", "domain_name", "from_webdomain", "username", "wildcar
d"]

=== Форма с su=владелец ===
Значения формы: {"username": "www-root", "domain_name": "casino723.cfd", "domain": "casino723.cfd", "crt
name": "casino723.cfd_le2", "wildcard": "on", "dns_check": "on", "from_webdomain": ""}
Найденные поля: ["crtname", "dns_check", "domain", "domain_name", "from_webdomain", "username", "wildcar
d"]

Диагностика завершена. Выпуск и удаление не запрашивались.
[root@mail ~]#
GPT-6 Astra
GPT-6 Astra
get_page_content
Содержимое страницы
Диагностика выполнилась. Важное расхождение: вы передали root, но обе формы выпуска вернули username=www-root. Значит, проверка с su=root не проверила список от имени пользователя, которого форма предлагает для сертификата.

Что теперь известно:

ISPmanager предлагает имя casino723.cfd_le2. Это значение формы, не гарантия, что отправка пройдёт.
Два списка вернули по 194 записи без совпадений по name и info. Моя диагностика пропустила поле key, поэтому утверждать, что записи домена отсутствуют, пока нельзя.
Причина отказа остаётся конкретной: конфликт имени crtname. Выпуск этим выводом не подтверждён.
Ниже цельный блок для Python 3.6, без ручных исправлений и ввода владельца. Он прочитает владельца непосредственно из карточки сайта, затем проверит сертификаты в соответствующем контексте, включая key. Ничего не выпускает, не сохраняет и не удаляет.

Выполните в том же SSH-терминале:

python3 - <<'PY'
import json
import subprocess
import xml.etree.ElementTree as ET

MGR = "/usr/local/mgr5/sbin/mgrctl"
DOMAIN = "casino723.cfd"

def show(label, data):
    print(label + ": " + json.dumps(data, ensure_ascii=False))

def read(function, params):
    args = [MGR, "-m", "ispmgr", function, "out=devel"]
    args.extend(k + "=" + v for k, v in params.items())
    try:
        result = subprocess.run(
            args,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            universal_newlines=True,
            timeout=60
        )
    except (OSError, subprocess.TimeoutExpired) as exc:
        raise SystemExit(
            "Чтение не выполнено: " + type(exc).__name__
        )

    try:
        root = ET.fromstring(result.stdout)
    except ET.ParseError:
        raise SystemExit(
            "Ответ не XML. Код завершения: {}. "
            "Сырой ответ скрыт.".format(result.returncode)
        )

    error = next(
        (e for e in root.iter()
         if e.tag.split("}")[-1] == "error"), None
    )
    if error is not None:
        show("Ошибка API", {
            k: error.get(k, "")
            for k in ("type", "object", "code")
        })
        raise SystemExit("Остановлено без изменений.")

    if result.returncode != 0:
        raise SystemExit(
            "Ошибка mgrctl: {}".format(result.returncode)
        )
    return root

print("=== Карточка сайта ===")
site = read("webdomain.edit", {"elid": DOMAIN})
name = site.findtext("name", "").strip()
owner = site.findtext("owner", "").strip()

show("Сайт", {
    "name": name,
    "owner": owner,
    "secure": site.findtext("secure", ""),
    "ssl_cert": site.findtext("ssl_cert", "")
})

if name.lower().rstrip(".") != DOMAIN or not owner:
    raise SystemExit(
        "Имя сайта или владелец не подтверждены. "
        "Дальнейшие запросы не выполнялись."
    )

selected = site.findtext("ssl_cert", "").strip()
allowed = (
    "key", "name", "owner", "username", "info", "type",
    "active", "valid_after", "letsencrypt_verifying",
    "letsencrypt_txt", "letsencrypt_failed"
)

for label, params in (
    ("Текущий контекст", {}),
    ("Контекст владельца " + owner, {"su": owner}),
):
    print("\n=== " + label + " ===")
    root = read("sslcert", params)
    rows = root.findall("elem")
    show("Количество записей", len(rows))
    matches = 0

    for row in rows:
        data = {
            field: row.findtext(field, "").strip()
            for field in allowed
            if row.find(field) is not None
        }
        identifiers = [
            data.get("key", ""), data.get("name", "")
        ]
        related = any(
            DOMAIN in data.get(field, "").lower()
            for field in ("key", "name", "info")
        )
        assigned = bool(selected) and any(
            value and (
                selected == value or
                selected == owner + "%#%" + value
            )
            for value in identifiers
        )
        if related or assigned:
            matches += 1
            show("Запись", data)

    show("Совпадений по домену или назначению сайту", matches)
    if rows and matches == 0:
        show("Названия полей первой записи", sorted(set(
            child.tag for child in rows[0]
        )))

print("\nЗавершено. Только чтение; выпуск не запрашивался.")
PY

Пришлите вывод. Эта проверка отделит ошибку контекста владельца от ошибки чтения списка сертификатов. Менять настройки на www-root, удалять маркеры или подбирать новое имя вслепую сейчас не предлагаю.

> PY
=== Карточка сайта ===
Сайт: {"name": "casino723.cfd", "owner": "www-root", "secure": "off", "ssl_cert": "selfsigned"}

=== Текущий контекст ===
Количество записей: 194
Совпадений по домену или назначению сайту: 0
Названия полей первой записи: ["active", "info", "is_letsencrypt", "key", "letsencrypt", "name", "owner"
, "type", "valid_after", "webdomains"]

=== Контекст владельца www-root ===
Количество записей: 194
Совпадений по домену или назначению сайту: 0
Названия полей первой записи: ["active", "info", "is_letsencrypt", "key", "letsencrypt", "name", "type",
"valid_after", "webdomains"]

Завершено. Только чтение; выпуск не запрашивался.
[root@mail ~]#
GPT-6 Astra
GPT-6 Astra
Навык активирован: page_content
Теперь владелец подтверждён самой панелью: www-root, а не root. Моя прежняя рекомендация оставить root была неверной для этого сайта.

Из вывода следует:

В карточке сайта HTTPS выключен: secure=off.
В поле сертификата указано selfsigned — это не подтверждение выпуска Let’s Encrypt.
В обоих возвращённых списках совпадений по домену и назначенному сертификату не обнаружено. Это не доказывает отсутствие заявки во всех внутренних состояниях панели.
Причину конфликта crtname эта проверка пока не установила.
Что исправить в программе
В настройке «Владелец WWW-домена» замените:

root → www-root

SSH-логин root менять не нужно: это отдельная настройка.

В показанном ранее исходнике есть важный момент: программа получает значения формы, а затем перезаписывает их параметрами своего запроса:

foreach (var pair in request)
    fields[pair.Key] = pair.Value;

Поэтому возвращённое панелью username=www-root могло заменяться обратно на root из настроек. Исправление владельца устраняет это конкретное расхождение, но ещё не гарантирует устранение конфликта имени.

Пока не запускайте повторный выпуск и не удаляйте маркеры заявки. Пришлите полный текст последней уже полученной ошибки программы, включая название этапа и ответ ISPmanager, если он есть. Пароли, токены и параметры авторизации скройте. Сейчас нужен именно отказ операции, а не ещё один одинаковый список сертификатов.

В программе и так Владелец WWW-домена указан всегда был www-root
Я че то не пойму!!
GPT-6 Astra
GPT-6 Astra
Вы всё указали правильно. Я ошибочно принял root, введённый в SSH-диагностике, за настройку программы. Это разные вещи. Извините — я запутал вас и дал необоснованный совет менять владельца.

В программе ничего менять с www-root не нужно. Панель подтвердила, что сайт принадлежит именно этому пользователю.

Что действительно установлено:

Программа получила отказ exists по полю crtname при отправке заявки.
Наши проверки списка не нашли соответствующую запись.
Почему возникает этот конфликт, пока не установлено. Моё объяснение через неправильного владельца не подтверждается. Утверждение о перезаписи владельца в вашей текущей версии тоже было преждевременным.
Вы уже прислали нужный текст ошибки. Повторно присылать его или выполнять те же проверки не требуется.

Для исправления теперь нужен актуальный код отправки заявки из той версии, которую вы запускаете, а не очередные предположения по настройкам. Пришлите исходный .cs-файл, где встречаются letsencrypt.generate и crtname, либо архив текущего проекта без паролей, токенов и сохранённых настроек доступа. Нужно проверить, какое имя программа фактически отправляет и как обрабатывает ответ exists.

Готовой проверенной сборки у меня сейчас нет. Обещать её работоспособность без проверки этого кода было бы повторением моей ошибки.

да от куда знаю который это файл? пришли нормальный код повершел который соберет и выдаст то то тебе нужно
GPT-6 Astra
GPT-6 Astra
Да, вам не нужно угадывать файл. Код ниже сам найдёт C#-исходники, связанные с выпуском сертификата, и соберёт их в один текстовый отчёт.

Запускайте в Windows PowerShell на компьютере с проектом, не в SSH на сервере. Откроется окно — выберите папку с исходниками программы. Если они в ZIP, сначала распакуйте его. Скрипт проверит и вложенные папки.

Он не запускает программу, не обращается к ISPmanager, не меняет исходники и ничего никуда не отправляет. Компилировать EXE для этой проверки не нужно.

Вставьте весь блок в PowerShell:

& {
    $ErrorActionPreference = 'Stop'

    try {
        Add-Type -AssemblyName System.Windows.Forms

        $dialog = New-Object System.Windows.Forms.FolderBrowserDialog
        $dialog.Description = 'Выберите папку с исходниками программы — вложенные папки тоже будут проверены'
        $dialog.ShowNewFolderButton = $false

        try {
            $selected = $dialog.ShowDialog()
            if ($selected -ne [System.Windows.Forms.DialogResult]::OK) {
                Write-Host 'Отменено. Ничего не изменено.'
                return
            }
            $root = $dialog.SelectedPath
        }
        finally {
            $dialog.Dispose()
        }

        Write-Host ''
        Write-Host 'Ищу исходники выпуска сертификата…' -ForegroundColor Cyan

        # Только исходники C#. Настройки, ключи, журналы и EXE не читаются.
        # Служебные каталоги и ссылки на другие каталоги пропускаются.
        $skipDirectories = @(
            '.git', '.vs', 'bin', 'obj', 'packages',
            'node_modules', '.idea'
        )

        $directories = New-Object 'System.Collections.Generic.Stack[string]'
        $directories.Push($root)

        $sourceFiles = New-Object 'System.Collections.Generic.List[string]'
        $skippedDirectories = 0

        while ($directories.Count -gt 0) {
            $current = $directories.Pop()

            try {
                $entries = @(Get-ChildItem -LiteralPath $current -Force -ErrorAction Stop)
            }
            catch {
                $skippedDirectories++
                continue
            }

            foreach ($entry in $entries) {
                if (($entry.Attributes -band [System.IO.FileAttributes]::ReparsePoint) -ne 0) {
                    continue
                }

                if ($entry.PSIsContainer) {
                    if ($skipDirectories -notcontains $entry.Name) {
                        $directories.Push($entry.FullName)
                    }
                }
                elseif (
                    $entry.Extension -ieq '.cs' -and
                    $entry.Name -notmatch '(?i)\.(designer|g|g\.i)\.cs$'
                ) {
                    $sourceFiles.Add($entry.FullName)
                }
            }
        }

        if ($sourceFiles.Count -eq 0) {
            Write-Host ''
            Write-Host 'В выбранной папке нет исходников .cs.' -ForegroundColor Yellow
            Write-Host 'Если здесь только EXE, выберите папку распакованного исходного проекта.'
            Write-Host 'Из одного EXE этот скрипт исходники не извлекает.'
            return
        }

        $pattern = '(?i)letsencrypt|crtname|outcome[-_ ]?unknown|class\s+IspmanagerAutomationService|class\s+DeploymentSchemaFix'

        $report = New-Object System.Text.StringBuilder
        [void]$report.AppendLine('Диагностика исходников: выпуск сертификата ISPmanager')
        [void]$report.AppendLine('Создано: ' + (Get-Date -Format 'yyyy-MM-dd HH:mm:ss'))
        [void]$report.AppendLine('Программа не запускалась. Сборка EXE не выполнялась.')
        [void]$report.AppendLine('Включены только подходящие исходные файлы .cs.')
        [void]$report.AppendLine('ВНИМАНИЕ: секреты, записанные прямо в исходниках, автоматически не удалены.')
        [void]$report.AppendLine('')

        $prefix = $root.TrimEnd('\') + '\'
        $matchedCount = 0
        $unreadableCount = 0

        foreach (%%MATH_INLINE_4%%sourceFiles | Sort-Object)) {
            try {
                $text = [System.IO.File]::ReadAllText($file)
            }
            catch {
                $unreadableCount++
                continue
            }

            if ($text -notmatch $pattern) {
                continue
            }

            $matchedCount++
            $relativePath = $file.Substring($prefix.Length)

            [void]$report.AppendLine(('=' * 78))
            [void]$report.AppendLine('ФАЙЛ: ' + $relativePath)
            [void]$report.AppendLine(('=' * 78))
            [void]$report.AppendLine($text)
            [void]$report.AppendLine('')
        }

        if ($matchedCount -eq 0) {
            Write-Host ''
            Write-Host ('Проверено файлов .cs: ' + $sourceFiles.Count)
            Write-Host 'Код выпуска сертификата не найден.' -ForegroundColor Yellow
            Write-Host 'Попробуйте выбрать папку уровнем выше или другой архив исходников.'
            Write-Host ('Непрочитанных файлов: ' + $unreadableCount)
            Write-Host ('Недоступных папок: ' + $skippedDirectories)
            return
        }

        [void]$report.AppendLine(('=' * 78))
        [void]$report.AppendLine('Найдено исходных файлов .cs: ' + $sourceFiles.Count)
        [void]$report.AppendLine('Включено файлов: ' + $matchedCount)
        [void]$report.AppendLine('Непрочитанных файлов: ' + $unreadableCount)
        [void]$report.AppendLine('Недоступных папок: ' + $skippedDirectories)

        $desktop = [Environment]::GetFolderPath(
            [Environment+SpecialFolder]::DesktopDirectory
        )

        if ([string]::IsNullOrWhiteSpace($desktop) -or
            -not (Test-Path -LiteralPath $desktop -PathType Container)) {
            $desktop = [System.IO.Path]::GetTempPath()
        }

        $suffix = [Guid]::NewGuid().ToString('N').Substring(0, 8)
        $filename = 'ISPmanager-code-' + (Get-Date -Format 'yyyyMMdd-HHmmss') +
                    '-' + $suffix + '.txt'
        $output = Join-Path $desktop $filename

        $utf8 = New-Object System.Text.UTF8Encoding($true)
        [System.IO.File]::WriteAllText($output, $report.ToString(), $utf8)

        Write-Host ''
        Write-Host ('Готово. Собрано файлов: ' + $matchedCount) -ForegroundColor Green
        Write-Host 'Отчёт сохранён здесь:'
        Write-Host $output -ForegroundColor Cyan
        Write-Host ''
        Write-Host 'Перед отправкой проверьте отчёт на пароли и токены в исходниках.'
        Write-Host 'Исходные файлы не изменены. Ничего не отправлено автоматически.'

        try {
            Start-Process -FilePath 'notepad.exe' -ArgumentList ('"' + $output + '"')
        }
        catch {
            Write-Host 'Не удалось открыть Блокнот. Откройте отчёт по указанному пути.'
        }
    }
    catch {
        Write-Host ''
        Write-Host ('Ошибка: ' + $_.Exception.Message) -ForegroundColor Red
        Write-Host 'Скопируйте сюда этот текст ошибки.'
    }
}

Прикрепите сюда полученный файл ISPmanager-code-….txt. Конфиги и сохранённые настройки скрипт не собирает, но если пароль или токен записан непосредственно в коде, замените его в отчёте на [СКРЫТО] перед отправкой. По этому файлу я смогу проверить фактическое формирование crtname и обработку ошибки — без новых догадок о владельце сайта.

>>
ParserError:
Line |
91 | foreach (%%MATH_INLINE_4%%sourceFiles | Sort-Object)) {
| ~
| Missing variable name after foreach. The correct form is: foreach ($a in $b) {...}
PS C:\Users\User\Desktop\DomainProgram-Fix-20260910-231222\source\346Jvfch75v6f634vf765-60ecea8c7bc346c3ed310f3c416beb6549390cac>
GPT-6 Astra
GPT-6 Astra
Размышление




В строку кода попал посторонний маркер %%MATH_INLINE_4%% вместо переменной PowerShell. Это повреждение текста блока, не ошибка ваших исходников.

Вы уже находитесь в папке проекта. Вставьте новый блок целиком в это же окно PowerShell. Он специально написан без переменных со знаком $, чтобы исключить такую подмену.

Блок найдёт нужные исходники и запишет их в ISPmanager-code-report.txt в текущей папке. Исходники не изменяет, программу не запускает, запросы на сервер не отправляет. Если такой отчёт уже существует, перезапишет только его.

& {
    Write-Host 'Ищу код выпуска сертификата...' -ForegroundColor Cyan

    Get-ChildItem -LiteralPath . -Recurse -File -Filter '*.cs' -ErrorAction Stop |
        Where-Object -Property FullName -NotMatch '\\(bin|obj|\.git|\.vs|packages|node_modules)\\' |
        Select-String -Pattern 'letsencrypt|crtname|outcome[-_ ]?unknown|class\s+IspmanagerAutomationService|class\s+DeploymentSchemaFix' -List -ErrorAction Stop |
        Select-Object -ExpandProperty Path -Unique |
        Get-Item -ErrorAction Stop |
        Select-String -Pattern '^' -ErrorAction Stop |
        ForEach-Object -MemberName ToString |
        Set-Content -LiteralPath '.\ISPmanager-code-report.txt' -Encoding UTF8 -ErrorAction Stop

    Write-Host ''
    Write-Host 'Проверьте размер отчёта ниже. Length — размер в байтах.' -ForegroundColor Green

    Get-Item -LiteralPath '.\ISPmanager-code-report.txt' -ErrorAction Stop |
        Select-Object FullName, Length |
        Format-List

    Write-Host 'Перед отправкой удалите из отчёта пароли и токены, если они записаны в исходниках.'
    Write-Host 'Если Length равен 0, подходящие исходники не найдены.'

    Start-Process -FilePath 'notepad.exe' -ArgumentList '.\ISPmanager-code-report.txt'
}

Прикрепите полученный ISPmanager-code-report.txt сюда. В нём будут тексты найденных файлов с путями и номерами строк. Настройки и ключевые файлы отдельно не собираются, но секреты, вписанные прямо в .cs, могут попасть в отчёт — их нужно скрыть перед отправкой.

https://github.com/raliya/346Jvfch75v6f634vf765/blob/main/ISPmanager-code-report.txt
GPT-6 Astra
GPT-6 Astra
get_page_content
Содержимое страницы
Отчёт открылся. В коде нашёл два конкретных дефекта: программа сама выбирает имя начиная с _le1 и не использует crtname, возвращённое формой; после ошибки exists она оставляет тот же блокирующий маркер, что и после таймаута. Проверяю, как исправить выбор имени, не разрешая повторную заявку с неизвестным результатом.

Навык активирован: web_search
get_serp
Поисковая выдача
Результаты (9)
Отчёт открыл. Нужный код найден: Services\DeploymentSchemaFix.WildcardOnly.cs. Искать и присылать исходники ещё раз не нужно.

Теперь видно, что проблема не в вашей настройке www-root. В текущем коде даже есть проверка совпадения владельца перед отправкой. Мои прежние объяснения про его подмену были неверными.

Что обнаружено в коде
1. Программа сама выбирает имя сертификата и игнорирует предложенное панелью.

В строках 179–198 она перебирает:

casino723.cfd_le1
casino723.cfd_le2
casino723.cfd_le3

Первое имя, которого нет в полученном списке, считается свободным. Однако отсутствие имени в этом списке не гарантирует, что сервер разрешит его использовать.

Далее программа получает форму, но не использует возвращённый crtname. В отправку уходит самостоятельно выбранное имя. В вашей диагностике панель предложила casino723.cfd_le2. Если программа получила такой же список без совпадений, она выбрала бы casino723.cfd_le1. Это уже конкретное расхождение, а не предположение о владельце.

2. После ошибки остаётся блокировка повторной попытки.

В строках 288–301, непосредственно перед отправкой, записывается файл:

outcome-unknown
имя_отправленного_сертификата

При ответе exists/crtname этот файл не обновляется. Следующий запуск останавливается только из-за его наличия — строки 157–163. Поэтому одной замены алгоритма выбора имени недостаточно: старая попытка останется заблокированной.

Получим точное имя из уже сохранённой попытки
Новый выпуск для этого не нужен. Вставьте в Windows PowerShell этот короткий блок. Он прочитает только маркеры попыток для casino723.cfd, ничего не изменит и не обратится к серверу. Переменных со знаком доллара здесь нет.

& {
    Write-Host 'Сохранённые попытки для casino723.cfd:' -ForegroundColor Cyan

    Get-ChildItem -LiteralPath (
        [System.IO.Path]::Combine(
            [Environment]::GetFolderPath(
                [Environment+SpecialFolder]::LocalApplicationData
            ),
            'TextFileProcessor',
            'deployment-certificate-requests'
        )
    ) -File -Filter '*.wc2-attempt' -ErrorAction Stop |
        Select-String -Pattern '^casino723\.cfd_le[0-9]+$' -Context 1,0 |
        ForEach-Object -MemberName ToString |
        Out-Host

    Write-Host ''
    Write-Host 'Чтение завершено. Если совпадений выше нет — сообщите об этом.'
    Write-Host 'Маркеры не изменялись. Заявки не отправлялись.'
}

Пришлите вывод этого блока. Это позволит сопоставить имя сохранённой попытки с вашим отказом exists/crtname, прежде чем разрешать повтор. Удалять все маркеры или просто начинать новый перебор имён нельзя считать исправлением: среди других попыток могут быть принятые сервером заявки.

Направление исправления теперь определено: учитывать имя из формы, раздельно сохранять явный отказ API и неизвестный исход при обрыве связи, не повторять отправку автоматически. Но проверенной рабочей сборкой это пока не является — я не буду снова утверждать обратное.